# Percorso di aggiornamento upstream v2.58.0 → v2.63.0

| Campo | Valore |
| --- | --- |
| Data di avvio | 2026-09-23 |
| Branch di partenza | `feature/oslms` @ `9f36bf4e8` |
| Branch del percorso | `merge/upstream-v2.63.0` |
| Base (ultima release upstream fusa) | `v2.58.0` |
| Arrivo | `v2.63.0` |
| Baseline del rilevatore | 0 errori, 31 avvisi C4 (test non ancora collegati) |

## Piano iniziale

Da `python3 scripts/upstream_plan.py`: ampiezza reale dal diff fra tag, non dalle note di release.

```
Release  Data        Commit  File  frappe-ui                       Rotture  Voci  Non censiti  Congelate
v2.58.1  2026-07-07  23      19    ^1.0.0-beta.7                   0        9     0            0
v2.59.0  2026-07-16  91      195   ^1.0.0-beta.7 → ^1.0.0-beta.24  0        20    82           1
v2.60.0  2026-07-24  15      47    ^1.0.0-beta.24                  0        0     9            0
v2.60.1  2026-07-28  9       6     ^1.0.0-beta.24                  0        1     0            0
v2.61.0  2026-07-30  96      269   ^1.0.0-beta.24                  0        23    96           1
v2.62.0  2026-08-25  200     515   ^1.0.0-beta.24 → 1.0.0-beta.29  0        23    125          1
v2.62.1  2026-08-28  14      7     1.0.0-beta.29                   0        0     1            0
v2.63.0  2026-09-10  65      155   1.0.0-beta.29                   3        5     20           0

```

File con modifiche nostre non censite, in totale: 154. Raccomandazione dello strumento: fermarsi a `v2.58.1` (prima del bump frappe-ui di `v2.59.0`). **Scelta del committente: arrivare a `v2.63.0`.**

## Release attraversate

### v2.58.1 — fusa (`6581b4cbb`)

**Ampiezza reale:** 23 commit, 19 file. frappe-ui invariato (`^1.0.0-beta.7`). 0 rotture dichiarate, 0 file non censiti, 0 pagine congelate toccate. Voci di inventario toccate (9): `chapter-row-chevron-lucide`, `course-form-os-sections`, `course-outline-editor-hash`, `lesson-ai-status`, `lesson-pdf-inline-filename`, `lesson-tag-badges`, `profile-own-without-lms-role`, `program-member-unpublished-course`, `quiz-submit-unanswered`.

**Fermate:**

- **F5 — sovrapposizioni annotate negli intent** (nessun commit `feat` nella release).
  - `quiz-submit-unanswered` contro upstream `567da4f1` (validazione dell'input e normalizzazione delle risposte nulle a `""`). Differenza reale: con la penalità per risposte sbagliate attiva, l'upstream dà −penalità alla domanda saltata, noi 0; il nostro test `test_blank_answer_is_not_penalised_by_negative_marking` sarebbe diventato rosso. **Decisione del committente:** entrambe valide, la scelta fra 0 e penalità deve essere un'impostazione del quiz; nel merge si tengono entrambe (saltata = 0) e l'impostazione si costruisce **dopo v2.63.0**, sul nuovo authoring dei quiz. → *attività aperta*.
  - `lesson-pdf-inline-filename` contro upstream `7e5a825e`/`48252d92`: **confermato** dal committente — versione upstream per `send_private_file`, resta il nostro `Content-Disposition` inline.
- **F1 — conflitto** in `lms/lms/doctype/course_lesson/course_lesson.py` (voce `lesson-pdf-inline-filename`): import uniti (`inspect`, `cache` dell'upstream + il nostro `quote`; `json` non più usato dopo l'upstream); `serve_resource` chiama `_serve_private_file` upstream e poi applica il nostro `Content-Disposition` protetto da `not in response.headers`; il nostro commento «send_private_file non accetta filename» riportato alla forma upstream perché non più vero. I test upstream chiamano `_serve_private_file` direttamente e non passano dal nostro ramo.
- **Fuso da git senza conflitto, verificato:** `lms/lms/doctype/lms_quiz/lms_quiz.py` contiene già normalizzazione upstream + nostro ramo per le saltate. **Ancora di `quiz-submit-unanswered` spostata** su `if answer not in (None, "")`: la vecchia (`result.get("answer") or []`) compariva due volte dopo il merge, anche nella riga upstream, e non avrebbe più segnalato la perdita della nostra.
- Intent di entrambe le voci aggiornati con lo stato post-merge.

**rerere:** nessuna risoluzione riapplicata; registrata quella di `course_lesson.py`.
**Verifiche:** rilevatore 0 errori (baseline 0) dopo la risoluzione e dopo il commit; build frontend verde (40 s); `frappe-ui-colors.json` invariato.

### v2.59.0 — fusa (`f02f4448f`)

**Ampiezza reale:** 91 commit, 195 file. **frappe-ui `^1.0.0-beta.7 → ^1.0.0-beta.24`**. 0 rotture dichiarate. 20 voci di inventario toccate, 82 file con modifiche nostre non censite, 1 pagina congelata toccata. Merge simulato (`git merge-tree`): 30 conflitti, di cui 25 su file non censiti, 3 su file censiti (`CourseCardOverlay.vue`, `BatchOverview.vue`, `CourseDetail.vue`), più `yarn.lock` e `it.po`.

**Fermate prima del merge:**

- **F6 — bump frappe-ui.** L'upstream rimuove anche codemirror, apexcharts, feather-icons, lo script `copy-colors-json` e il file `frontend/src/utils/frappe-ui-colors.json`, ancora importato da 5 nostri file (tra cui `CourseCard.vue`, `AdminBatchDashboard.vue`, `CourseDashboard.vue`, `utils/index.js`). Il 417 di produzione con beta.24 era solo telemetria (`boot_config` assente in Frappe v16); l'upstream dichiara ancora compatibilità con Frappe `<=17.0.0-dev`. **Decisione del committente:** procedere; sistemare solo ciò che rompe la build; **riconciliazione completa dei 23 override frappe-ui una sola volta dopo v2.62.0, contro beta.29** → *attività aperta*.
- **F6 — pagina congelata `CourseOverview.vue`:** nulla da portare nella copia. La correzione upstream (indice del corso ricaricato al cambio di corso) riguarda la risorsa `outline` che la nostra copia ha commentato insieme alla sezione «Course content»; i ritocchi alle dimensioni dei titoli usano classi che la copia non ha.
- **F7 — 82 file non censiti.** **Decisione del committente: censirli tutti prima del merge.** Lavoro diviso in 6 gruppi funzionali disgiunti (impostazioni e ruoli, classi, corsi/programmi/home, lezioni e media, quiz/compiti/esercizi/discussioni, infrastruttura e controlli); un commit per gruppo.
- **F7 chiusa:** 82 file censiti in 6 commit (`62e760d4b` infrastruttura e controlli, `7cf4133e1` impostazioni/profilo/ruoli, `dd363292c` lezioni/editor/media, `2269af35f` corsi/programmi/home, `9a702ddbe` quiz/compiti/esercizi/discussioni, `b4a244376` classi): solo marcatori-commento (verificato riga per riga) più 190 voci; inventario da 35 a 223 voci. Piano rilanciato: 0 file non censiti per v2.59.0.
- **F5:** nelle note 29 `feat`, nel diff reale solo 2; uno solo tocca file nostri (`f0eefa157`, indirizzo nelle transazioni + chiusura del menu in `Link.vue`): aggiunta, nessuna sostituzione.
- **F1 — 32 conflitti**, esattamente quelli previsti dalla simulazione `git merge-tree`; `rerere` non aveva risoluzioni da riapplicare, ha registrato le 32 nuove. Risolti per intent (5 gruppi in parallelo + `it.po`, `package.json`, `yarn.lock`):
  - `it.po`: un solo punto, tenute entrambe le voci; 2 traduzioni upstream nuove che avrebbero coperto il nostro `it.csv` (maiuscole: «Il Contenuto…», «Invio Predefinito») allineate al CSV. `msgfmt -c` pulito.
  - `package.json`/`yarn.lock`: **frappe-ui fissato esatto a `1.0.0-beta.24`** (la versione che risolve il lock upstream; lezione dell'incidente del caret); lock rigenerato dal lock upstream + nostre dipendenze; una sola copia di `prosemirror-state` (1.4.4).
  - `lms_assignment_submission.py`: il Valutatore resta ammesso alla correzione con un insieme **locale** di ruoli privilegiati; l'insieme condiviso in `utils.py` (certificati, progressi) non è toccato; `enforce_grading_permission` non annulla più in silenzio il suo voto.
  - `permissions.py`: ramo Valutatore riportato nel nuovo `resolve_lesson_access`.
  - `Settings.vue`: ricostruito su `SettingsDialog` upstream conservando chiavi stabili delle schede, schede solo-amministratore, schede os_lms (con icone lucide-vue-next tramite fallback), Google Calendar, benvenuto, interruttori della barra laterale.
  - Dettaglio per file nei rapporti degli agenti; ancore aggiornate: `lesson-long-strings-wrap`, `upload-unattached-new-lesson`, `assignment-valutatore-grading`, `quiz-list-style`, `settings-tabs-stable-keys` (2), `settings-oslms-tabs-injection`, `discussion-no-embed-toolbar` (3), `settings-dialog-style`.
- **Decisioni del committente durante il merge:**
  - Embed nelle discussioni: prop OSLMS-CUSTOM `excludeItems` in `RichTextEditor.vue` (esclude `InsertIframe`).
  - Membri: la riga apre la modifica, avatar/nome aprono il profilo.
  - Compiti: la risposta è in sola lettura per chi corregge (nuova voce `assignment-answer-readonly-for-graders`); l'upstream l'aveva resa modificabile.
  - Quiz: il Valutatore legge i quiz dei corsi e delle classi che valuta (nuova voce `permissions-valutatore-quiz-access`); `can_access_quiz` upstream non lo conosceva.
  - Dialogo impostazioni: ritocchi riapplicati su `SettingsDialog` (padding e scorrimento del pannello, id `settings-modal`).
  - `upload-unattached-new-lesson` → `accepted-drift` (l'upstream fa lo stesso; la nostra non funzionava più).
  - `CourseDashboard`: tenuto lo stato vuoto upstream «Nessuno studente iscritto», ripristinato dopo la perdita in un vecchio merge.
- **F2:** nessuna perdita silenziosa (rilevatore 0 errori dopo il commit).
- **F3 — build:** rotta per l'import di `frappe-ui-colors.json` in `AdminBatchDashboard.vue` → `67f2c1008` (variabili CSS `var(--<hue>-400)` come l'upstream). Adeguamento: colore indigo del nuovo editor negli annunci → `2d6b67dc2`. Build verde (40 s), nessun avviso rilevante.
- **Censiti a parte** (`49e48fca3`): prezzo nascosto sulla card del corso, vista a schede per il Valutatore nel dettaglio corso.
- **Vitest:** 21 falliti su 236, **identici nome per nome a quelli del commit precedente al merge** (baseline in worktree temporaneo con beta.7): fallimenti preesistenti (mock di `@/utils` senza `getEditorI18n`, Pinia non installata nei test, Dialog con vecchia API), non regressioni. → *attività aperta*.

### v2.60.0 — fusa (`b994e4ddc`)

**Ampiezza reale:** 15 commit, 47 file; frappe-ui invariato (resta fissato a `1.0.0-beta.24`); 0 rotture dichiarate; nuove dipendenze upstream `pdfjs-dist` 4.10.38 e `vite-plugin-static-copy`.

- **F7:** 2 file non censiti → `010152dab` (`time-ago-italian` in `utils/format.js`; sito `LessonContent.vue` su `upload-pdf-block-mobile`).
- **Collisione di nomi:** l'upstream aggiunge `frontend/src/tests/PdfBlock.test.ts`, che su filesystem case-insensitive (`core.ignorecase=true`) collide con il nostro `pdfBlock.test.ts` → rinominato prima del merge, contenuto invariato (`7ee6053e1`).
- **F5 — decisioni del committente:**
  - **`PdfBlock.vue`:** l'upstream introduce un visualizzatore pdf.js che legge il PDF dentro la lezione anche su mobile, in concorrenza con il nostro (scheda «apri in un'altra scheda» su mobile, iframe su desktop). **Adottato l'upstream** con le nostre correzioni: URL privati di `serve_resource` non ricodificati, testi tradotti (funzione locale su `window.__`, così il test upstream resta verde senza modifiche), riconoscimento del PDF dall'estensione nel blocco upload. La voce `upload-pdf-block-mobile` è riscritta sulla nuova regola; il nostro test, legato alla regola ritirata, è riscritto da lì (`pdfBlockPrivateUrl.test.ts`, 4 casi) come prevede la regola «un test è un output».
  - **`Questionnaire.vue`:** eliminato dal redesign dell'onboarding «persona»; accettato. Voce `questionnaire-surface-base-token` → `accepted-drift`, **da controllare nella prova** (il nuovo onboarding non deve avere lo stesso problema di colore).
- **F1 — 7 conflitti:** `Questionnaire.vue` (eliminazione accettata), `components.d.ts` (generato: presa la versione upstream, poi rigenerato dalla build → `c63738cc9`), `LessonContent.vue` (upstream, ora identico alla nostra regola), `PdfBlock.vue` (vedi sopra), `upload.js` (nostro riconoscimento del tipo e scheda di download + `destroy()` upstream che libera il worker pdf.js), `SettingFields.vue` (tenuto il nostro `isVideoField`, presa la correzione upstream dei checkbox), `vite.config.js` (entrambi gli import).
- **Verifiche:** rilevatore 0 errori (228 voci); build verde (43 s); test upstream `PdfBlock.test.ts` 5/5; Vitest 277 passati, **21 falliti identici alla baseline**, nessun nuovo fallimento.

### v2.60.1 — fusa (`5cb884477`)

**Ampiezza reale:** 9 commit, 6 file; nessun bump, nessuna rottura, nessun file non censito, nessun `feat`. Merge senza conflitti. Tocca `course_lesson.py` (ricerca dei media delle lezioni con il query builder: corregge file con `_` o `%` nel nome) accanto alla voce `lesson-pdf-inline-filename`, senza toccarne la regola; rimuove la dipendenza razorpay, non usata dal nostro codice. Rilevatore 0 errori, build verde.

### v2.61.0 — fusa (`1b4b2d2f5`)

**Ampiezza reale:** 96 commit, 269 file; frappe-ui invariato; 0 rotture dichiarate; 189 voci di inventario toccate; 32 file non censiti; merge simulato: **59 conflitti**.

**Prima del merge:**

- **F5 — 10 `feat` reali, nessuna sostituzione di funzioni nostre:** la «vista studente» diventa una route vera (sparisce l'anteprima incorporata in `CourseEditor`, il pulsante «Modifica» accanto al titolo della lezione diventa il link «Editor view» nell'intestazione: il nuovo link usa ancora l'hash `#course editor`, lo stesso bug che la voce `lesson-edit-button-editor-hash` corregge → correzione da riapplicare al nuovo link nella risoluzione); layout dichiarativo unico per le pagine elenco (riscrittura in cui riesprimere le nostre regole sugli elenchi); integrazione Raven (non in uso); rinomina delle lezioni «Untitled»; onboarding persona (già accettato in v2.60.0).
- **F6 — `CourseOverview.vue` congelata:** l'upstream accorpa solo gli import dei tipi (`@/types/api` → `@/types`); `@/types/api` esiste ancora e la copia compila. **Decisione del committente: nulla da portare.**
- **F7 — 32 file non censiti. Decisione del committente: censirli tutti**, con lo stesso metodo di v2.59.0 (3 gruppi).
- **F7 chiusa:** 32 file censiti in 3 commit (`a7f1e3811` impostazioni e modali, `e6228f450` controlli/layout/infrastruttura, `7b5cbff2b` pagine di corsi e classi); solo marcatori-commento verificati riga per riga; inventario a **280 voci**; piano: 0 file non censiti per v2.61.0; rilevatore 0 errori.
- **F1 — 59 conflitti**, esattamente quelli previsti; `rerere` non aveva risoluzioni da riapplicare e ha registrato le 58 nuove (più l'eliminazione di `style.css`). Ripresa della sessione dopo il passaggio di consegne. 3 risolti a mano (`components.d.ts` preso upstream e poi rigenerato → `6362b39f4`; `patches.txt` con entrambe le patch, prima l'indice upstream poi il nostro backfill; `lms/public/css/style.css` eliminazione accettata, nessun riferimento residuo), 56 da 7 agenti in parallelo per gruppi disgiunti, seguendo gli intent («ATTENZIONE al merge di v2.61.0»):
  - *Editor e lezioni:* `CourseEditor.vue` = upstream (sparisce l'anteprima incorporata/`lessonMode`) con `'#editor'` sui tre siti, compreso il nuovo watch che ripulisce `?lessonMode`; `Lesson.vue`: il nostro pulsante «Modifica» sparisce come upstream, la regola `#editor` passa sul link «Editor View» dell'intestazione (gate upstream `canEditLesson`); `ChapterRow.vue`: `#editor` era un innesto non censito → marcato e aggiunto alla voce `course-outline-editor-hash`; `StudentLessonSidebar` su `ul/li` upstream con la nostra chiave del capitolo attivo; `InlineLessonMenu` importa i tipi da `@/types` (il campo `text_offset` è passato da sé in `types/notes.ts` con la rinomina).
  - *Corsi:* `Courses.vue` e `Programs.vue` ricostruiti sul nuovo `ListPage` upstream con le nostre regole riespresse (ricerca su `update:modelValue`, schede per ruolo, categorie per scheda, gate Gestore); pagina da 24 corsi (upstream, prima 30); `CourseDetail`: pulsante Pubblica solo sulla scheda Impostazioni (regola upstream) scritto su `activeTabId`; `CourseDetailsSection` tiene il nostro `TagPicker`.
  - *Classi:* `Batches.vue` su `ListPage` (filtro Certificazione upstream lasciato fuori; ora raggiungibile con `?certification=true`); `BatchDetail`: Pubblica su `currentTabKey === 'Settings'`; `Announcements`: `ul/li` upstream con chiave per indice (il nostro override `get_announcements` non restituisce `name`).
  - *Home, programmi, profilo:* `LiveClassCard` tenuto; `ProfileCertificates` tiene la nostra scheda cliccabile (visualizzatore TrueSkills, pulsanti openbadge dentro la scheda) invece del link upstream al PDF; `Profile` tiene NoPermission sugli errori e prende NotFound upstream solo per risposta vuota.
  - *Valutazioni e discussioni:* `ListPage` upstream su Quiz, Compiti ed Esercizi; `DiscussionReplies` su `ul/li` con ordine inverso nella chat di classe ed esclusione dell'Embed.
  - *Controlli e utilità:* `Link.vue` con gli eventi dell'override Combobox obsoleto + `aria-label` upstream; `MultiLink.vue` nostro (reka) con `disabled` e risoluzione dei titoli upstream; `utils/index.js` con `Heading` upstream.
  - *Layout, impostazioni, router, API:* skip link e `<main>` upstream con i nostri `AiFixedButtons`/sfondo; menu mobile con i nostri gate; `router.js` con NotFound upstream in coda; `get_profile_details` = upstream.
- **Decisioni del committente durante il merge:**
  - **PDF** (upstream `4ba152155`: pdf.js solo su WebKit, iframe nativo altrove): **gate allargato** — pdf.js anche su Android/mobile (`usesInlinePdfViewer`), iframe nativo su desktop non-Safari, nessuna doppia codifica degli URL privati (`encodePdfURL`, ora unico punto usato da `PdfBlock`, `upload.js`, `LessonContent.vue`). Nuovo test `pdfInlineViewerMobile.test.ts` (10 casi); al mock di `pdfBlockPrivateUrl.test.ts` aggiunto `PDFWorker` (come nel test upstream), asserzioni invariate.
  - **Raven** (nuovo gruppo «Integrations»): scheda solo per gli amministratori (nuova voce `settings-raven-admin-only`).
  - **Stile degli elenchi su ListPage** (Quiz, Compiti): aspetto upstream, **da verificare nell'app**, poi si decide se riportare `os-list-view`; siti `os-list-view` e `#prefix` mobile tolti dalle voci con nota.
  - **Test upstream `multiLink.test.ts`**: mock adeguato (Popover reale + stub LoadingIndicator), 11/11 verdi con asserzioni upstream → `31677fd69`.
  - **accepted-drift** (l'upstream ora fa lo stesso): `legacy-style-css-formatting`, `editor-header-inline-toolbar`, `profile-own-without-lms-role` (con ordine anti-enumerazione upstream), `courses-list-certification-first-click` (ToggleFilter upstream).
  - **Rinomina notturna delle lezioni «Untitled lesson»** (upstream `bbab35054`): i vettori dell'AI tutor sono etichettati col nome della lezione (9 lezioni indicizzate in produzione verrebbero rinominate) → hook `after_rename` in os_lms che ripulisce i vettori col vecchio nome e rimette la lezione in coda → `060a3642b`, voce `lesson-rename-rag-reindex`, test backend 4/4 nel container.
  - **Vista studente** upstream che non spegneva i nostri ruoli (un Valutatore in anteprima vedeva il pannello di correzione) → spenti anche `is_docente`/`is_valutatore` e i ruoli Valutatore/Docente/Gestore → `8788bdcbb`, voce `student-view-oslms-roles`.
  - **Endpoint disponibilità del valutatore** (override os_lms che scrive in lettura e manca del controllo di accesso upstream): **rimandato**, attività aperta di sicurezza.
  - **Test upstream nuovi rossi** (16): aggiunti alla baseline dei fallimenti noti, riconciliazione unica a fine percorso.
- **Ancore aggiornate:** `lesson-long-strings-wrap`, `courses-programs-theme-card-hooks` (2), `courses-programs-list-search` (2), `discussion-no-embed-toolbar`, `discussion-batch-chat-newest-first`, `editor-header-inline-toolbar`, `profile-own-without-lms-role`, `courses-list-certification-first-click`, `notes-type-text-offset` (file spostato), `upload-pdf-block-mobile` (riscritta); siti aggiunti a `course-editor-tab-hash`, `course-outline-editor-hash`, `trueskills-certificate-viewer`, `announcement-list-pagination`, `multilink-reka-combobox`.
- **F2:** nessuna perdita silenziosa (rilevatore 0 errori prima e dopo il commit del merge).
- **F3:** lock cambiato solo di 4 righe (installazione con `--frozen-lockfile` pulita); build verde (47 s, poi 42 s dopo gli adeguamenti).
- **Vitest:** 570 passati, 37 falliti = 21 preesistenti identici + 16 in test **nuovi upstream** (`lessonFormTeardown` titolo su una riga ×5 per Pinia, come la baseline; `coursesFilters` ×5 per il mock che non conosce le nostre personalizzazioni di Corsi; `ProfileEvaluator` ×6 perché la pagina chiama il nostro endpoint). Nessun test che prima passava si è rotto. Nuova baseline: `.git/oslms-upstream-walk-tools/fail-261.txt`.
- **Rilevatore finale:** 0 errori, inventario a 283 voci.

### v2.62.0 — fusa (`335f9aa8d`)

**Ampiezza reale:** 200 commit, 515 file; frappe-ui `^1.0.0-beta.24` → `1.0.0-beta.29`; 0 rotture dichiarate; 24 file non censiti; **96 conflitti**. È la release del redesign mobile upstream e dei moduli instradati (`pages/Forms/*`, che sostituiscono 12 modali).

**Prima del merge (sessione del 2026-09-23):**

- **F7 — 24 file non censiti. Decisione: censirli tutti** → 4 commit (`6715d8a5a` editor/quiz/import/statistiche, `83f69df67` doctype backend, `68832c6b8` modali di classi/corsi/live, `7be817a2d` shell/tema/menu utente/registrazione).
- **F6 — `CourseOverview.vue` congelata:** skeleton dell'outline e `v-safe-html` upstream; **nulla da portare** (outline commentata nella copia, descrizione via `CourseDescription`).
- **F5 — decisioni del committente:** blocco sequenziale upstream (`enforce_lesson_completion`, lato server) **al posto** del nostro `enforce_lesson_order`, tenendo il blocco del quiz a completamento; **redesign mobile adottato**, riesprimendo solo le regole ancora valide; filtri upstream sugli elenchi: il Valutatore vede le live della sua classe in sola lettura e le classi non pubblicate che valuta, il Docente vede, crea e modifica le live; `LiveClassForm` upstream diventa l'unico modulo (anche modifica e riprogrammazione); la nostra voce di menu «Importa» diventa «Carica file».
- **F1 — 96 conflitti** risolti da agenti per gruppi; 12 modali eliminate dall'upstream (`git rm`), regole riportate nei moduli instradati; `LayoutHeader.vue` tenuto (usato dalle nostre pagine delle simulazioni).

**Ripresa (2026-09-24), prima del commit del merge:**

- **Inventario** aggiornato con un nuovo strumento verificato (`site_ops.py`: giro a vuoto identico byte per byte, verifica semantica di ogni operazione): rilevatore da **166 errori a 0**. 168 spostamenti meccanici (file spostati in `pages/Forms/`, `components/Courses/`, `routes.js`; ancore riscritte), 29 siti tolti perché coperti in parte dall'upstream (con nota nell'intent), 17 voci **accepted-drift** approvate: `empty-state-mobile-full-width`, `course-form-mobile-layout`, `quiz-list-mobile-columns`, `batch-detail-mobile-header`, `mobile-nav-sidebar-settings-toggles`, `batch-detail-tab-keys`, `batch-detail-hash-sync`, `course-outline-editor-hash`, `course-new-modal-title-translated`, `profile-edit-image-optional`, `quiz-cached-remount`, `vitest-inline-frappe-ui`, `exercise-submission-commented-icons`, `settings-email-template-modal-formatting`, `quiz-submission-list-style`, `assignment-submission-list-header`, `mobile-nav-programs-hidden`.
- **Decisioni del committente:** Programmi tornano sulla barra del telefono (upstream); riga «Cerca…» della palette in fondo all'elenco (ora in un gruppo proprio dopo tutti i risultati, disattivata mentre i risultati si aggiornano); pulsante indietro del Valutatore su telefono = cronologia; ordinamento studenti con intestazioni cliccabili su desktop.
- **`rerere`** ha registrato le risoluzioni al commit (nessuna riapplicata).

**Adeguamenti dopo il merge (commit separati):**

| Commit | Cosa |
| --- | --- |
| `f35a6babc` | dichiarazioni dei componenti rigenerate |
| `d0ab8953d` | blocco sequenziale: patch dati `enforce_lesson_order` → `enforce_lesson_completion` (os_lms v0_0_6), chiavi per l'app mobile calcolate sul gate upstream, interruttore duplicato tolto, traduzioni |
| `993f9448c` | filtri upstream sugli elenchi di Live Class, Batch, Course Lesson allargati a Docente e Valutatore; il Docente crea/modifica/elimina le live end-to-end |
| `03b4e1122` | **perdita silenziosa (F2):** `MemberForm` upstream senza ruolo Valutatore né creazione con i soli ruoli scelti → riportati, override `get_member`, regola censita (non lo era mai stata) |
| `628019520` | decodifica HTML inerte in `CourseDescription` e `searchRoutes` (test `innerHtmlSinks`) |
| `88bdf9afe` | risultati di ricerca: compiti → `AssignmentForm`, programmi → `ProgramForm`/`ProgramDetail` |
| `68f64c2f3` | CTA mobile del corso `sticky` sopra la nuova barra (62 px misurati + area sicura); pulsanti AI ricalcolati |
| `2656865d9` | import ZIP dei corsi chiuso al Gestore anche sul modulo instradato |
| `0b7986a0b` | z-index dei popover: selettore reka (quello radix era inerte con frappe-ui 1.0) |
| `4aeb85722` | tolta la riga «Modalità colore» dalla pagina «Tu» del telefono (tema fissato dall'amministratore) |
| `de0eda15e` | «indietro» con la cronologia per il Valutatore (prop `historyBack` su `PageHeader`/`ListPage`) |
| `887a6c1f3` | intestazioni ordinabili in `ResponsiveListView`, selettore solo su telefono |
| `44c0c6fd4` | codice nostro allineato ai nuovi cancelli di sicurezza (`v-safe-html`, `safeUrl`, `v-external`, `openExternal`; helper `safeImageData`, `openWithOpener` per l'OAuth di Google Calendar, `openPendingTab` per i certificati) |
| `73a5836fc` | pulsante «Archivia» delle simulazioni tenue sui token del tema (decisione del committente) |

- **Filtri sugli elenchi, verifica in produzione (sola lettura):** tutti i Docenti hanno anche Moderator; 4 utenti sono solo Valutatore (gli unici davvero colpiti dai filtri nuovi); il Docente ha già pieni permessi di ruolo su LMS Live Class da desk.
- **F3:** installazione pulita, **build verde** (45 s), classi `calc(...)` generate.
- **Vitest:** 1467 test, 134 rossi, tutti classificati in `.git/oslms-upstream-walk-tools/fail-262.txt`: baseline v2.61.0 + test nuovi upstream il cui mock non conosce i nostri componenti + test che verificano comportamenti upstream cambiati da nostre regole (palette, ruoli dei membri, «Modalità colore», manifest degli upload, scheda predefinita del profilo, avvisi obbligatori, regole delle live, compositore annunci, salvataggio completo del programma). **Nessun bug di codice trovato**; i cancelli di sicurezza nuovi sono tutti verdi tranne 9 righe dell'override `FontColor`.
- **Rimandato (decisione):** riconciliazione dei 23 override frappe-ui **dopo v2.63.0** (frappe-ui resta beta.29), comprese le 9 righe di `FontColor`.
- **Rilevatore finale:** 0 errori.

### v2.62.1 — fusa (`6f80186b7`)

**Ampiezza reale:** 14 commit, 7 file; nessuna rottura, nessun file non censito, nessuna pagina congelata; voci toccate: `profile-edit-codice-fiscale`, `profile-edit-reduced-fields`, `signup-form-terms-no-category`.

- **Fermate di giudizio:** nessuna (nessun `feat`).
- **F1 — 1 conflitto**, `pages/Forms/ProfileEditForm.vue`: l'upstream generalizza il calcolo di «modulo modificato» (tutti i campi confrontati, vuoti normalizzati, `serverField` per l'immagine) e toglie il parametro `newVal` che la nostra metà usava ancora. Regola `profile-edit-codice-fiscale` riespressa sulla versione upstream: il codice fiscale resta escluso dal confronto generico e si confronta con il valore caricato a parte. `signup-form.html` (macro `alert_banner` resa autonoma) fuso senza conflitti, voce intatta.
- **Rilevatore:** 0 errori. **Build:** verde (dipendenze invariate). **Vitest:** 4 test nuovi di `profileEditRoute.test.ts` agiscono su campi che nascondiamo (Open to, titolo, biografia) → baseline di riconciliazione.

### v2.63.0 — analisi prima del merge (2026-09-24)

**Ampiezza reale:** 65 commit, 155 file; frappe-ui invariato (beta.29); **3 rotture dichiarate**; 69 voci toccate; 2 file non censiti; merge simulato: **17 conflitti**.

**F4 — rotture dichiarate** (analisi di 3 agenti in sola lettura, verificate a campione):

- **`feat(quiz)!` banca domande condivisa (#2710).** Nessun doctype nuovo; campo `LMS Question.marks` con patch di backfill (non tocca consegne storiche); nuovi endpoint `get_question_meta`/`get_question_bank` (Moderator o Course Creator); `QuizForm` riscritto (salvataggio automatico, banca domande, anteprima), nuove pagine `Questions` e `QuizSubmissions` (tutte le consegne), **eliminati** `QuizQuestionForm.vue` e `QuizSubmissionList.vue`. Impatto: il Valutatore perderebbe la lettura delle consegne (la nuova lista lo rimanda ai Corsi); l'anteprima dell'autore creerebbe consegne vere se il nostro pulsante «Invia» non rispetta `preview`; ~81 stringhe nuove senza traduzione; `quiz-question-type-translated` e `quiz-question-radio-style-removed` diventano obsolete; `quiz-submit-unanswered` sopravvive intatta. In produzione tutti i Docenti hanno Course Creator, quindi la banca domande funziona per loro. Costo stimato 12-16 h.
- **`fix(lesson)!` il contenuto non viene più distrutto al salvataggio (#2709).** `ignore_xss_filter` su `content`/`instructor_content` (serve `migrate`), guardia client `parseStoredEditorJs` con avviso «lezione non visualizzabile», `BaseInline` riscritto, allineamento emesso come `<span class="lms-align">`, e (cc66dc6e0) **eliminato lo strumento colore** `utils/inline/Color.ts`, su cui avevamo una correzione. In produzione: 0 lezioni con contenuto illeggibile, 0 con `<lms-align>`, 1 con `<lms-inline-color>` (perderebbe il colore alla prima modifica), 2 con `<span style="color">` del vecchio strumento (sopravvivono meglio di oggi). Costo: conflitti 0,5-1 h + opzione colore scelta.
- **`feat(raven)!` sincronizzazione dei canali (#2706).** Nessun effetto backend senza Raven installato, ma aggiunge `"@framework/ui": "link:../../frappe/ui"` e un import `@framework/ui/ConditionBuilder` verificato da `assertFrameworkUiLinked` in `vite.config.js`: il pacchetto esiste solo in Frappe *develop*, mentre il nostro stack usa `version-16-hotfix` → **la build dell'intera SPA fallirebbe** in dev e in produzione. Conflitti su `MultiLink.vue` (nostra riscrittura reka), `vite.config.js`, `package.json`. Costo 4-6 h.

**F5 — novità:** proctoring dei quiz (webcam, riconoscimento del volto, foto a ogni violazione, conteggio e consegna automatica), nuovo; conteggio in `ListPage`, `align` su `Link`, `forceCards` su `ResponsiveListView`, lingue ceco e slovacco: nessuna sostituzione di funzioni nostre.

**F7 — non censiti:** `utils/inline/Color.ts` (nostra correzione su un file che l'upstream elimina) e `lms_quiz_submission.json`, dove da noi lo Studente ha **scrittura** e il Course Creator permessi pieni: deriva di un vecchio export che annulla la correzione di sicurezza upstream `876504f39`. In produzione contano però i Custom DocPerm (Studente lettura+scrittura+creazione sulle proprie consegne): segnalato come attività aperta di sicurezza, nessuna modifica fatta.

**Decisioni del committente (2026-09-24):** Raven → stub locale di `@framework/ui` (scheda solo admin); strumento colore → **riscritto** sul nuovo `BaseInline` con patch per la lezione che usa `<lms-inline-color>`; proctoring adottato (spento di default), registro con foto **non visibile al Valutatore**; `lms_quiz_submission.json` → versione upstream; Valutatore ammesso in sola lettura nella nuova pagina delle consegne; impostazione per quiz «saltata = 0 / −penalità» subito dopo il merge (default 0); pulsanti del nuovo modulo quiz con aspetto upstream; avviso «varranno 0 punti».

**Passaggio di consegne (2026-09-24):** sessione fermata su richiesta del committente **prima del merge di v2.63.0**; nessun merge in corso. Per riprendere: `/upstream-upgrade` sul branch `merge/upstream-v2.63.0` (fermata `HANDOFF` con i passi successivi); note di risoluzione dei conflitti in `.git/oslms-upstream-walk-tools/f4_263_notes.md`.

### v2.63.0 — fusa (`b81e94159`, ripresa del 2026-09-24)

**Ripresa:** stato coerente (7 merge antenati di `HEAD`, nessun merge in corso), rilevatore a 0 errori. **F7 chiuso** prima del merge registrando le decisioni negli `intent` di `editor-inline-color-persist` ed `editor-color-picker-tool` (`c5f986bd3`); `lms_quiz_submission.json` imposto alla versione v2.63.0 dentro il merge (git l'avrebbe fuso **senza conflitto conservando la nostra deriva sui permessi**).

**F1 — 17 conflitti, come previsto;** `rerere` non ha riapplicato risoluzioni (solo preimmagini registrate).

| File | Risoluzione | Voci |
| --- | --- | --- |
| `lms/patches.txt`, `types/api.ts` | entrambi i lati (patch nostra `backfill_batch_course_enrollments` + upstream; `is_docente` + `is_evaluator`) | — |
| `utils/inline/Color.ts` | cancellazione presa (riscrittura separata) | `editor-inline-color-persist` |
| `pages/Forms/QuizQuestionForm.vue` | cancellazione presa; il nuovo editor traduce i tipi e non ha stili globali sui radio | `quiz-question-type-translated`, `quiz-question-radio-style-removed` → **accepted-drift** (ok del committente) |
| `pages/QuizSubmissionList.vue` | cancellazione presa; regole riportate sulla nuova `QuizSubmissions.vue`: Valutatore ammesso in sola lettura (niente selezione/Elimina, niente filtro per quiz), «indietro» dalla cronologia; `is_valutatore` aggiunto a `SessionUser` | `quiz-submission-valutatore-readonly`, `valutatore-phone-back-history` |
| `pages/QuizSubmission.vue` | layout upstream; Salva solo con `canGrade` (`v-else-if`), voti delle domande aperte `:disabled="!canGrade"` | `quiz-submission-valutatore-readonly` (ancora aggiornata) |
| `pages/Quizzes.vue` | versione upstream (ricerca in `ListPage`, rotta `NewQuiz`, `useBulkDeleteAction`) + spunta verde/celle `text-sm` + `searchLikeFilter` | `quiz-list-style`, `assessment-list-search-words` |
| `pages/Forms/QuizForm.vue` | versione upstream per intero | `quiz-form-mobile-layout` → **accepted-drift** (decisione pulsanti) |
| `components/Quiz.vue` | base upstream (redesign proctoring) + riapplicati: Check/Next/Submit affiancati (Submit mai in anteprima), `isBlankAnswer`, `recordCurrentAttempt` su Check e Next, conferma sempre, timer a orologio con `submitQuiz('timer_expired')`, consegna una sola volta, «Submit Quiz», «Try Again» solid, `card` sulla domanda, intestazioni tradotte; testo della conferma «varranno 0 punti» | `quiz-skip-unanswered-ui`, `quiz-timer-wall-clock`, `quiz-player-style-translations` (3 siti superati tolti, ok del committente) |
| `Controls/MultiLink.vue` | nostra riscrittura reka + porting a mano di `emptyText`, `allowSelectAll`, ricerca azzerata alla chiusura, nuova ricerca al cambio di doctype (`searchKey`) | `multilink-reka-combobox` |
| `Controls/Link.vue` | `open-on-click` tenuto; il nuovo `align` mappato sulla prop `placement` dell'override | `link-control-stale-combobox-events` |
| `ResponsiveListView.vue` | prop di entrambi (ordinamento + `forceCards`/`noDetailSeparator`); ordinamento resta sul ramo desktop | `course-dashboard-sortable-students` |
| `Layouts/ListPage.vue` | import upstream + tipo `RouteLocationRaw` nostro | `valutatore-phone-back-history` |
| `pages/Lesson.vue` | `getEditorI18n()` (include la direzione) + helper upstream `openLinksInNewTab` | `editor-i18n-labels` |
| `pages/LessonForm.vue` | upstream (`parseStoredEditorJs`, `markUnreadable`) | — |
| `utils/index.js` | tolto l'import di `Color`, tenuto `VIMEO_SHARE_RE` | `embed-vimeo-share-links` |
| `vite.config.js` (+ `package.json`, `tsconfig.json`, `vitest.config.ts`, `yarn.lock`) | **stub locale** di `@framework/ui/ConditionBuilder` via alias; tolti dipendenza, `assertFrameworkUiLinked`, `preserveSymlinks`; `interactjs` inizialmente non riaccettato in `optimizeDeps` per una nota sbagliata della sessione precedente, **ripristinato** dopo (`optimizeDeps.include`: senza, il dev server `yarn dev` si fermava al caricamento con «interact.min.js does not provide an export named 'default'», importato da `grid-layout-plus` via frappe-ui; la build di produzione non era colpita); lock rigenerato | nuova voce `raven-framework-ui-stub` |

**Adeguamenti dopo il merge:**

| Commit | Cosa |
| --- | --- |
| `fc4ff3791` | dichiarazioni dei componenti rigenerate (6 componenti Raven eliminati upstream) |
| `1b486601c` | registro del proctoring con foto negato al Valutatore di classe: override os_lms di `get_quiz_violation_logs` (l'endpoint upstream lo dava a chiunque leggesse la consegna) + pagina che non lo chiede né lo mostra; test; nuova voce `proctoring-log-hidden-from-valutatore` |
| `b252e62b3` | `<lms-align>` tolto dall'allowlist del sanitizzatore (upstream emette `span.lms-align`; 0 lezioni in produzione) |
| `4ff63b218` | strumento colore **riscritto** in `src/oslms/utils/inline/Color.ts` su `ToolButton` (emette `span.lms-inline-color`), con le tre protezioni della vecchia correzione; patch os_lms `v0_0_7.convert_lms_inline_color` per i vecchi elementi salvati (lezione 0790); test Vitest e unitario |
| `b1cb0e654` | 161 traduzioni IT delle stringhe nuove (quiz, banca domande, proctoring, avvisi della lezione); escluse le 70 della scheda Raven |
| `4884db2e8` | `contentHasQuiz` legge il contenuto con `parseStoredEditorJs` |

- **Rilevatore:** 0 errori. **Build:** verde (50 s). **Type-check:** non è un cancello (737 errori preesistenti nel progetto).
- **Vitest:** 1562 test, 156 rossi = baseline v2.62 (140) + 18 nuovi, tutti classificati in `.git/oslms-upstream-walk-tools/fail-263.txt`, **nessun bug di codice**: 3 Raven (stub), 11 `multiLink` (test del `MultiSelect` upstream), 2 `linkOnCreate` (prop `align` del `Combobox`: override da riconciliare), 1 `lessonFormTeardown` (mock senza Pinia), 1 `quizCachedRemount` (nostro test su una frase tolta dal redesign).
- **Punto aperto nuovo:** 72 stringhe già senza traduzione prima di v2.63.0 nei file toccati, e le 70 della scheda Raven.

**Dopo la chiusura della release:** impostazione per quiz «domanda saltata = 0 / −penalità» (`1f6913194`): campo os_lms `LMS Quiz.penalize_unanswered`, spento di default (ogni quiz esistente resta «saltata = 0»); con la penalità attiva la saltata perde `marks_to_cut`; interruttore nel nuovo `QuizForm`, conferma di consegna che dice quanto toglie ogni saltata; 2 test backend nuovi.

## Chiusura del percorso (2026-09-24)

### Suite completa (Passo 7)

| Controllo | Esito |
| --- | --- |
| `scripts/check_customizations.py` | **0 errori**, 289 avvisi `C4` (voci senza test collegati, attesi); inventario da 33 a 329 voci |
| `python3 -m unittest discover -s scripts/tests -t .` | **68/68 OK** |
| Vitest (`frontend`) | 1562 test, 156 rossi, **tutti in baseline** (`fail-263.txt`): nessun bug di codice |
| `migrate` del sito di sviluppo (autorizzato) | riuscito; patch upstream `set_question_marks_from_quizzes`, `backfill_batch_course_enrollments` e nostra `v0_0_7.convert_lms_inline_color` applicate; campo `penalize_unanswered` e doctype `LMS Quiz Violation Log` presenti |
| Test os_lms (esclusi i 2 `integration` che chiamano LLM veri) | 269 test, **266 verdi**; 3 rossi nelle simulazioni AI (`test_api` eval, `test_orchestrator` quota, `test_runner`), codice **non toccato** dal percorso (diff vuoto da `start_commit`), quindi indipendenti dal merge; non riverificati su `start_commit` |
| Test lms mirati (quiz, sanitizzazione EditorJS, gate sequenziale, accesso alle consegne) | 103 test, **tutti verdi**, eseguiti con commit disattivati e rollback: **DB di sviluppo invariato** (i `tearDownClass` upstream e nostri cancellano **tutte** le `LMS Question`; il sito ne aveva già 0 prima di questa esecuzione) |
| `/upstream-check` da `start_commit` | rapporto `docs/upstream-checks/2026-09-24-v2.63.0.md`: 0 voci perse, 2 a rischio **corrette**, più un guasto fuori inventario **corretto** (sotto) |

**Trovati e corretti in chiusura:**

- `a4363c4a8` — **`override_api.py` di os_lms non si importava più da v2.62.0** (importava `get_instructor_info`, tolta dall'upstream): profilo utente, impostazioni, barra laterale, membri, ruoli, annunci, notifiche e ricerca sarebbero andati in errore. Corretti anche i parametri che l'upstream ha aggiunto e i nostri override scartavano in silenzio: `category` della palette, `limit_page_length` di corsi e classi.
- `b8b1e82a1` — il Valutatore di classe non apriva più i quiz dentro le lezioni: la riscrittura upstream di `can_access_quiz` aveva messo il controllo di iscrizione prima del nostro ramo. Ramo spostato; test nuovo. Aggiunta la guardia permanente `test_override_signatures.py` e la voce `oslms-api-overrides-upstream-compat`.
- **Lezione per il processo:** nessuno dei controlli del percorso (build frontend, rilevatore a ancore, Vitest) esercita il backend os_lms: il guasto di v2.62.0 è passato inosservato per due release. Il controllo delle firme e dell'importabilità degli hook va eseguito dopo ogni merge (ora c'è un test).

### Cosa è stato adottato, rifiutato, riespresso

- **Adottato dall'upstream:** visualizzatore PDF pdf.js (con le nostre correzioni e il gate allargato a mobile), redesign mobile, moduli instradati al posto delle modali, blocco sequenziale lato server al posto del nostro `enforce_lesson_order`, `LiveClassForm` unico modulo, nuovo authoring dei quiz con banca domande, pagine consegne e modulo del quiz, proctoring (spento di default), salvataggio delle lezioni protetto, allineamento come `span.lms-align`, onboarding persona. In totale 26 voci passate ad `accepted-drift`, ciascuna con data e motivo.
- **Rifiutato consapevolmente:** `@framework/ui` (stub locale, Frappe v16); `title_only` nella ricerca della palette (troviamo anche per contenuto); rimozione dello strumento colore (riscritto); scrittura dello Studente sulle proprie consegne nel JSON (preso l'upstream, ma i Custom DocPerm restano: vedi sotto); registro del proctoring con foto al Valutatore; `MultiSelect` upstream in `MultiLink` (tenuta la nostra riscrittura reka con le novità portate a mano).
- **Riespresso:** regole del Valutatore (quiz, consegne, live, classi, indietro dalla cronologia, vista studente), Docente sulle live, codice fiscale nel profilo, CTA mobile del corso, pulsanti AI, palette, traduzioni, salto delle domande e timer del quiz, blocco dei quiz a completamento, reindicizzazione RAG alla rinomina delle lezioni.

### Cosa resta aperto

1. **Riconciliazione dei 22 override frappe-ui** contro beta.29 (~3000 righe di differenza; `Combobox` 1082 righe, con la prop `align` dei `Link`; `FontColor` con 9 sfondi grezzi; fork `DataImport`; `Menu.vue` il cui originale non esiste più, quindi override morto). Lavoro a sé su un branch dedicato (decisione del committente).
2. **SICUREZZA:** i Custom DocPerm di `LMS Quiz Submission` (in produzione e nel DB di sviluppo) danno allo Studente lettura, scrittura e creazione sulle proprie consegne; il JSON è ora quello upstream (sola lettura), ma i Custom DocPerm lo scavalcano.
3. **SICUREZZA:** override os_lms `get_evaluator_details` (scrive in lettura, manca del controllo di accesso upstream); bug `is_authorised`/`is_authorized` in `ProfileEvaluator.vue`.
4. **Test:** riconciliazione unica dei 156 rossi Vitest (`fail-263.txt`, voci in `test_reconcile` dello stato); 3 test delle simulazioni AI da capire.
5. **Traduzioni IT:** 72 stringhe già scoperte prima di v2.63.0 nei file toccati, le 70 della scheda Raven, e le liste di v2.61.0 e v2.62.0 da verificare.
6. **Voci `confidence = "low"` toccate dal percorso:** 33, elencate nel rapporto di verifica.
7. Da decidere dopo la prova nell'app: stile degli elenchi Quiz e Compiti (`os-list-view`), div `gradient-overlay` inerte nel layout desktop, eliminazione del vecchio tool `editorjs-color-picker` e della sua dipendenza.

### Cosa provare nell'app (`http://lms.localhost:8000/lms`)

Il sito di sviluppo è già migrato alla nuova versione. Tra parentesi: perché la prova è in lista.

**Avvio e ruoli**

1. Accedi come **Studente**, come **Docente** e come **Moderatore**: la SPA si carica, la barra laterale mostra le voci giuste per il ruolo, le impostazioni si aprono. *(Override API di os_lms ripristinati, `a4363c4a8`: prima di questa correzione il profilo utente andava in errore.)*
2. Come **Moderatore**, in Impostazioni → Membri: l'elenco si carica, crea un membro con il solo ruolo Valutatore e poi cambiane il ruolo. Deve avere solo i ruoli scelti. *(Override `get_members`/`get_member`/`save_role`, voce `member-valutatore-role-exact-roles`.)*
3. Come **Amministratore**, in Impostazioni → Integrazioni → Raven: la scheda mostra l'avviso «L'editor delle condizioni richiede una versione più recente di Frappe» e nessun errore. Come **Moderatore** la scheda non c'è. *(Stub `@framework/ui`, voci `raven-framework-ui-stub`, `settings-raven-admin-only`.)*

**Quiz (v2.63.0)**

4. Come **Studente**, in un quiz senza «mostra risposte»: salta una domanda, vai avanti con Avanti e premi «Invia Quiz» da una domanda intermedia. La conferma deve dire «Hai 1 domanda senza risposta. Se consegni ora, varranno 0 punti», la consegna deve riuscire e la domanda saltata deve valere 0. Ripeti in un quiz **con** «mostra risposte»: Verifica, Avanti e «Invia Quiz» devono stare affiancati. *(Voci `quiz-skip-unanswered-ui`, `quiz-submit-unanswered`: `Quiz.vue` ricostruito sul redesign upstream.)*
5. Come **Docente**, in un quiz: attiva «Abilita penalità», metti 1 punto da togliere e attiva «Penalizza le domande senza risposta». Poi, come **Studente**, salta una domanda: la conferma deve dire «ognuna toglierà 1 punto» e la saltata deve valere −1. Con l'interruttore spento deve valere 0. *(Feature `1f6913194`.)*
6. Come **Studente**, apri un quiz a tempo, passa a un'altra scheda del browser e torna dopo la scadenza: il quiz deve risultare consegnato una sola volta. *(Voce `quiz-timer-wall-clock`: timer riapplicato sul player nuovo.)*
7. Come **Docente**, nel nuovo modulo del quiz usa «Anteprima» e rispondi: non deve esserci il pulsante di consegna, e non deve comparire nessuna consegna nella pagina Invii. *(Guardia `preview` sul nostro pulsante di consegna.)*
8. Come **Docente**, crea domande dalla banca domande (i tipi devono essere in italiano: «Scelta singola», «Risposta breve», «A risposta aperta») e verifica le traduzioni del modulo del quiz e della pagina Invii. *(`b1cb0e654`; voci del modale domanda in accepted-drift.)*
9. Come **Moderatore**, in un quiz con la sorveglianza attiva (Chrome, webcam): avvia il quiz, cambia scheda, consegna. Nel dettaglio della consegna devono comparire il numero di violazioni e il «Registro della sorveglianza» con le istantanee. *(Nuova funzione upstream adottata.)*

**Valutatore di classe**

10. Come **Valutatore** di una classe: apri una lezione con un quiz di un corso della classe. Il quiz si deve caricare. *(Regressione trovata in chiusura, `b8b1e82a1`.)*
11. Come **Valutatore**: Dashboard classe → studente → riga di un quiz. Il dettaglio mostra le risposte, **senza** Salva, con i voti delle domande aperte non modificabili e **senza** il registro della sorveglianza, anche se il tentativo ha violazioni. Dalla briciola «Invii» si apre l'elenco: nessuna casella di selezione, nessun Elimina, nessun filtro per quiz. Su telefono, «indietro» torna alla pagina precedente. *(Voci `quiz-submission-valutatore-readonly`, `valutatore-phone-back-history`, `proctoring-log-hidden-from-valutatore`.)*

**Lezioni**

12. Come **Docente**, nell'editor di una lezione: seleziona del testo, usa lo strumento colore (secchiello) per colore del testo ed evidenziazione, anche aprendo il selettore di sistema; poi allinea al centro un paragrafo, salva e ricarica. Colore ed allineamento devono restare. Prova anche a togliere il colore. *(Strumento riscritto `4ff63b218`; voce `editor-inline-color-persist`.)*
13. Come **Studente**, apri una lezione con un quiz incorporato in un corso a blocco sequenziale, su una lezione ancora bloccata: deve comparire l'avviso «quiz bloccato». *(`contentHasQuiz` su `parseStoredEditorJs`.)*

**Ricerca ed elenchi**

14. Con **qualsiasi ruolo**: Cmd+K → Vai a → Corsi, poi digita. Devono comparire solo corsi, e la riga «Cerca…» deve restare in fondo. *(Categoria ripristinata, voce `command-palette-result-routes`.)*
15. Come **Moderatore**, in Corsi e in Classi: cambia «righe per pagina» nel piè dell'elenco e scorri. Nessuna riga deve ripetersi o mancare. *(`limit_page_length` ripristinato.)*
16. Come **Moderatore**, in un campo a scelta multipla (es. valutatori della classe): cerca, chiudi, riapri. Deve ripartire dall'elenco completo, e «Cancella» deve funzionare. *(`MultiLink` con le novità portate a mano.)*
17. Come **Moderatore**, nel modulo di una classe o di un corso: i campi collegati (es. corso, categoria) devono mostrare il valore salvato, non vuoto. *(Override `Combobox` vecchio, da riconciliare.)*

**Da release precedenti, ancora da vedere**

18. Come **Gestore**: la voce «Carica file» del menu utente e il nuovo import dati sotto Configurazione. *(Decisione v2.62.0.)*
19. Come **Studente nuovo**: il nuovo onboarding persona, senza problemi di colore. *(`Questionnaire.vue` eliminato in v2.60.0.)*
20. Come **Docente**: gli elenchi Quiz e Compiti con l'aspetto upstream, per decidere se riportare `os-list-view`. *(Decisione v2.61.0.)*
21. Come **Studente**, un PDF in una lezione su desktop Chrome (iframe), su Safari/iPhone e su Android (pdf.js), compreso un PDF privato. *(Voce `upload-pdf-block-mobile`.)*
22. Come **Docente**: crea, modifica e riprogramma una live dal modulo della classe. Poi, come **Valutatore** della classe, verifica che le live si vedano in sola lettura. *(Decisioni v2.62.0.)*
23. Come **Studente**, su telefono, in un corso: la barra «Continua» non deve coprire la barra di navigazione, e i pulsanti AI devono stare sopra entrambe. *(Voci `course-mobile-cta-bar`, `ai-fixed-buttons-layouts`.)*


---

## Passaggio di consegne (2026-09-23)

La sessione si è fermata per contesto esaurito, su richiesta del committente, **prima del merge di v2.61.0**. Tutto è committato sul branch `merge/upstream-v2.63.0`; nessun merge in corso.

| | |
| --- | --- |
| Release fuse | v2.58.1 (`6581b4cbb`), v2.59.0 (`f02f4448f`), v2.60.0 (`b994e4ddc`), v2.60.1 (`5cb884477`) |
| Release corrente | v2.61.0 — fermate F5/F6/F7 chiuse, **manca il merge** (59 conflitti previsti) |
| Da fare dopo | v2.62.0 (bump frappe-ui → `1.0.0-beta.29` + riconciliazione dei 23 override), v2.62.1, v2.63.0 (3 rotture dichiarate), chiusura (Passo 7 della skill) |
| Stato macchina | `.git/oslms-upstream-walk.json` (campo `stop.next_steps` con le risoluzioni già note) |
| Strumenti | `.git/oslms-upstream-walk-tools/` (vedi il suo `README.md`) |

**Per riprendere:** lanciare `/upstream-upgrade` sul branch `merge/upstream-v2.63.0`: la skill trova il file di stato e propone di riprendere dalla fermata `HANDOFF` di v2.61.0.

**Attività aperte registrate:** impostazione per-quiz «domanda saltata = 0 / −penalità» (dopo v2.63.0); riconciliazione degli override frappe-ui (dopo v2.62.0); verifica del nuovo onboarding persona nell'app; 21 test Vitest che falliscono già prima del percorso.

## Domande aperte e probabili bug raccolti (non bloccanti, da decidere con il committente)

Emersi dal censimento e dalle risoluzioni; nessuno è stato corretto di iniziativa.

### infra-controls (commit 62e760d4b)
- command-palette-search-page-entry (low): la palette mantiene la voce "Search for …" che apre /search, rimossa dall'upstream (46a356b08). Voluta?
- command-palette-jobs-removed (low): perché Jobs è stato tolto dalla palette? Sezione Jobs non usata dal cliente?
- event-modal-status-placeholder (low): placeholder __('Select option') sullo Status della valutazione: voluto o pulizia traduzioni?
- Da unificare: regola "Chrome PDF generator per i certificati" (anche in CertificationLinks, CourseCardOverlay, CourseCertification, ProfileCertificates); vite-socketio-port-define usata anche in main.js e socket.js (non censiti).
### settings-roles
- low: profile-tabs-certificates-only (About/Slots/Schedule rimossi: flusso prenotazione slot valutatore non usato? la route Profile redirige ancora a ProfileAbout), profile-cover-image-removed, profile-edit-reduced-fields (headline/social/lingua/bio tolti: voluto che l'utente non possa cambiare lingua?), sidebar-more-group-removed, sidebar-frappe-onboarding-removed (white-label?)
- 265842cde ha tolto min: 1 da lesson_dwell_time (upstream 56efa2428): voluto?
- PROBABILI BUG (non toccati): VideoSourceInput perso in SettingFields; riga membro apre NewMemberModal invece di EditMemberModal (codice fiscale); tab senza key (Course Progress, Email); typo "restoredshortly" in AppSidebar (c634f2bd1)
### lessons-media
- editor-color-picker-tool (low): editorjs-color-picker registrato ma fuori da INLINE_TOOLBAR_ORDER: serve ancora? Da decidere PRIMA di v2.63.0 (che rimuove il Color tool upstream): quale strumento colora il testo e come si mostrano i contenuti già salvati con <lms-inline-color>?
- low: editor-header-inline-toolbar (solo per il colore?), lesson-form-style-tweaks (!bg-transparent sul titolo), upload-toolbox-icon-theme, prose-hr-table-colors
- SICUREZZA (non toccato): sanitizeHTML (514f89cf0) reinserisce gli iframe estratti SENZA DOMPurify, con tutti gli attributi, su campi v-html (oggetti notifiche, bio): possibile XSS; + 2 console.log di debug. Restringere a src YouTube/Vimeo?
- <lms-align> manca in ADD_TAGS di sanitizeJSON lato client: l'allineamento si perde al caricamento (gap noto). Aggiungerlo?
- Tenute separate (stessa regola, pagina diversa): lesson-instructors-hidden-from-students / course-instructors-hidden-from-students; lesson-edit-button-editor-hash / course-outline-editor-hash
### courses-programs-home
- CourseCard: nel blocco "Certificazione disponibile" è tornata la stella+rating upstream al posto dell'icona Award (ae30b25cb) → ripristinare l'icona?
- CourseCard: rimozione del prezzo per i corsi a pagamento (footer rimosso 84bde1890) voluta? (low)
- Scheda "Iscritti" tolta anche ai non-studenti non admin (5e3e6529d): voluto?
- CourseDashboard: stato vuoto upstream "No students enrolled yet" mancante (showStudentsEmptyState inutilizzato); % progresso senza guardia su 0 iscritti (NaN/Infinity); Tooltip row.value → persi in merge?
- Programs.vue: campo 'modified' nella risorsa senza uso visibile: perché?
- PROBABILE BUG (non toccato): ProgramForm validateTitle pulisce program.name invece di program.title
- CourseReviews: blocco residuo di merge (invisibile, recensioni commentate nell'override): confermi di scartarlo al merge?
- low: courses-list-upcoming-students-unpublished (irraggiungibile), program-form-full-document
- Da unificare: course-certificate-pdf-chrome + event-modal-certificate-chrome-pdf (stessa regola)
- CourseOverview (override): valutare il porting del fetch con watch di v2.59.0 (per ora la risorsa outline è commentata → non serve)
### assessments-discussions
- AssignmentSubmissionList: tenere il nostro <header class="main-page-header"> o prendere LayoutHeader upstream? (low)
- Discussions.vue: prop newLabel senza chiamanti da ca61596d2: tenere o togliere?
- Quiz.vue: refuso text-ink-grey-9; avviso di consegna dice "valutate come errate" ma il backend dà 0 senza penalità → cambiare il testo? (collegato alla futura impostazione per-quiz)
- quiz-skip-unanswered-ui (lato schermo) è separata da quiz-submit-unanswered (backend): tenute separate con rimando
- RISCHIO MERGE v2.59.0: PRIVILEGED_ROLES spostato in utils.py e enforce_grading_permission annullerebbe in silenzio il voto del Valutatore; Embed ritornerebbe con RichTextEditor; checkmark Show Answers usa FeatherIcon rimosso
### batches
- low: batch-list-certification-filter-hidden (perché tolto il filtro "Certificazione"?), batch-feedback-removed (feedback di classe tolto per sempre?)
- __('Completato') letterale italiano in BatchStudentProgress: voluto?
- PROBABILE BUG (non toccato): NewBatchModal usa <MultiLink> ma importa MultiSelect → selettore Istruttori forse invisibile e "Instructors is required" blocca la creazione. Da verificare nell'app
- canMakeAnnouncement (dd55dbbbc) inutilizzato: tenerlo?
- upload-unattached-new-lesson: l'upstream (v2.59.0, uploadContext.docname) fa ora esattamente la nostra modifica, che non funzionava più (prop rimossa); preso upstream. Marcare accepted-drift? (ancora temporanea sulla riga upstream)
### risoluzione v2.59.0 — batches
- DA FARE dopo il commit del merge (adeguamento): colore indigo nuovo in RichTextEditor → aggiungere --prose-color-indigo / --prose-highlight-indigo (chiaro/scuro) in Announcements.vue (voce announcement-named-colors). AnnouncementModal già aggiornato (indigo violet-600/100)
- SICUREZZA preesistente (non toccato): BatchOverview.vue rende v-html="batch.data.batch_details" SENZA sanitizeRichHTML (la base v2.58.1 lo aveva): regressione da merge precedente
### risoluzione v2.59.0 — courses
- GIUDIZIO DA CONFERMARE: CourseDashboard — ripristinato lo stato vuoto upstream "No students enrolled yet" (perso dal merge 3caf07639, showStudentsEmptyState rimasto inutilizzato). Se era voluto nasconderlo: togliere il div + v-else
- DA CENSIRE dopo il merge (commit separato): prezzo nascosto in CourseCardOverlay (8fafc219c, nessuna voce né marcatore); showTabs/isValutatore in CourseDetail (Valutatore vede la vista a schede, nessun marcatore)
- Build/runtime da verificare: Dialog con vecchia API (v-model + :options + #body-content) in ProgramForm con beta.24; ProgramForm usa ancora TextEditor
### v2.61.0 settings-modals
- PROBABILE BUG (non toccato): ZoomAccountForm — il merge 027c0c56f ha lasciato il corpo in <template #body-content> (slot inesistente in SettingsLayout) e perso il pulsante Salva #header-actions → campo Google Calendar invisibile e nessun Salva. Da verificare nell'app
- ReviewModal usa ancora la vecchia API Dialog (v-model + :options): prendere upstream e tenere solo l'etichetta tradotta? (recensioni nascoste, impatto basso)
- event-modal-certificate-chrome-pdf cita ancora CertificationLinks.vue ma la regola ora vive in useCertificateViewer.js, Event.vue, CourseCertification.vue → allineare la voce
### risoluzione v2.61.0
- SICUREZZA (rimandata dal committente): override os_lms `get_evaluator_details` scrive in lettura (Course Evaluator + calendario Google) e non ha `enforce_evaluator_access` dell'upstream; bug `is_authorised` (os_lms) vs `is_authorized` (pagina): «Your calendar is set.» non compare mai.
- SICUREZZA preesistente (ancora aperta): `BatchOverview.vue` rende `batch_details` con v-html senza `sanitizeRichHTML`.
- Da verificare nell'app: elenchi Quiz/Compiti/Esercizi/Corsi/Classi/Programmi sul nuovo `ListPage` upstream (stile `os-list-view` non più applicabile; `text-xs` delle celle Compiti rimpicciolisce anche le schede su telefono).
- Accessibilità (bassa): scheda certificato del profilo e scheda programma in StudentHome sono `div` cliccabili, non raggiungibili da tastiera.
- `Profile.vue`: per un nome utente inesistente mostriamo NoPermission (upstream ora NotFound/404): valutare NotFound per DoesNotExistError insieme alla scelta sull'endpoint del profilo.
- MultiLink: tornare al `MultiSelect` upstream alla riconciliazione degli override dopo v2.62.0?
- Pulizie minori: import inutilizzato `ClipboardList, Plus` in ProgrammingExercises.vue; ricerca di Quiz/Esercizi non riporta la paginazione all'inizio; `readOnlyMode` non definito in BatchDetail (già in `batch-detail-header-actions`).
- Traduzioni IT delle nuove stringhe upstream (elenco nelle attività aperte dello stato).
