# Percorso di aggiornamento upstream v2.58.0 → v2.63.0

| Campo | Valore |
| --- | --- |
| Data di avvio | 2026-09-23 |
| Branch di partenza | `feature/oslms` @ `9f36bf4e8` |
| Branch del percorso | `merge/upstream-v2.63.0` |
| Base (ultima release upstream fusa) | `v2.58.0` |
| Arrivo | `v2.63.0` |
| Baseline del rilevatore | 0 errori, 31 avvisi C4 (test non ancora collegati) |

## Piano iniziale

Da `python3 scripts/upstream_plan.py`: ampiezza reale dal diff fra tag, non dalle note di release.

```
Release  Data        Commit  File  frappe-ui                       Rotture  Voci  Non censiti  Congelate
v2.58.1  2026-07-07  23      19    ^1.0.0-beta.7                   0        9     0            0
v2.59.0  2026-07-16  91      195   ^1.0.0-beta.7 → ^1.0.0-beta.24  0        20    82           1
v2.60.0  2026-07-24  15      47    ^1.0.0-beta.24                  0        0     9            0
v2.60.1  2026-07-28  9       6     ^1.0.0-beta.24                  0        1     0            0
v2.61.0  2026-07-30  96      269   ^1.0.0-beta.24                  0        23    96           1
v2.62.0  2026-08-25  200     515   ^1.0.0-beta.24 → 1.0.0-beta.29  0        23    125          1
v2.62.1  2026-08-28  14      7     1.0.0-beta.29                   0        0     1            0
v2.63.0  2026-09-10  65      155   1.0.0-beta.29                   3        5     20           0

```

File con modifiche nostre non censite, in totale: 154. Raccomandazione dello strumento: fermarsi a `v2.58.1` (prima del bump frappe-ui di `v2.59.0`). **Scelta del committente: arrivare a `v2.63.0`.**

## Release attraversate

### v2.58.1 — fusa (`6581b4cbb`)

**Ampiezza reale:** 23 commit, 19 file. frappe-ui invariato (`^1.0.0-beta.7`). 0 rotture dichiarate, 0 file non censiti, 0 pagine congelate toccate. Voci di inventario toccate (9): `chapter-row-chevron-lucide`, `course-form-os-sections`, `course-outline-editor-hash`, `lesson-ai-status`, `lesson-pdf-inline-filename`, `lesson-tag-badges`, `profile-own-without-lms-role`, `program-member-unpublished-course`, `quiz-submit-unanswered`.

**Fermate:**

- **F5 — sovrapposizioni annotate negli intent** (nessun commit `feat` nella release).
  - `quiz-submit-unanswered` contro upstream `567da4f1` (validazione dell'input e normalizzazione delle risposte nulle a `""`). Differenza reale: con la penalità per risposte sbagliate attiva, l'upstream dà −penalità alla domanda saltata, noi 0; il nostro test `test_blank_answer_is_not_penalised_by_negative_marking` sarebbe diventato rosso. **Decisione del committente:** entrambe valide, la scelta fra 0 e penalità deve essere un'impostazione del quiz; nel merge si tengono entrambe (saltata = 0) e l'impostazione si costruisce **dopo v2.63.0**, sul nuovo authoring dei quiz. → *attività aperta*.
  - `lesson-pdf-inline-filename` contro upstream `7e5a825e`/`48252d92`: **confermato** dal committente — versione upstream per `send_private_file`, resta il nostro `Content-Disposition` inline.
- **F1 — conflitto** in `lms/lms/doctype/course_lesson/course_lesson.py` (voce `lesson-pdf-inline-filename`): import uniti (`inspect`, `cache` dell'upstream + il nostro `quote`; `json` non più usato dopo l'upstream); `serve_resource` chiama `_serve_private_file` upstream e poi applica il nostro `Content-Disposition` protetto da `not in response.headers`; il nostro commento «send_private_file non accetta filename» riportato alla forma upstream perché non più vero. I test upstream chiamano `_serve_private_file` direttamente e non passano dal nostro ramo.
- **Fuso da git senza conflitto, verificato:** `lms/lms/doctype/lms_quiz/lms_quiz.py` contiene già normalizzazione upstream + nostro ramo per le saltate. **Ancora di `quiz-submit-unanswered` spostata** su `if answer not in (None, "")`: la vecchia (`result.get("answer") or []`) compariva due volte dopo il merge, anche nella riga upstream, e non avrebbe più segnalato la perdita della nostra.
- Intent di entrambe le voci aggiornati con lo stato post-merge.

**rerere:** nessuna risoluzione riapplicata; registrata quella di `course_lesson.py`.
**Verifiche:** rilevatore 0 errori (baseline 0) dopo la risoluzione e dopo il commit; build frontend verde (40 s); `frappe-ui-colors.json` invariato.

### v2.59.0 — fusa (`f02f4448f`)

**Ampiezza reale:** 91 commit, 195 file. **frappe-ui `^1.0.0-beta.7 → ^1.0.0-beta.24`**. 0 rotture dichiarate. 20 voci di inventario toccate, 82 file con modifiche nostre non censite, 1 pagina congelata toccata. Merge simulato (`git merge-tree`): 30 conflitti, di cui 25 su file non censiti, 3 su file censiti (`CourseCardOverlay.vue`, `BatchOverview.vue`, `CourseDetail.vue`), più `yarn.lock` e `it.po`.

**Fermate prima del merge:**

- **F6 — bump frappe-ui.** L'upstream rimuove anche codemirror, apexcharts, feather-icons, lo script `copy-colors-json` e il file `frontend/src/utils/frappe-ui-colors.json`, ancora importato da 5 nostri file (tra cui `CourseCard.vue`, `AdminBatchDashboard.vue`, `CourseDashboard.vue`, `utils/index.js`). Il 417 di produzione con beta.24 era solo telemetria (`boot_config` assente in Frappe v16); l'upstream dichiara ancora compatibilità con Frappe `<=17.0.0-dev`. **Decisione del committente:** procedere; sistemare solo ciò che rompe la build; **riconciliazione completa dei 23 override frappe-ui una sola volta dopo v2.62.0, contro beta.29** → *attività aperta*.
- **F6 — pagina congelata `CourseOverview.vue`:** nulla da portare nella copia. La correzione upstream (indice del corso ricaricato al cambio di corso) riguarda la risorsa `outline` che la nostra copia ha commentato insieme alla sezione «Course content»; i ritocchi alle dimensioni dei titoli usano classi che la copia non ha.
- **F7 — 82 file non censiti.** **Decisione del committente: censirli tutti prima del merge.** Lavoro diviso in 6 gruppi funzionali disgiunti (impostazioni e ruoli, classi, corsi/programmi/home, lezioni e media, quiz/compiti/esercizi/discussioni, infrastruttura e controlli); un commit per gruppo.
- **F7 chiusa:** 82 file censiti in 6 commit (`62e760d4b` infrastruttura e controlli, `7cf4133e1` impostazioni/profilo/ruoli, `dd363292c` lezioni/editor/media, `2269af35f` corsi/programmi/home, `9a702ddbe` quiz/compiti/esercizi/discussioni, `b4a244376` classi): solo marcatori-commento (verificato riga per riga) più 190 voci; inventario da 35 a 223 voci. Piano rilanciato: 0 file non censiti per v2.59.0.
- **F5:** nelle note 29 `feat`, nel diff reale solo 2; uno solo tocca file nostri (`f0eefa157`, indirizzo nelle transazioni + chiusura del menu in `Link.vue`): aggiunta, nessuna sostituzione.
- **F1 — 32 conflitti**, esattamente quelli previsti dalla simulazione `git merge-tree`; `rerere` non aveva risoluzioni da riapplicare, ha registrato le 32 nuove. Risolti per intent (5 gruppi in parallelo + `it.po`, `package.json`, `yarn.lock`):
  - `it.po`: un solo punto, tenute entrambe le voci; 2 traduzioni upstream nuove che avrebbero coperto il nostro `it.csv` (maiuscole: «Il Contenuto…», «Invio Predefinito») allineate al CSV. `msgfmt -c` pulito.
  - `package.json`/`yarn.lock`: **frappe-ui fissato esatto a `1.0.0-beta.24`** (la versione che risolve il lock upstream; lezione dell'incidente del caret); lock rigenerato dal lock upstream + nostre dipendenze; una sola copia di `prosemirror-state` (1.4.4).
  - `lms_assignment_submission.py`: il Valutatore resta ammesso alla correzione con un insieme **locale** di ruoli privilegiati; l'insieme condiviso in `utils.py` (certificati, progressi) non è toccato; `enforce_grading_permission` non annulla più in silenzio il suo voto.
  - `permissions.py`: ramo Valutatore riportato nel nuovo `resolve_lesson_access`.
  - `Settings.vue`: ricostruito su `SettingsDialog` upstream conservando chiavi stabili delle schede, schede solo-amministratore, schede os_lms (con icone lucide-vue-next tramite fallback), Google Calendar, benvenuto, interruttori della barra laterale.
  - Dettaglio per file nei rapporti degli agenti; ancore aggiornate: `lesson-long-strings-wrap`, `upload-unattached-new-lesson`, `assignment-valutatore-grading`, `quiz-list-style`, `settings-tabs-stable-keys` (2), `settings-oslms-tabs-injection`, `discussion-no-embed-toolbar` (3), `settings-dialog-style`.
- **Decisioni del committente durante il merge:**
  - Embed nelle discussioni: prop OSLMS-CUSTOM `excludeItems` in `RichTextEditor.vue` (esclude `InsertIframe`).
  - Membri: la riga apre la modifica, avatar/nome aprono il profilo.
  - Compiti: la risposta è in sola lettura per chi corregge (nuova voce `assignment-answer-readonly-for-graders`); l'upstream l'aveva resa modificabile.
  - Quiz: il Valutatore legge i quiz dei corsi e delle classi che valuta (nuova voce `permissions-valutatore-quiz-access`); `can_access_quiz` upstream non lo conosceva.
  - Dialogo impostazioni: ritocchi riapplicati su `SettingsDialog` (padding e scorrimento del pannello, id `settings-modal`).
  - `upload-unattached-new-lesson` → `accepted-drift` (l'upstream fa lo stesso; la nostra non funzionava più).
  - `CourseDashboard`: tenuto lo stato vuoto upstream «Nessuno studente iscritto», ripristinato dopo la perdita in un vecchio merge.
- **F2:** nessuna perdita silenziosa (rilevatore 0 errori dopo il commit).
- **F3 — build:** rotta per l'import di `frappe-ui-colors.json` in `AdminBatchDashboard.vue` → `67f2c1008` (variabili CSS `var(--<hue>-400)` come l'upstream). Adeguamento: colore indigo del nuovo editor negli annunci → `2d6b67dc2`. Build verde (40 s), nessun avviso rilevante.
- **Censiti a parte** (`49e48fca3`): prezzo nascosto sulla card del corso, vista a schede per il Valutatore nel dettaglio corso.
- **Vitest:** 21 falliti su 236, **identici nome per nome a quelli del commit precedente al merge** (baseline in worktree temporaneo con beta.7): fallimenti preesistenti (mock di `@/utils` senza `getEditorI18n`, Pinia non installata nei test, Dialog con vecchia API), non regressioni. → *attività aperta*.

### v2.60.0 — fusa (`b994e4ddc`)

**Ampiezza reale:** 15 commit, 47 file; frappe-ui invariato (resta fissato a `1.0.0-beta.24`); 0 rotture dichiarate; nuove dipendenze upstream `pdfjs-dist` 4.10.38 e `vite-plugin-static-copy`.

- **F7:** 2 file non censiti → `010152dab` (`time-ago-italian` in `utils/format.js`; sito `LessonContent.vue` su `upload-pdf-block-mobile`).
- **Collisione di nomi:** l'upstream aggiunge `frontend/src/tests/PdfBlock.test.ts`, che su filesystem case-insensitive (`core.ignorecase=true`) collide con il nostro `pdfBlock.test.ts` → rinominato prima del merge, contenuto invariato (`7ee6053e1`).
- **F5 — decisioni del committente:**
  - **`PdfBlock.vue`:** l'upstream introduce un visualizzatore pdf.js che legge il PDF dentro la lezione anche su mobile, in concorrenza con il nostro (scheda «apri in un'altra scheda» su mobile, iframe su desktop). **Adottato l'upstream** con le nostre correzioni: URL privati di `serve_resource` non ricodificati, testi tradotti (funzione locale su `window.__`, così il test upstream resta verde senza modifiche), riconoscimento del PDF dall'estensione nel blocco upload. La voce `upload-pdf-block-mobile` è riscritta sulla nuova regola; il nostro test, legato alla regola ritirata, è riscritto da lì (`pdfBlockPrivateUrl.test.ts`, 4 casi) come prevede la regola «un test è un output».
  - **`Questionnaire.vue`:** eliminato dal redesign dell'onboarding «persona»; accettato. Voce `questionnaire-surface-base-token` → `accepted-drift`, **da controllare nella prova** (il nuovo onboarding non deve avere lo stesso problema di colore).
- **F1 — 7 conflitti:** `Questionnaire.vue` (eliminazione accettata), `components.d.ts` (generato: presa la versione upstream, poi rigenerato dalla build → `c63738cc9`), `LessonContent.vue` (upstream, ora identico alla nostra regola), `PdfBlock.vue` (vedi sopra), `upload.js` (nostro riconoscimento del tipo e scheda di download + `destroy()` upstream che libera il worker pdf.js), `SettingFields.vue` (tenuto il nostro `isVideoField`, presa la correzione upstream dei checkbox), `vite.config.js` (entrambi gli import).
- **Verifiche:** rilevatore 0 errori (228 voci); build verde (43 s); test upstream `PdfBlock.test.ts` 5/5; Vitest 277 passati, **21 falliti identici alla baseline**, nessun nuovo fallimento.

### v2.60.1 — fusa (`5cb884477`)

**Ampiezza reale:** 9 commit, 6 file; nessun bump, nessuna rottura, nessun file non censito, nessun `feat`. Merge senza conflitti. Tocca `course_lesson.py` (ricerca dei media delle lezioni con il query builder: corregge file con `_` o `%` nel nome) accanto alla voce `lesson-pdf-inline-filename`, senza toccarne la regola; rimuove la dipendenza razorpay, non usata dal nostro codice. Rilevatore 0 errori, build verde.

### v2.61.0 — censimento completato, merge da eseguire

**Ampiezza reale:** 96 commit, 269 file; frappe-ui invariato; 0 rotture dichiarate; 189 voci di inventario toccate; 32 file non censiti; merge simulato: **59 conflitti**.

**Prima del merge:**

- **F5 — 10 `feat` reali, nessuna sostituzione di funzioni nostre:** la «vista studente» diventa una route vera (sparisce l'anteprima incorporata in `CourseEditor`, il pulsante «Modifica» accanto al titolo della lezione diventa il link «Editor view» nell'intestazione: il nuovo link usa ancora l'hash `#course editor`, lo stesso bug che la voce `lesson-edit-button-editor-hash` corregge → correzione da riapplicare al nuovo link nella risoluzione); layout dichiarativo unico per le pagine elenco (riscrittura in cui riesprimere le nostre regole sugli elenchi); integrazione Raven (non in uso); rinomina delle lezioni «Untitled»; onboarding persona (già accettato in v2.60.0).
- **F6 — `CourseOverview.vue` congelata:** l'upstream accorpa solo gli import dei tipi (`@/types/api` → `@/types`); `@/types/api` esiste ancora e la copia compila. **Decisione del committente: nulla da portare.**
- **F7 — 32 file non censiti. Decisione del committente: censirli tutti**, con lo stesso metodo di v2.59.0 (3 gruppi).
- **F7 chiusa:** 32 file censiti in 3 commit (`a7f1e3811` impostazioni e modali, `e6228f450` controlli/layout/infrastruttura, `7b5cbff2b` pagine di corsi e classi); solo marcatori-commento verificati riga per riga; inventario a **280 voci**; piano: 0 file non censiti per v2.61.0; rilevatore 0 errori.
- **Merge non ancora eseguito** (vedi «Passaggio di consegne»).

---

## Passaggio di consegne (2026-09-23)

La sessione si è fermata per contesto esaurito, su richiesta del committente, **prima del merge di v2.61.0**. Tutto è committato sul branch `merge/upstream-v2.63.0`; nessun merge in corso.

| | |
| --- | --- |
| Release fuse | v2.58.1 (`6581b4cbb`), v2.59.0 (`f02f4448f`), v2.60.0 (`b994e4ddc`), v2.60.1 (`5cb884477`) |
| Release corrente | v2.61.0 — fermate F5/F6/F7 chiuse, **manca il merge** (59 conflitti previsti) |
| Da fare dopo | v2.62.0 (bump frappe-ui → `1.0.0-beta.29` + riconciliazione dei 23 override), v2.62.1, v2.63.0 (3 rotture dichiarate), chiusura (Passo 7 della skill) |
| Stato macchina | `.git/oslms-upstream-walk.json` (campo `stop.next_steps` con le risoluzioni già note) |
| Strumenti | `.git/oslms-upstream-walk-tools/` (vedi il suo `README.md`) |

**Per riprendere:** lanciare `/upstream-upgrade` sul branch `merge/upstream-v2.63.0`: la skill trova il file di stato e propone di riprendere dalla fermata `HANDOFF` di v2.61.0.

**Attività aperte registrate:** impostazione per-quiz «domanda saltata = 0 / −penalità» (dopo v2.63.0); riconciliazione degli override frappe-ui (dopo v2.62.0); verifica del nuovo onboarding persona nell'app; 21 test Vitest che falliscono già prima del percorso.

## Domande aperte e probabili bug raccolti (non bloccanti, da decidere con il committente)

Emersi dal censimento e dalle risoluzioni; nessuno è stato corretto di iniziativa.

### infra-controls (commit 62e760d4b)
- command-palette-search-page-entry (low): la palette mantiene la voce "Search for …" che apre /search, rimossa dall'upstream (46a356b08). Voluta?
- command-palette-jobs-removed (low): perché Jobs è stato tolto dalla palette? Sezione Jobs non usata dal cliente?
- event-modal-status-placeholder (low): placeholder __('Select option') sullo Status della valutazione: voluto o pulizia traduzioni?
- Da unificare: regola "Chrome PDF generator per i certificati" (anche in CertificationLinks, CourseCardOverlay, CourseCertification, ProfileCertificates); vite-socketio-port-define usata anche in main.js e socket.js (non censiti).
### settings-roles
- low: profile-tabs-certificates-only (About/Slots/Schedule rimossi: flusso prenotazione slot valutatore non usato? la route Profile redirige ancora a ProfileAbout), profile-cover-image-removed, profile-edit-reduced-fields (headline/social/lingua/bio tolti: voluto che l'utente non possa cambiare lingua?), sidebar-more-group-removed, sidebar-frappe-onboarding-removed (white-label?)
- 265842cde ha tolto min: 1 da lesson_dwell_time (upstream 56efa2428): voluto?
- PROBABILI BUG (non toccati): VideoSourceInput perso in SettingFields; riga membro apre NewMemberModal invece di EditMemberModal (codice fiscale); tab senza key (Course Progress, Email); typo "restoredshortly" in AppSidebar (c634f2bd1)
### lessons-media
- editor-color-picker-tool (low): editorjs-color-picker registrato ma fuori da INLINE_TOOLBAR_ORDER: serve ancora? Da decidere PRIMA di v2.63.0 (che rimuove il Color tool upstream): quale strumento colora il testo e come si mostrano i contenuti già salvati con <lms-inline-color>?
- low: editor-header-inline-toolbar (solo per il colore?), lesson-form-style-tweaks (!bg-transparent sul titolo), upload-toolbox-icon-theme, prose-hr-table-colors
- SICUREZZA (non toccato): sanitizeHTML (514f89cf0) reinserisce gli iframe estratti SENZA DOMPurify, con tutti gli attributi, su campi v-html (oggetti notifiche, bio): possibile XSS; + 2 console.log di debug. Restringere a src YouTube/Vimeo?
- <lms-align> manca in ADD_TAGS di sanitizeJSON lato client: l'allineamento si perde al caricamento (gap noto). Aggiungerlo?
- Tenute separate (stessa regola, pagina diversa): lesson-instructors-hidden-from-students / course-instructors-hidden-from-students; lesson-edit-button-editor-hash / course-outline-editor-hash
### courses-programs-home
- CourseCard: nel blocco "Certificazione disponibile" è tornata la stella+rating upstream al posto dell'icona Award (ae30b25cb) → ripristinare l'icona?
- CourseCard: rimozione del prezzo per i corsi a pagamento (footer rimosso 84bde1890) voluta? (low)
- Scheda "Iscritti" tolta anche ai non-studenti non admin (5e3e6529d): voluto?
- CourseDashboard: stato vuoto upstream "No students enrolled yet" mancante (showStudentsEmptyState inutilizzato); % progresso senza guardia su 0 iscritti (NaN/Infinity); Tooltip row.value → persi in merge?
- Programs.vue: campo 'modified' nella risorsa senza uso visibile: perché?
- PROBABILE BUG (non toccato): ProgramForm validateTitle pulisce program.name invece di program.title
- CourseReviews: blocco residuo di merge (invisibile, recensioni commentate nell'override): confermi di scartarlo al merge?
- low: courses-list-upcoming-students-unpublished (irraggiungibile), program-form-full-document
- Da unificare: course-certificate-pdf-chrome + event-modal-certificate-chrome-pdf (stessa regola)
- CourseOverview (override): valutare il porting del fetch con watch di v2.59.0 (per ora la risorsa outline è commentata → non serve)
### assessments-discussions
- AssignmentSubmissionList: tenere il nostro <header class="main-page-header"> o prendere LayoutHeader upstream? (low)
- Discussions.vue: prop newLabel senza chiamanti da ca61596d2: tenere o togliere?
- Quiz.vue: refuso text-ink-grey-9; avviso di consegna dice "valutate come errate" ma il backend dà 0 senza penalità → cambiare il testo? (collegato alla futura impostazione per-quiz)
- quiz-skip-unanswered-ui (lato schermo) è separata da quiz-submit-unanswered (backend): tenute separate con rimando
- RISCHIO MERGE v2.59.0: PRIVILEGED_ROLES spostato in utils.py e enforce_grading_permission annullerebbe in silenzio il voto del Valutatore; Embed ritornerebbe con RichTextEditor; checkmark Show Answers usa FeatherIcon rimosso
### batches
- low: batch-list-certification-filter-hidden (perché tolto il filtro "Certificazione"?), batch-feedback-removed (feedback di classe tolto per sempre?)
- __('Completato') letterale italiano in BatchStudentProgress: voluto?
- PROBABILE BUG (non toccato): NewBatchModal usa <MultiLink> ma importa MultiSelect → selettore Istruttori forse invisibile e "Instructors is required" blocca la creazione. Da verificare nell'app
- canMakeAnnouncement (dd55dbbbc) inutilizzato: tenerlo?
- upload-unattached-new-lesson: l'upstream (v2.59.0, uploadContext.docname) fa ora esattamente la nostra modifica, che non funzionava più (prop rimossa); preso upstream. Marcare accepted-drift? (ancora temporanea sulla riga upstream)
### risoluzione v2.59.0 — batches
- DA FARE dopo il commit del merge (adeguamento): colore indigo nuovo in RichTextEditor → aggiungere --prose-color-indigo / --prose-highlight-indigo (chiaro/scuro) in Announcements.vue (voce announcement-named-colors). AnnouncementModal già aggiornato (indigo violet-600/100)
- SICUREZZA preesistente (non toccato): BatchOverview.vue rende v-html="batch.data.batch_details" SENZA sanitizeRichHTML (la base v2.58.1 lo aveva): regressione da merge precedente
### risoluzione v2.59.0 — courses
- GIUDIZIO DA CONFERMARE: CourseDashboard — ripristinato lo stato vuoto upstream "No students enrolled yet" (perso dal merge 3caf07639, showStudentsEmptyState rimasto inutilizzato). Se era voluto nasconderlo: togliere il div + v-else
- DA CENSIRE dopo il merge (commit separato): prezzo nascosto in CourseCardOverlay (8fafc219c, nessuna voce né marcatore); showTabs/isValutatore in CourseDetail (Valutatore vede la vista a schede, nessun marcatore)
- Build/runtime da verificare: Dialog con vecchia API (v-model + :options + #body-content) in ProgramForm con beta.24; ProgramForm usa ancora TextEditor
### v2.61.0 settings-modals
- PROBABILE BUG (non toccato): ZoomAccountForm — il merge 027c0c56f ha lasciato il corpo in <template #body-content> (slot inesistente in SettingsLayout) e perso il pulsante Salva #header-actions → campo Google Calendar invisibile e nessun Salva. Da verificare nell'app
- ReviewModal usa ancora la vecchia API Dialog (v-model + :options): prendere upstream e tenere solo l'etichetta tradotta? (recensioni nascoste, impatto basso)
- event-modal-certificate-chrome-pdf cita ancora CertificationLinks.vue ma la regola ora vive in useCertificateViewer.js, Event.vue, CourseCertification.vue → allineare la voce
