# Da fare dopo l'aggiornamento upstream v2.58.0 → v2.63.0

| | |
| --- | --- |
| Creato il | 2026-09-24 |
| Branch | `merge/upstream-v2.63.0` (non unito in `feature/oslms`, non pubblicato) |
| Sito di prova | `http://lms.localhost:8000/lms` (già migrato a v2.63.0), oppure `http://localhost:8081` con `yarn dev` |
| Rapporti di riferimento | [percorso](2026-09-23-percorso-v2.58.0-v2.63.0.md), [verifica](2026-09-24-v2.63.0.md) |

Elenco operativo: si spunta una casella quando la prova è fatta o la correzione è committata. Accanto a ogni correzione si annotano il commit o il motivo per cui è stata scartata.

**Ordine consigliato:** prima le prove della parte A. Poi si unisce il branch in `feature/oslms`. Le correzioni di sicurezza B1 vanno fatte prima del rilascio in produzione: sono già presenti in produzione oggi, non le ha introdotte l'aggiornamento.

---

## A. Prove nell'app (prima di unire il branch)

Per ogni prova sono indicati il ruolo, cosa fare, cosa deve succedere e, fra parentesi, perché è in lista.

### Avvio e ruoli

- [ ] **A1 — Studente, Docente, Moderatore.** Accedi con ciascun ruolo. La SPA si carica, la barra laterale mostra le voci giuste per il ruolo, le impostazioni si aprono. *(Override API di os_lms ripristinati, `a4363c4a8`: prima il profilo utente andava in errore.)*
- [ ] **A2 — Qualsiasi ruolo.** Apri la Home con la console del browser aperta. Non deve comparire nessun errore, in particolare nessun 417 su `boot_config`. *(Ponte della telemetria, `51bbf7158`.)*
- [ ] **A3 — Moderatore.** In Impostazioni → Membri: l'elenco si carica. Crea un membro con il solo ruolo Valutatore, poi cambiagli il ruolo: deve avere solo i ruoli scelti. *(Voce `member-valutatore-role-exact-roles`.)*
- [ ] **A4 — Amministratore / Moderatore.** Impostazioni → Integrazioni → Raven: l'amministratore vede l'avviso «L'editor delle condizioni richiede una versione più recente di Frappe» e nessun errore. Il Moderatore non vede la scheda. *(Voci `raven-framework-ui-stub`, `settings-raven-admin-only`.)*

### Quiz

- [ ] **A5 — Studente.** In un quiz senza «mostra risposte»: salta una domanda, vai avanti con Avanti e premi «Invia Quiz» da una domanda intermedia. La conferma dice «Hai 1 domanda senza risposta. Se consegni ora, varranno 0 punti», la consegna riesce e la saltata vale 0. Ripeti in un quiz **con** «mostra risposte»: Verifica, Avanti e «Invia Quiz» stanno affiancati. *(Voci `quiz-skip-unanswered-ui`, `quiz-submit-unanswered`.)*
- [ ] **A6 — Docente, poi Studente.** Nel quiz attiva «Abilita penalità», metti 1 punto da togliere e attiva «Penalizza le domande senza risposta». Da Studente salta una domanda: la conferma dice «ognuna toglierà 1 punto» e la saltata vale −1. Con l'interruttore spento vale 0. *(Impostazione `1f6913194`.)*
- [ ] **A7 — Studente.** Quiz a tempo: passa a un'altra scheda del browser e torna dopo la scadenza. Il quiz risulta consegnato una sola volta. *(Voce `quiz-timer-wall-clock`.)*
- [ ] **A8 — Docente.** Nel nuovo modulo del quiz usa «Anteprima» e rispondi. Non c'è il pulsante di consegna, e nella pagina Invii non compare nessuna consegna. *(Guardia `preview` sul pulsante di consegna.)*
- [ ] **A9 — Docente.** Crea domande dalla banca domande. I tipi sono in italiano («Scelta singola», «Risposta breve», «A risposta aperta»), e sono tradotti anche il modulo del quiz e la pagina Invii. *(Traduzioni `b1cb0e654`.)*
- [ ] **A10 — Moderatore.** In un quiz con la sorveglianza attiva (Chrome, webcam): avvia il quiz, cambia scheda e consegna. Nel dettaglio della consegna compaiono il numero di violazioni e il «Registro della sorveglianza» con le istantanee. *(Proctoring adottato dall'upstream.)*

### Valutatore di classe

- [ ] **A11 — Valutatore.** Apri una lezione con un quiz di un corso della sua classe: il quiz si carica. *(Regressione corretta `b8b1e82a1`.)*
- [ ] **A12 — Valutatore.** Dashboard della classe → studente → riga di un quiz. Il dettaglio mostra le risposte **senza** Salva, con i voti delle domande aperte non modificabili e **senza** il registro della sorveglianza, anche se ci sono violazioni. Dalla briciola «Invii»: nessuna casella di selezione, nessun Elimina, nessun filtro per quiz. Su telefono «indietro» torna alla pagina precedente. *(Voci `quiz-submission-valutatore-readonly`, `valutatore-phone-back-history`, `proctoring-log-hidden-from-valutatore`.)*

### Lezioni

- [ ] **A13 — Docente.** Nell'editor della lezione: seleziona del testo e usa lo strumento colore (secchiello) per il colore del testo e l'evidenziazione, anche aprendo il selettore di sistema. Poi allinea al centro un paragrafo, salva e ricarica: colore e allineamento restano. Prova anche a togliere il colore. *(Strumento riscritto `4ff63b218`, voce `editor-inline-color-persist`.)*
- [ ] **A14 — Studente.** In un corso con blocco sequenziale, apri una lezione ancora bloccata che contiene un quiz: compare l'avviso «quiz bloccato». *(`contentHasQuiz` su `parseStoredEditorJs`.)*

### Ricerca ed elenchi

- [ ] **A15 — Qualsiasi ruolo.** Cmd+K → Vai a → Corsi, poi digita: compaiono solo corsi, e la riga «Cerca…» resta in fondo. *(Voce `command-palette-result-routes`.)*
- [ ] **A16 — Moderatore.** In Corsi e in Classi cambia «righe per pagina» nel piè dell'elenco e scorri: nessuna riga si ripete o manca. *(`limit_page_length` ripristinato.)*
- [ ] **A17 — Moderatore.** In un campo a scelta multipla (es. valutatori della classe): cerca, chiudi e riapri. L'elenco riparte completo e «Cancella» funziona. *(Voce `multilink-reka-combobox`.)*
- [ ] **A18 — Moderatore.** Nel modulo di una classe o di un corso, i campi collegati (es. corso, categoria) mostrano il valore salvato e non restano vuoti. *(Override `Combobox` vecchio, vedi B2.1.)*

### Da release precedenti

- [ ] **A19 — Gestore.** La voce «Carica file» nel menu utente e il nuovo import dati sotto Configurazione. *(Decisione v2.62.0.)*
- [ ] **A20 — Studente nuovo.** Il nuovo onboarding «persona» si vede senza problemi di colore. *(`Questionnaire.vue` eliminato in v2.60.0.)*
- [ ] **A21 — Docente.** Gli elenchi Quiz e Compiti con l'aspetto upstream, per decidere B6.1. *(Decisione v2.61.0.)*
- [ ] **A22 — Studente.** Un PDF in una lezione su desktop Chrome (iframe), su Safari/iPhone e su Android (pdf.js), compreso un PDF privato. *(Voce `upload-pdf-block-mobile`.)*
- [ ] **A23 — Docente, poi Valutatore.** Il Docente crea, modifica e riprogramma una live dal modulo della classe; il Valutatore della classe vede le live in sola lettura. *(Decisioni v2.62.0.)*
- [ ] **A24 — Studente, su telefono.** In un corso la barra «Continua» non copre la barra di navigazione, e i pulsanti AI stanno sopra entrambe. *(Voci `course-mobile-cta-bar`, `ai-fixed-buttons-layouts`.)*

---

## B. Correzioni da fare

### B1 — Sicurezza (prima del rilascio in produzione)

- [ ] **B1.1 — Custom DocPerm su `LMS Quiz Submission`.** In produzione e nel DB di sviluppo lo Studente ha lettura, scrittura e creazione sulle proprie consegne. Il JSON del doctype ora è quello upstream (sola lettura), ma i Custom DocPerm lo scavalcano. Da decidere: togliere scrittura e creazione allo Studente (la consegna passa dalle API whitelisted) e capire come applicarlo in produzione (patch o fixture). Sul DB di produzione solo query di lettura per verificare.
- [ ] **B1.2 — Override os_lms `get_evaluator_details`** (`apps/os_lms/os_lms/os_lms/api.py`). Scrive (Course Evaluator e calendario Google) dentro una chiamata di sola lettura e non ha il controllo `enforce_evaluator_access` dell'upstream.
- [ ] **B1.3 — `is_authorised` / `is_authorized`.** os_lms restituisce `is_authorised` (`api.py`), mentre `ProfileEvaluator.vue` legge `is_authorized`: «Your calendar is set.» non compare mai. Da correggere insieme a B1.2.
- [ ] **B1.4 — `sanitizeHTML`** (`frontend/src/utils/index.js`). Reinserisce gli iframe estratti senza passarli da DOMPurify, con tutti gli attributi, su campi resi con `v-html` (oggetti delle notifiche, bio): possibile XSS. Proposta: limitare gli iframe a `src` YouTube e Vimeo. Togliere anche i 2 `console.log` di debug (righe ~1072 e ~1139).

### B2 — Override frappe-ui (branch dedicato, decisione del committente)

- [ ] **B2.1 — Riconciliare i 22 override** in `frontend/src/overrides/frappe-ui/` con frappe-ui **1.0.0-beta.29**: circa 3000 righe di differenza.
  - `Combobox` (1082 righe): prop `align` dei `Link` e `modelValue`, collegato alla prova A18.
  - `FontColor`: 9 sfondi grezzi.
  - fork `DataImport`, da riconfrontare su tutti e 6 i componenti.
  - `Menu.vue`: l'originale non esiste più, quindi l'override è morto e va tolto.
- [ ] **B2.2 — `MultiLink`:** decidere se tornare al `MultiSelect` upstream durante la riconciliazione o tenere la nostra riscrittura reka.

### B3 — Test

- [ ] **B3.1 — Riconciliare una sola volta i 156 test Vitest rossi**, già classificati in `.git/oslms-upstream-walk-tools/fail-263.txt` (dettaglio in `test_reconcile` di `.git/oslms-upstream-walk.v2.63.0.done.json`). Regola: si ripara il codice o si decide la regola con il committente, mai il test per farlo passare.
- [ ] **B3.2 — I 3 test rossi delle simulazioni AI** (`test_api` eval, `test_orchestrator` quota, `test_runner`): capire se falliscono già su `start_commit`.

### B4 — Probabili bug ancora presenti (riverificati il 2026-09-24)

- [ ] **B4.1 — `components/Settings/ZoomAccountForm.vue`.** Il corpo sta in `<template #body-content>`, uno slot che `SettingsLayout` non ha, e manca il pulsante Salva: il campo Google Calendar probabilmente non si vede e non si può salvare. Da verificare nell'app, poi correggere.
- [ ] **B4.2 — `pages/Forms/ProgramForm.vue`.** `validateTitle` ripulisce `program.name` invece di `program.title`, che è il campo legato all'input.
- [ ] **B4.3 — `components/Sidebar/AppSidebar.vue`.** Refuso «restoredshortly» nell'avviso di aggiornamento del sito.

### B5 — Traduzioni IT

- [ ] **B5.1** Le 72 stringhe già senza traduzione prima di v2.63.0 nei file toccati.
- [ ] **B5.2** Le 70 stringhe della scheda Raven (solo amministratore, bassa priorità).
- [ ] **B5.3** Verificare gli elenchi di v2.61.0 e v2.62.0 (elenco nelle attività aperte dello stato archiviato).

### B6 — Decisioni del committente (dopo le prove)

- [ ] **B6.1 — Stile degli elenchi Quiz e Compiti:** tenere l'aspetto upstream o riportare `os-list-view` (dopo A21).
- [ ] **B6.2 — Vecchio `editorjs-color-picker`:** toglierlo, insieme alla dipendenza, ora che c'è lo strumento colore riscritto.
- [ ] **B6.3 — Div `bg-gradient-overlay` inerte** nel layout desktop: togliere o dargli uno stile.
- [ ] **B6.4 — 33 voci d'inventario «a bassa confidenza»** toccate dal percorso: confermare che la regola dichiarata sia quella vera (elenco nel [rapporto di verifica](2026-09-24-v2.63.0.md)).
- [ ] **B6.5 — Domande aperte raccolte durante il censimento:** l'elenco è nel rapporto di percorso, sezione «Domande aperte e probabili bug raccolti». Molte sono di prodotto, per esempio i campi del profilo tolti o il filtro Certificazione delle classi. Vanno riviste una per una: alcune voci sono già state superate dal percorso.

### B7 — Pulizie minori (da riverificare prima di toccarle, tranne dove indicato)

- [ ] Import inutilizzato `ClipboardList, Plus` in `pages/ProgrammingExercises/ProgrammingExercises.vue` (verificato).
- [ ] La ricerca di Quiz ed Esercizi non riporta la paginazione alla prima pagina.
- [ ] Accessibilità: la scheda del certificato nel profilo e la scheda del programma in `StudentHome` sono `div` cliccabili, non raggiungibili da tastiera.
- [ ] `Profile.vue`: per un nome utente inesistente mostra NoPermission, mentre l'upstream mostra NotFound (404).
