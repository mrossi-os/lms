# Inventario delle personalizzazioni marcate OSLMS-CUSTOM.
#
# Fonte di verità unica: da qui si genera la verifica dopo ogni merge upstream.
# Chi aggiunge una personalizzazione aggiunge anche la voce, nella stessa sessione.
# Schema e criteri: vedi README.md in questa cartella.

schema_version = 1
area = "spa-grafts"

# ---------------------------------------------------------------------------
# Card laterale del corso (CourseCardOverlay.vue)
# ---------------------------------------------------------------------------

[[entries]]
id = "course-card-admin-gate"
title = "Menu di gestione sulla card corso visibile anche al Docente"
layer = "spa-graft"
confidence = "high"
intent = """
Il ruolo custom Docente è un istruttore globale: deve essere trattato come amministratore
del corso sulla card (niente CTA di iscrizione, azioni di gestione) esattamente come
Moderator e l'istruttore del corso. L'upstream conosce solo Moderator e istruttore."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "Boolean(user.data?.is_docente)"
  symbol = "isAdmin"

  [entries.visibility]
  subject = "trattamento da amministratore sulla card corso"
  allow = ["Moderator", "Docente", "course-instructor"]
  deny = ["Student", "Valutatore", "Guest"]

[[entries]]
id = "course-card-valutatore-no-enroll"
title = "Nessuna CTA di iscrizione per il Valutatore"
layer = "spa-graft"
confidence = "high"
intent = """
Il Valutatore di un batch che contiene il corso lo consulta in sola lettura e non è mai
uno studente. Raggiunge le lezioni senza iscrizione: offrirgli la CTA di iscrizione è una
trappola, perché un click crea una LMS Enrollment reale e lo elenca tra gli studenti.
Il flag is_valutatore è iniettato dall'override os_lms di get_course_details."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "Boolean(props.course.data?.is_valutatore)"
  symbol = "isValutatore"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "!isAdmin.value && !isValutatore.value"
  symbol = "canEnroll"

  [[entries.sites]]
  file = "frontend/src/types/api.ts"
  anchor = "is_valutatore?: 0 | 1"

  [entries.visibility]
  subject = "CTA di iscrizione / acquisto sulla card corso"
  allow = ["Student", "Guest"]
  deny = ["Valutatore", "Moderator", "Docente", "course-instructor"]

[[entries]]
id = "course-card-certificate-dedup"
title = "Niente doppio pulsante certificato dopo l'emissione"
layer = "spa-graft"
confidence = "high"
intent = """
Una volta emesso il certificato deve restare solo il «View Certificate» di
CertificationLinks, non anche un «Get Certificate». LMS Enrollment.certificate non viene
valorizzato all'emissione, quindi la fonte autorevole è get_certification_details (lo
stesso segnale che usa CertificationLinks); si attende il caricamento per evitare flash."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "url: 'lms.lms.api.get_certification_details'"
  symbol = "certification"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "!certification.data.certificate"
  symbol = "canGetCertificate"

[[entries]]
id = "trueskills-certificate-viewer"
title = "Il certificato TrueSkills sostituisce il PDF interno"
layer = "spa-graft"
confidence = "high"
intent = """
Per i corsi con emissione TrueSkills il certificato di completamento è l'immagine
openbadge TrueSkills (aperta in linea), non il PDF del Print Format interno. Vale sulla
card corso e nella pagina certificati del profilo; i certificati interni o di batch
continuano ad aprire il PDF. TrueSkills è alternativo al certificato interno, quindi il
pulsante si abilita anche con enable_certification spento."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "openCourseCertificate(props.course.data?.name)"
  symbol = "fetchCertificate"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "props.course.data?.trueskills_certificate_enabled"
  symbol = "canGetCertificate"

  [[entries.sites]]
  file = "frontend/src/pages/ProfileCertificates.vue"
  anchor = "const { openIssuedCertificate } = useCertificateViewer()"

  [[entries.sites]]
  file = "frontend/src/types/lms/LMSCourse.ts"
  anchor = "trueskills_certificate_enabled?: 0 | 1"

[[entries]]
id = "course-card-outline"
title = "Programma del corso navigabile al posto delle statistiche"
layer = "spa-graft"
confidence = "high"
intent = """
La card mostra il programma del corso (CourseOutline) navigabile al posto del blocco
«This course includes» con le statistiche, che resta commentato nel file per un facile
ripristino insieme alle sue computed e alle icone importate."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "<div v-if=\"!hideOutline\" :class=\"readOnlyMode ? 'mt-4' : 'mt-0'\">"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "OSLMS-CUSTOM: \"This course includes\" stats block disabled"

# ---------------------------------------------------------------------------
# Trattamento mobile della pagina corso
# ---------------------------------------------------------------------------

[[entries]]
id = "course-mobile-cta-bar"
title = "Barra fissa «Continue Learning» su telefono"
layer = "spa-graft"
confidence = "high"
intent = """
Sul telefono (<640px, MobileLayout con barra di navigazione in basso) lo studente iscritto
vede una barra fissa a tutta larghezza con «Continue Learning», che diventa l'unica azione
primaria; il pulsante nella card viene nascosto. La pagina aggiunge padding in fondo per
non coprire l'ultima sezione, e pubblica la presenza della barra nello store mobileCta
così che i pulsanti AI flottanti si sollevino invece di sovrapporsi. Soglia 640 e non 768
per restare dentro la shell telefono."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "() => isMobile.value && Boolean(props.course.data?.membership)"
  symbol = "showMobileCta"

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "{ 'pb-36': showMobileCta }"

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "mobileCta.setBar(visible)"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "v-if=\"!hideContinueCta\""

  [[entries.sites]]
  file = "frontend/src/stores/mobileCta.js"
  anchor = "defineStore('mobile-cta'"

  [[entries.sites]]
  file = "frontend/src/oslms/components/AiFixedButtons.vue"
  anchor = "mobileCta.barVisible ? 'bottom-34 sm:bottom-6' : 'bottom-20 sm:bottom-9'"

[[entries]]
id = "course-mobile-outline-bottom"
title = "Programma del corso in fondo alla pagina su telefono"
layer = "spa-graft"
confidence = "high"
intent = """
Sul telefono (<640px) il programma del corso si sposta in fondo alla pagina, sotto le
sezioni di contenuto, e viene tolto dalla card (hideOutline). In ogni layout più largo
resta nella card, quindi il rendering in fondo è condizionato alla shell telefono per non
duplicare il programma."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "<section v-if=\"isMobile\">"

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "const isMobile = computed<boolean>(() => size.width < 640)"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "hideOutline?: boolean"

[[entries]]
id = "course-card-hide-when-empty"
title = "La card corso sparisce quando resterebbe vuota"
layer = "spa-graft"
confidence = "high"
intent = """
Su telefono programma e CTA escono dalla card, che può rimanere senza contenuto: invece di
un riquadro vuoto con bordo la card non si rende. Per saperlo replica la visibilità di
CertificationLinks usando la stessa risorsa certification."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "v-if=\"hasCardContent\""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "const certLinksVisible = computed<boolean>"

[[entries]]
id = "course-tablet-inline-card"
title = "Card corso completa in linea sul tablet"
layer = "override-file"
confidence = "high"
intent = """
Tra 640 e 1023px la pagina è a colonna singola ma la card laterale desktop (lg:) è
nascosta: lì si rende in linea la card completa originale, così il trattamento mobile
(barra fissa + programma in fondo) resta confinato al telefono."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "<div v-if=\"isNarrow && !isMobile\" class=\"lg:hidden\">"

# ---------------------------------------------------------------------------
# Pagina corso (override CourseOverview.vue)
# ---------------------------------------------------------------------------

[[entries]]
id = "course-hero-banner"
title = "Banner hero del corso"
layer = "spa-graft"
confidence = "high"
intent = """
Se il corso ha un hero abilitato con un media, il media compare come sezione a tutta
larghezza con angoli arrotondati tra l'introduzione breve e «About this course», e usa lo
stesso player delle lezioni. In quel caso il video di anteprima sparisce dalla card
(hideVideo) e la card scende per allinearsi. I dati hero arrivano annidati dall'override
os_lms di get_course_details."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "Boolean(heroData.value?.enabled && heroData.value?.media_url)"
  symbol = "hasHero"

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = ":hero=\"heroData\""

  [[entries.sites]]
  file = "frontend/src/oslms/components/CourseHero.vue"
  anchor = "<section class=\"mb-8 w-full overflow-hidden rounded-md bg-black\">"

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = "v-if=\"course.data?.video_link && !hideVideo\""

  [[entries.sites]]
  file = "frontend/src/components/CourseCardOverlay.vue"
  anchor = ":class=\"{ 'md:mt-16': hideVideo }\""

[[entries]]
id = "course-instructors-hidden-from-students"
title = "Istruttori del corso nascosti agli studenti"
layer = "override-file"
confidence = "high"
intent = """
L'elenco degli istruttori in testa alla pagina corso è visibile solo a moderatori e
istruttori del corso, non agli studenti."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "v-if=\"isCourseAdmin && course.data.instructors?.length\""

  [entries.visibility]
  subject = "elenco istruttori nella pagina corso"
  allow = ["Moderator", "course-instructor"]
  deny = ["Student", "Guest"]

[[entries]]
id = "course-short-intro-preline"
title = "Introduzione breve del corso su più righe"
layer = "override-file"
confidence = "high"
intent = """
short_introduction è un campo Small Text in cui gli autori scrivono su più righe; l'HTML
collasserebbe gli a capo, quindi si conservano con whitespace-pre-line."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "class=\"text-ink-gray-7 leading-6 whitespace-pre-line\""

[[entries]]
id = "course-content-section-removed"
title = "Sezione «Course content» rimossa dalla pagina corso"
layer = "override-file"
confidence = "high"  # rimozione confermata voluta dal committente il 2026-09-23
intent = """
La sezione «Course content» dell'upstream è commentata, insieme alla risorsa
get_course_outline che la alimentava (per non sprecare la chiamata API). Il programma
del corso vive nella card (o in fondo su telefono). Motivazione dedotta: evitare di
mostrare due volte il programma."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "OSLMS-CUSTOM: \"Course content\" section removed"

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "OSLMS-CUSTOM: outline resource/stats removed"

[[entries]]
id = "course-description-collapsible"
title = "Descrizione del corso comprimibile"
layer = "override-file"
confidence = "high"
intent = """
La descrizione lunga del corso si rende con CourseDescription, che la comprime con
«mostra di più / di meno» invece di mostrarla sempre per intero."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "<CourseDescription :description=\"course.data.description\" />"

[[entries]]
id = "course-feature-sections"
title = "Sezioni in evidenza del corso"
layer = "override-file"
confidence = "high"
intent = """
Il corso può avere sezioni in evidenza configurate dall'autore, rese dopo la descrizione;
i dati arrivano annidati dall'override os_lms di get_course_details e il rendering tiene
conto dell'iscrizione dello studente."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = ":sections=\"featureSections\""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "(props.course.data as any)?.feature_sections ?? null"

[[entries]]
id = "course-reviews-removed"
title = "Recensioni rimosse dalla pagina corso"
layer = "override-file"
confidence = "high"  # rimozione confermata voluta dal committente il 2026-09-23
intent = """
Il blocco CourseReviews dell'upstream è commentato: le recensioni non compaiono nella
pagina corso. Motivazione non documentata nel codice, presumibilmente scelta del
committente."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "OSLMS-CUSTOM: CourseReviews removed"

[[entries]]
id = "course-creator-card-removed"
title = "Card dell'autore rimossa dalla pagina corso"
layer = "override-file"
confidence = "high"  # rimozione confermata voluta dal committente il 2026-09-23
intent = """
La CourseCreatorCard dell'upstream nella colonna laterale è commentata. Coerente con
l'occultamento degli istruttori agli studenti, ma la motivazione non è scritta."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "OSLMS-CUSTOM: CourseCreatorCard removed"

[[entries]]
id = "course-overview-tag-colors"
title = "Colori dei tag anche nella pagina corso"
layer = "override-file"
confidence = "high"
intent = """
La mappa colori dei tag (LMS OS Tag) è fornita da Courses.vue solo nell'elenco corsi;
la pagina corso la ricarica e la fornisce, altrimenti CourseTagBadges non si rende."""

  [[entries.sites]]
  file = "frontend/src/overrides/pages/Courses/CourseOverview.vue"
  anchor = "provide('tagColorMap', tagColorMap)"

# ---------------------------------------------------------------------------
# Programma del corso (ChapterRow.vue)
# ---------------------------------------------------------------------------

[[entries]]
id = "chapter-row-chevron-lucide"
title = "Chevron dei capitoli come componente lucide"
layer = "spa-graft"
confidence = "high"
intent = """
Il chevron di apertura del capitolo usa il componente lucide ChevronRight perché con
la resa upstream non compariva."""

  [[entries.sites]]
  file = "frontend/src/components/ChapterRow.vue"
  anchor = "<ChevronRight"

[[entries]]
id = "lesson-tag-badges"
title = "Badge dei tag sulle lezioni nel programma"
layer = "spa-graft"
confidence = "high"
intent = """
Ogni lezione nel programma del corso mostra i propri tag come badge. I tag arrivano
come stringa separata da virgole dall'override os_lms di get_course_outline."""

  [[entries.sites]]
  file = "frontend/src/components/ChapterRow.vue"
  anchor = "import CourseTagBadges from '@/oslms/components/CourseTagBadges.vue'"

  [[entries.sites]]
  file = "frontend/src/components/ChapterRow.vue"
  anchor = ":tags=\"lesson.tags\""

  [[entries.sites]]
  file = "frontend/src/types/api.ts"
  anchor = "tags?: string"

[[entries]]
id = "lesson-ai-status"
title = "Stato di indicizzazione AI delle lezioni nell'editor"
layer = "spa-graft"
confidence = "high"
intent = """
Nel programma in modalità modifica ogni lezione mostra in forma compatta lo stato di
indicizzazione per l'assistente AI (RAG os_lms). Lo stato arriva dall'override os_lms
di get_course_outline."""

  [[entries.sites]]
  file = "frontend/src/components/ChapterRow.vue"
  anchor = "<LessonAIStatus :lesson=\"lesson\" />"

  [[entries.sites]]
  file = "frontend/src/types/api.ts"
  anchor = "index_status?: string"

  [entries.visibility]
  subject = "stato AI della lezione nel programma"
  allow = ["course-editor"]
  deny = ["Student", "Guest"]

# ---------------------------------------------------------------------------
# Video, batch, profilo
# ---------------------------------------------------------------------------

[[entries]]
id = "video-preview-plyr"
title = "Anteprima YouTube/Vimeo nel player Plyr"
layer = "spa-graft"
confidence = "high"
intent = """
Un'anteprima YouTube/Vimeo si riproduce con lo stesso player Plyr delle lezioni invece
che con l'iframe nudo del provider, così non compaiono i controlli del provider (titolo,
canale, condividi, griglia di fine video). Il contenitore riserva il 16:9 mentre Plyr
parte, per non far saltare la pagina."""

  [[entries.sites]]
  file = "frontend/src/components/VideoPreview.vue"
  anchor = "<div v-if=\"isEmbed\" class=\"preview-player aspect-video w-full bg-black\">"

[[entries]]
id = "batch-description-preline"
title = "Descrizione del batch su più righe"
layer = "spa-graft"
confidence = "high"
intent = """
La descrizione del batch è un campo Small Text scritto su più righe; si conservano gli a
capo con whitespace-pre-line, come per l'introduzione breve del corso."""

  [[entries.sites]]
  file = "frontend/src/pages/Batches/BatchOverview.vue"
  anchor = "class=\"my-3 leading-6 text-ink-gray-7 card whitespace-pre-line\""

[[entries]]
id = "batch-preview-video"
title = "Video di anteprima nella pagina batch"
layer = "spa-graft"
confidence = "high"
intent = """
Il video di anteprima del batch compare sotto la descrizione breve a ogni larghezza (mai
in colonna laterale), largo quanto la descrizione e in 16:9. Solo il video: il resto di
BatchOverlay (prezzo, posti, CTA di iscrizione) resta fuori da questa pagina."""

  [[entries.sites]]
  file = "frontend/src/pages/Batches/BatchOverview.vue"
  anchor = ":video-link=\"batch.data.video_link\""

# ---------------------------------------------------------------------------
# Programmi
# ---------------------------------------------------------------------------

[[entries]]
id = "program-description"
title = "Descrizione del programma visibile allo studente"
layer = "spa-graft"
confidence = "high"
intent = """
La descrizione del programma (campo custom os_lms) si mostra sia nel dettaglio del
programma sia nel dialogo di iscrizione, con la formattazione conservata e sanificata
con sanitizeRichHTML. hasRichContent, aggiunta al file upstream sanitizeRichHTML.ts,
nasconde la descrizione svuotata che l'editor salva come <p></p>: senza, lo studente
vedrebbe un blocco vuoto."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramDetail.vue"
  anchor = ":description=\"sanitizeRichHTML(program.data.description)\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramEnrollment.vue"
  anchor = ":description=\"sanitizeRichHTML(program.data.description)\""

  [[entries.sites]]
  file = "frontend/src/utils/sanitizeRichHTML.ts"
  anchor = "export const hasRichContent"
  symbol = "hasRichContent"

[[entries]]
id = "home-programs-row"
title = "Riga dei programmi nella home dello studente"
layer = "spa-graft"
confidence = "high"
intent = """
La home dello studente mostra i suoi programmi o, se non è iscritto a nessuno, quelli
pubblicati (stesso schema della riga dei batch). Riusa la risorsa programs dello store
settings già caricata dalla sidebar invece di una seconda chiamata; la carica solo se
nessuno l'ha fatto (layout mobile senza sidebar). Un programma non iscritto apre il
dialogo di iscrizione invece del dettaglio."""

  [[entries.sites]]
  file = "frontend/src/pages/Home/StudentHome.vue"
  anchor = "<div v-if=\"homePrograms.length\" class=\"mt-10\">"

  [[entries.sites]]
  file = "frontend/src/pages/Home/StudentHome.vue"
  anchor = "if (!programs.data && !programs.loading) programs.reload()"

[[entries]]
id = "program-member-unpublished-course"
title = "Membri del programma vedono i corsi non pubblicati"
layer = "spa-graft"
confidence = "high"
intent = """
Un programma si pubblica nel suo insieme: chi ne è membro vede, apre e si iscrive a ogni
corso del programma anche se il corso da solo non è pubblicato. La regola vive in tre
guardie, due delle quali in file upstream: get_course_details e la validazione
dell'iscrizione di LMSEnrollment, entrambe tramite is_course_in_member_program (definita in
lms/lms/utils.py accanto a is_course_valutatore); la terza è la guardia di redirect di
CourseDetail.vue, che legge il flag in_member_program esposto dall'override os_lms di
get_course_details. Se sparisce una delle guardie server, lo studente del programma non
vede il corso o non può iscriversi; se sparisce quella della pagina, viene rimandato alla
lista corsi. Decisione del cliente del 2026-09-23."""

  [[entries.sites]]
  file = "lms/lms/utils.py"
  anchor = "def is_course_in_member_program(course: str, user: str = None) -> bool:"
  symbol = "is_course_in_member_program"

  [[entries.sites]]
  file = "lms/lms/utils.py"
  anchor = "and not is_course_in_member_program(course)"
  symbol = "get_course_details"

  [[entries.sites]]
  file = "lms/lms/doctype/lms_enrollment/lms_enrollment.py"
  anchor = "and not is_course_in_member_program(self.course, self.member)"
  symbol = "LMSEnrollment.validate_course_enrollment_eligibility"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDetail.vue"
  anchor = "!course.data?.in_member_program &&"

  [[entries.sites]]
  file = "frontend/src/types/api.ts"
  anchor = "in_member_program?: 0 | 1"

  [entries.checks]
  backend = "apps/os_lms/os_lms/os_lms/tests/test_program_course_access.py"

# ---------------------------------------------------------------------------
# Override di componenti frappe-ui
# ---------------------------------------------------------------------------

[[entries]]
id = "toc-node-translations"
title = "Traduzione delle stringhe dell'indice nel TextEditor"
layer = "override-file"
confidence = "high"
intent = """
Copia integrale di TocNodeView.vue di frappe-ui con due sole differenze: il messaggio
«There are no headings in this document.» passa per __() e il titolo del pulsante di
rimozione è in italiano. Va risincronizzata con l'upstream a ogni aggiornamento di
frappe-ui che tocca il file."""

  [[entries.sites]]
  file = "frontend/src/overrides/frappe-ui/src/components/TextEditor/extensions/toc-node/TocNodeView.vue"
  anchor = "__('There are no headings in this document.')"

  [[entries.sites]]
  file = "frontend/src/overrides/frappe-ui/src/components/TextEditor/extensions/toc-node/TocNodeView.vue"
  anchor = ":title=\"__('Rimuovi indice')\""

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 — innesti senza marcatore nei file toccati da v2.58.1
# (Fase 0-bis: completamento dell'inventario prima del primo merge)
# ---------------------------------------------------------------------------

[[entries]]
id = "course-form-os-sections"
title = "Impostazioni custom del corso nel modulo impostazioni"
layer = "spa-graft"
confidence = "high"
intent = """
Le impostazioni del corso volute dal cliente vivono in due componenti os_lms montati dentro
il CourseForm upstream: OsCourseDetailForm (hero con immagine o video, feature sections) nella
colonna principale e OsCourseSettings (regole di apprendimento: blocco delle lezioni in
sequenza, blocco del quiz al completamento) nella barra laterale. Se un merge riscrive
CourseForm e li perde, i campi restano nel database ma spariscono dal modulo: nessuno potrà
più modificarli. L'upstream rifattorizza periodicamente questa pagina (memoria
courseform-sectioned-custom-grafts): in caso di riscrittura, rimontare i due componenti nella
nuova struttura, non tornare a un modulo monolitico."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseForm.vue"
  anchor = "<OsCourseDetailForm />"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseForm.vue"
  anchor = "<OsCourseSettings />"

[[entries]]
id = "course-outline-editor-hash"
title = "Modifica lezione dal programma del corso apre la scheda giusta"
layer = "spa-graft"
confidence = "high"
intent = """
Il pulsante di modifica di una lezione nel programma del corso porta al corso con l'hash
'#editor', che è quello della scheda Editor; l'upstream usava '#course editor', che non
corrisponde a nessuna scheda, e l'editor della lezione si apriva sulla scheda sbagliata.
Se l'upstream corregge da sé l'hash in modo equivalente, accettare la sua versione e marcare
la voce come accepted-drift."""

  [[entries.sites]]
  file = "frontend/src/components/CourseOutline.vue"
  anchor = "hash: '#editor',"
  symbol = "navigateToLesson"

[[entries]]
id = "profile-own-without-lms-role"
title = "Il proprio profilo è visibile anche senza ruolo LMS"
layer = "spa-graft"
confidence = "high"
intent = """
Un utente vede sempre il proprio profilo, anche senza un ruolo LMS (per esempio un account
appena creato a cui non è ancora stato assegnato LMS Student); per vedere il profilo di altri
serve ancora un ruolo LMS. Un username inesistente restituisce "non trovato" invece di un
errore generico. Senza questa regola, un nuovo utente che apre il proprio profilo riceve un
errore di permesso."""

  [[entries.sites]]
  file = "lms/lms/api.py"
  anchor = "if details.name != frappe.session.user and not has_lms_role():"
  symbol = "get_profile_details"

[[entries]]
id = "lesson-pdf-inline-filename"
title = "PDF delle lezioni con il nome giusto su mobile"
layer = "spa-graft"
confidence = "high"
intent = """
serve_resource dà un nome esplicito ai file privati serviti inline (in pratica i PDF delle
lezioni) con un Content-Disposition inline; senza, il browser usa l'ultimo pezzo dell'URL, cioè
il nome del metodo, e Chrome su mobile mostra quello al posto del nome del PDF. Dal merge di
v2.58.1 (2026-09-23, confermato dal committente) il file si serve con _serve_private_file
dell'upstream, che passa filename= solo alle versioni di Frappe che lo accettano; la nostra
aggiunta resta dopo, protetta da «Content-Disposition not in response.headers», così non
sovrascrive l'intestazione se Frappe l'ha già messa."""

  [[entries.sites]]
  file = "lms/lms/doctype/course_lesson/course_lesson.py"
  anchor = "inline; filename*=UTF-8''"
  symbol = "serve_resource"

[[entries]]
id = "quiz-submit-unanswered"
title = "Un quiz si può consegnare con domande senza risposta"
layer = "spa-graft"
confidence = "high"
intent = """
Una domanda saltata arriva con risposta vuota o nulla; prima faceva fallire l'intera consegna
e lo studente non riusciva più a consegnare il quiz. Ora le risposte vuote vengono filtrate e
la domanda vale 0, anche con la penalità per risposte sbagliate attiva. Dal merge di v2.58.1
(2026-09-23) convive con la correzione upstream 567da4f1: l'upstream valida l'input e normalizza
le risposte nulle a "", poi il nostro filtro tratta come saltata ogni domanda senza risposte non
vuote. Senza il nostro ramo l'upstream penalizzerebbe la domanda saltata come una risposta
sbagliata. Decisione del committente: la scelta fra 0 e penalità deve diventare
un'impostazione del quiz, da costruire dopo il merge di v2.63.0 sul nuovo authoring dei quiz;
fino ad allora vale 0. Il test test_lms_quiz.py contiene i casi aggiunti con la correzione."""

  [[entries.sites]]
  file = "lms/lms/doctype/lms_quiz/lms_quiz.py"
  anchor = "if answer not in (None, \"\")"
  symbol = "process_results"

  [entries.checks]
  backend = "lms/lms/doctype/lms_quiz/test_lms_quiz.py"


# ---------------------------------------------------------------------------
# Censimento 2026-09-23 prima del merge di v2.59.0 — infrastruttura e controlli
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 — gruppo infra-controls (package.json, vite.config.js,
# controlli e componenti trasversali) prima del merge di v2.59.0
# ---------------------------------------------------------------------------

# --- Build e dipendenze (vite.config.js, package.json) ---------------------

[[entries]]
id = "vite-override-theme-plugin"
title = "Plugin osOverrideTheme per sostituire componenti senza fork"
layer = "spa-graft"
confidence = "high"
intent = """
Tutto il sistema di override (frontend/src/overrides/) dipende da questo plugin Vite: intercetta
gli import .vue che risolvono dentro node_modules (frappe-ui) o dentro src/ e, se esiste un file
allo stesso percorso sotto src/overrides/, usa quello. Serve a restilizzare e correggere
componenti di frappe-ui e pagine upstream senza modificarli, riducendo i conflitti di merge.
La guardia sull'importer (un import fatto DA un file di override non viene mai reindirizzato)
impedisce la ricorsione infinita degli override «wrap» che reimportano l'originale (memoria
frappe_ui_override_wrap_recursion). Se il plugin sparisce da plugins[], ogni override smette di
applicarsi in silenzio: la build passa ma l'interfaccia torna quella upstream. Richiede che
frappe-ui resti in optimizeDeps.exclude in sviluppo (riga upstream), altrimenti il plugin non
vede gli import relativi interni. ATTENZIONE al merge di v2.59.0: frappe-ui passa a
1.0.0-beta.24; ogni override sotto src/overrides/frappe-ui va ridiffato contro i nuovi
originali (percorsi spostati = override ignorati senza errori)."""

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "osOverrideTheme(),"

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "if (importerPath.startsWith(overridesDir)) return null"
  symbol = "osOverrideTheme"

[[entries]]
id = "vite-original-component-aliases"
title = "Alias verso i componenti frappe-ui originali per gli override «wrap»"
layer = "spa-graft"
confidence = "high"
intent = """
Gli override di Switch, FileUploader e TextEditor avvolgono il componente originale invece di
copiarlo; per importare l'originale senza ricadere sull'override stesso usano gli alias
frappe-ui-*-original, che puntano direttamente al file in node_modules scavalcando plugin e
«exports» del pacchetto. Per ospitarli resolve.alias è passato dalla forma oggetto upstream
alla forma array: al merge, se l'upstream aggiunge alias nella forma oggetto, vanno riportati
nell'array, non il contrario. ATTENZIONE al merge di v2.59.0 (frappe-ui beta.24): verificare
che i tre percorsi node_modules/frappe-ui/src/components/{Switch,FileUploader,TextEditor}
esistano ancora, altrimenti la build fallisce su un alias che non risolve."""

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "find: 'frappe-ui-switch-original',"

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "find: 'frappe-ui-fileuploader-original',"

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "find: 'frappe-ui-texteditor-original',"

[[entries]]
id = "vite-utils-alias-oslms"
title = "@/utils risolto sul modulo utils di os_lms"
layer = "spa-graft"
confidence = "high"
intent = """
Ogni import di '@/utils' nell'app finisce su src/oslms/utils/index.js, che riesporta tutto
src/utils/index.js e sostituisce timeAgo con una versione che passa i testi relativi («2 ore
fa», «ieri»…) per __(), così le date relative escono in italiano senza toccare il file utils
upstream. L'alias deve restare prima di quello generico '@' nell'array, altrimenti non scatta.
Se si perde, le date relative tornano in inglese in tutta l'app."""

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "replacement: path.resolve(__dirname, 'src/oslms/utils/index.js'),"

[[entries]]
id = "vite-socketio-port-define"
title = "Porta socket.io letta da common_site_config in fase di build"
layer = "spa-graft"
confidence = "high"
intent = """
Nell'ambiente Docker di sviluppo (e in bench con porte non standard) la porta di socket.io non
è quella predefinita: vite.config la legge da common_site_config.json (con ripiego sul file
nella cartella superiore per la build locale) e la inietta come costante __SOCKETIO_PORT__,
usata da main.js (opzioni socketio di FrappeUI) e da socket.js. Se la define sparisce, la build
fallisce su __SOCKETIO_PORT__ non definito, oppure le notifiche in tempo reale non si
connettono."""

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "__SOCKETIO_PORT__: configSite.socketio_port"

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "async function importConfigSite(isDev) {"

[[entries]]
id = "vite-toc-fallback-translation"
title = "Traduzione del messaggio dell'indice senza titoli nel rendering salvato"
layer = "spa-graft"
confidence = "high"
intent = """
Il blocco «indice» del TextEditor, quando il documento non ha titoli, scrive nel contenuto
salvato un messaggio inglese cablato in un file .ts di frappe-ui, fuori dalla portata di
osOverrideTheme (che gestisce solo .vue). Un piccolo plugin Vite sostituisce quella stringa con
l'italiano, in coppia con l'override TocNodeView.vue (voce toc-node-translations) che copre la
vista interattiva. La sostituzione è letterale: se frappe-ui cambia il testo o sposta il file,
non fallisce ma smette di tradurre. ATTENZIONE al merge di v2.59.0: con frappe-ui beta.24
verificare che toc-node-extension.ts contenga ancora esattamente la stringa cercata (in beta.7
esiste anche una copia in src/molecules/editor/extensions/toc-node/toc-render.ts, non coperta)."""

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "osTranslateTocFallback(),"

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "'No headings found in this document.'"
  symbol = "osTranslateTocFallback"

[[entries]]
id = "deps-tiptap-direct-deps"
title = "@tiptap dichiarati come dipendenze dirette e deduplicati"
layer = "spa-graft"
confidence = "high"
intent = """
Gli override del TextEditor (MediaNodeView, InsertIframe, TocNodeView, IframeNodeView,
SlashCommandsList) importano @tiptap/vue-3, @tiptap/core e @tiptap/pm con specificatori nudi.
Sono solo dipendenze transitive di frappe-ui: in un'installazione pulita (Docker, bench build)
restano annidate sotto frappe-ui e Rollup non le risolve («failed to resolve import
@tiptap/vue-3»). Dichiararle dirette le rende sempre risolvibili; aggiungerle a resolve.dedupe
garantisce un'unica copia condivisa con frappe-ui (copie doppie rompono i controlli instanceof
di tiptap). ATTENZIONE al merge di v2.59.0: con frappe-ui beta.24 il lock upstream porta tiptap
3.26.1, compatibile con ^3.26.0; se frappe-ui richiedesse una major diversa, allineare la
versione qui."""

  [[entries.sites]]
  file = "frontend/package.json"
  anchor = '"@tiptap/core":'

  [[entries.sites]]
  file = "frontend/package.json"
  anchor = '"@tiptap/pm":'

  [[entries.sites]]
  file = "frontend/package.json"
  anchor = '"@tiptap/vue-3":'

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "'@tiptap/pm',"

[[entries]]
id = "deps-prosemirror-resolutions"
title = "Versione unica dei pacchetti prosemirror via resolutions"
layer = "spa-graft"
confidence = "high"
intent = """
Dichiarare @tiptap come dipendenze dirette aveva installato una seconda copia di
prosemirror-model e prosemirror-view annidata sotto @tiptap/pm. Due istanze generano chiavi di
plugin in collisione e ogni TextEditor va in crash con «Adding different instances of a keyed
plugin (plugin$)» (le Discussioni restavano vuote). Le resolutions fissano una sola versione di
model, view, state e transform per ogni installazione pulita, Docker compreso. Il blocco non
esiste upstream: se il merge prende il package.json dell'upstream va riaggiunto. ATTENZIONE al
merge di v2.59.0: dopo il salto a frappe-ui beta.24 rifare yarn install e verificare che
node_modules contenga una sola versione di ciascun pacchetto (il lock upstream ne ha due di
model e view); se una dipendenza nuova chiede una versione superiore, alzare i pin invece di
toglierli."""

  [[entries.sites]]
  file = "frontend/package.json"
  anchor = '"resolutions": {'

  [[entries.sites]]
  file = "frontend/package.json"
  anchor = '"prosemirror-model":'

[[entries]]
id = "vite-prosemirror-optimizedeps"
title = "Pre-bundle di tutti gli entrypoint tiptap/prosemirror in sviluppo"
layer = "spa-graft"
confidence = "high"
intent = """
In sviluppo frappe-ui è escluso da optimizeDeps (serve a osOverrideTheme), quindi lo scanner di
Vite non vede i sottopercorsi @tiptap/pm/* né i prosemirror-* nudi importati solo attraverso
frappe-ui: vengono serviti grezzi da node_modules accanto alla copia ottimizzata, con due
prosemirror-state e il crash «keyed plugin (plugin$)» su ogni pagina con TextEditor. Elencare
l'insieme completo li mette in un unico passaggio di ottimizzazione che condivide una sola
copia. resolve.dedupe non basta: governa la risoluzione, non la divisione ottimizzato/grezzo.
Verifica senza browser (memoria prosemirror_plugin_dup_optimizedeps): dopo vite optimize
--force il messaggio «Adding different instances of a keyed plugin» deve comparire in un solo
chunk di node_modules/.vite/deps. ATTENZIONE al merge di v2.59.0: l'upstream toglie 'interactjs'
dalla stessa lista include, subito sopra al nostro blocco (conflitto di contesto, accettare la
rimozione); dopo il salto a beta.24 ricontrollare se @tiptap/pm esporta sottopercorsi nuovi e
aggiungerli."""

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "'@tiptap/pm/changeset',"

  [[entries.sites]]
  file = "frontend/vite.config.js"
  anchor = "'prosemirror-changeset',"

[[entries]]
id = "deps-editorjs-color-picker"
title = "Dipendenza editorjs-color-picker per il colore del testo nelle lezioni"
layer = "spa-graft"
confidence = "high"
intent = """
Lo strumento di colore del testo nell'editor delle lezioni (ColorPickerInline in utils) usa
editorjs-color-picker, dipendenza solo nostra: il package.json upstream non la conosce e un
merge che prende la versione upstream la toglie, rompendo la build o lo strumento colore
(memoria frappeui_1_0_beta_merge_2026_06). ATTENZIONE al merge di v2.59.0: la riga sta tra
dompurify e editorjs-drag-drop, a ridosso di codemirror e feather-icons che l'upstream rimuove;
nel conflitto conservare questa riga."""

  [[entries.sites]]
  file = "frontend/package.json"
  anchor = '"editorjs-color-picker":'

# --- Controllo Link (Controls/Link.vue) -------------------------------------

[[entries]]
id = "link-control-stale-combobox-events"
title = "Controllo Link agganciato all'API del Combobox sovrascritto"
layer = "spa-graft"
confidence = "high"
intent = """
Il Combobox realmente usato in tutta l'app è l'override in src/overrides/frappe-ui/.../Combobox,
copia di una versione più vecchia: emette solo update:modelValue, update:selectedOption, focus,
blur e input, e non gestisce size, loading né update:query/update:open. Link.vue deve quindi
usare @input per la ricerca, @focus (onFocus) per caricare la lista la prima volta e
:open-on-click per aprire il menu; con gli eventi nuovi dell'upstream ogni campo Link si apre
vuoto e la ricerca non restituisce nulla (è già successo dopo un merge, memoria
combobox_override_event_api). open-on-click invece di open-on-focus evita che il menu si
riapra quando il Combobox rimette il fuoco sull'input dopo una scelta. Questi binding vanno
riportati agli eventi nuovi solo quando l'override del Combobox verrà riallineato a frappe-ui.
Il wrapper os-link (src/oslms/components/Controls/Link.vue) passa tutto a questo componente.
ATTENZIONE al merge di v2.59.0: l'upstream aggiunge :open="isOpen", imposta isOpen dentro
onOpen (che da noi non esiste, sostituito da onFocus: conflitto sicuro) e chiude il menu in
handleCreate prima di aprire il modale di creazione. Con l'override attuale la prop open non
viene letta, quindi quella chiusura non avrà effetto: tenere @input/@focus, e valutare a parte
se servono chiusura del menu e prop open nell'override."""

  [[entries.sites]]
  file = "frontend/src/components/Controls/Link.vue"
  anchor = '@input="onQuery"'

  [[entries.sites]]
  file = "frontend/src/components/Controls/Link.vue"
  anchor = '@focus="onFocus"'

  [[entries.sites]]
  file = "frontend/src/components/Controls/Link.vue"
  anchor = ':open-on-click="true"'

  [[entries.sites]]
  file = "frontend/src/components/Controls/Link.vue"
  anchor = "function onFocus(): void {"
  symbol = "onFocus"

# --- Palette Cmd+K (CommandPalette.vue) -------------------------------------

[[entries]]
id = "command-palette-result-routes"
title = "Risultati della palette Cmd+K aperti sulla pagina giusta secondo il ruolo"
layer = "spa-graft"
confidence = "high"
intent = """
L'upstream instradava ogni risultato che non fosse un corso su BatchDetail, così lezioni, quiz,
esercitazioni, percorsi finivano su una pagina classe inesistente. La mappatura doctype → rotta
vive ora in un solo modulo condiviso con la pagina /search (src/oslms/utils/searchRoutes.ts):
una lezione apre la lezione stessa, un quiz si apre nella lezione che lo ospita per lo studente
e nell'editor per moderatore o docente (isManager: stesso controllo delle pagine di
destinazione). Un risultato senza pagina da aprire restituisce una rotta nulla e navigateTo
non fa nulla invece di andare in errore. Lato server l'override os_lms di search_sqlite filtra
i risultati per permessi e aggiunge la posizione della lezione."""

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "item.route = getSearchResultRoute(item, isManager.value)"
  symbol = "generateSearchResults"

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "Boolean(user?.data?.is_moderator || user?.data?.is_instructor)"
  symbol = "isManager"

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "if (!route) return"
  symbol = "navigateTo"

[[entries]]
id = "command-palette-search-page-entry"
title = "Voce «Cerca …» della palette verso la pagina /search"
layer = "spa-graft"
confidence = "low"
intent = """
Mentre si digita, la palette mostra come prima voce «Search for "<testo>"», che apre la pagina
/search con la query. L'upstream ha tolto questa voce e la pagina Search nel commit 46a356b08
(già in v2.58.1); da noi la pagina è stata ripristinata perché una rotta custom la usa (rapporto
di merge 2026-06) ed è uno dei due punti di ricerca allineati da searchRoutes.ts. Il codice
della voce è quello upstream precedente alla rimozione, conservato nei merge. Motivazione
dedotta: senza questa voce la pagina /search non è raggiungibile dalla palette. Il titolo del
gruppo resta il letterale 'Jump to' perché CommandPaletteGroup lo traduce già in fase di
rendering."""

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "name: 'Search',"
  symbol = "appendSearchPage"

[[entries]]
id = "command-palette-jobs-removed"
title = "Voce «Jobs» rimossa dalla palette Cmd+K"
layer = "spa-graft"
confidence = "low"
intent = """
L'elenco «Jump to» della palette mostra solo Corsi e Classi: la voce Jobs dell'upstream (e la
relativa icona Briefcase) è stata tolta (commit 7142c05ba). Motivazione non scritta nel commit;
presumibilmente la sezione offerte di lavoro non fa parte del prodotto usato dal cliente. Se un
merge reintroduce la voce Jobs nell'elenco, toglierla di nuovo."""

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "OSLMS-CUSTOM: Jobs entry removed from the Jump to list"

[[entries]]
id = "command-palette-i18n-labels"
title = "Etichette della palette Cmd+K tradotte anche dopo il ricaricamento"
layer = "spa-graft"
confidence = "high"
intent = """
Il segnaposto del campo di ricerca passa per __() e i titoli Corsi/Classi dell'elenco «Jump to»
sono getter invece di stringhe: l'elenco è costruito una volta al setup, che al ricaricamento
completo della pagina avviene prima che le traduzioni asincrone siano caricate, e un __()
valutato lì resterebbe in inglese (memoria translation_baked_ref_freezes_english). Il getter
ripete __() a ogni rendering."""

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = ":placeholder=\"__('Search')\""

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "return __('Courses')"

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "return __('Batches')"

[[entries]]
id = "command-palette-mark-theme-tokens"
title = "Evidenziazione dei risultati di ricerca sui token del tema"
layer = "spa-graft"
confidence = "high"
intent = """
L'evidenziazione <mark> delle corrispondenze usa i token del tema (bg-surface-amber-1,
text-ink-gray-9) invece del colore fisso amber-100 dell'upstream: così si adatta al tema scuro
e il testo resta leggibile (il <mark> predefinito rende il testo nero). Lo stile è globale (non
scoped), come nell'upstream. ATTENZIONE al merge di v2.59.0 (frappe-ui beta.24):
verificare che le classi bg-surface-amber-1 e text-ink-gray-9 esistano ancora, altrimenti la
build fallisce su @apply."""

  [[entries.sites]]
  file = "frontend/src/components/CommandPalette/CommandPalette.vue"
  anchor = "@apply bg-surface-amber-1 text-ink-gray-9;"

# --- Stato vuoto (EmptyStateLayout.vue) -------------------------------------

[[entries]]
id = "empty-state-translated-name"
title = "Nome dell'entità tradotto nei messaggi di stato vuoto"
layer = "spa-graft"
confidence = "high"
intent = """
Titolo e descrizione predefiniti dello stato vuoto («No {0} Found», «There are no {0}
currently…») ricevono il nome dell'entità passato dalla pagina in inglese (es. «Courses»); lo
traduciamo con __() prima di inserirlo, altrimenti la frase italiana contiene la parola inglese
(«Nessun Courses trovato»)."""

  [[entries.sites]]
  file = "frontend/src/components/Layouts/EmptyStateLayout.vue"
  anchor = "__('No {0} Found').format(__(props.name))"
  symbol = "computedTitle"

  [[entries.sites]]
  file = "frontend/src/components/Layouts/EmptyStateLayout.vue"
  anchor = ".format(__(props.name)?.toLowerCase())"
  symbol = "computedDescription"

[[entries]]
id = "empty-state-mobile-full-width"
title = "Stato vuoto a tutta larghezza su telefono"
layer = "spa-graft"
confidence = "high"
intent = """
Le larghezze fisse dell'upstream (2/12, 4/12, 8/12 dello schermo) su telefono comprimono
icona e testo dello stato vuoto in una colonna strettissima. Sotto il breakpoint sm il blocco
occupa tutta la larghezza; da sm in su valgono le frazioni originali. ATTENZIONE al merge di
v2.59.0: l'upstream cambia le classi del contenitore radice (min-h-64, grow) e del titolo
(text-lg-medium), ma non widthClass: accettare le sue e mantenere questi valori."""

  [[entries.sites]]
  file = "frontend/src/components/Layouts/EmptyStateLayout.vue"
  anchor = "return 'w-full sm:w-2/12'"
  symbol = "widthClass"

  [[entries.sites]]
  file = "frontend/src/components/Layouts/EmptyStateLayout.vue"
  anchor = "return 'w-full sm:w-8/12'"
  symbol = "widthClass"

  [[entries.sites]]
  file = "frontend/src/components/Layouts/EmptyStateLayout.vue"
  anchor = "return 'w-full sm:w-4/12'"
  symbol = "widthClass"

# --- Pannello notifiche (NotificationPanel.vue) -----------------------------

[[entries]]
id = "notifications-tabs-translated"
title = "Schede Da leggere/Lette del pannello notifiche tradotte"
layer = "spa-graft"
confidence = "high"
intent = """
Le etichette delle schede sono getter che richiamano __() a ogni rendering, mentre value tiene
la chiave logica 'Unread'/'Read' usata da filtro e stato vuoto. Un array costruito al setup
valuta __() prima che le traduzioni asincrone siano caricate e, al ricaricamento completo della
pagina, le schede resterebbero in inglese (TabButtons rende l'etichetta così com'è). ATTENZIONE
al merge di v2.59.0: con frappe-ui beta.24 l'upstream passa da :buttons a :options su
TabButtons; accettare il cambio e verificare che il nuovo TabButtons usi ancora value come
chiave e legga label al rendering (così il getter continua a funzionare)."""

  [[entries.sites]]
  file = "frontend/src/components/Notifications/NotificationPanel.vue"
  anchor = "return __('Unread')"

  [[entries.sites]]
  file = "frontend/src/components/Notifications/NotificationPanel.vue"
  anchor = "return __('Read')"

[[entries]]
id = "notifications-link-routing"
title = "Clic su una notifica porta alla pagina giusta o avvisa"
layer = "spa-graft"
confidence = "high"
intent = """
I link delle notifiche possono essere assoluti o relativi, con o senza prefisso /lms e con un
frammento di scheda (#discussions); il parsing a indici fissi dell'upstream sbagliava parametri
o finiva sulla pagina sbagliata. navigateToPage normalizza il percorso, porta il frammento
nell'hash della rotta, apre direttamente la lezione per i link courses/<corso>/learn/<c>-<l>
(dove puntano gli avvisi di punteggio dei quiz, il cui link è compilato dall'hook os_lms
before_insert su Notification Log), apre QuizPage per i quiz autonomi, instrada ogni classe su
BatchDetail e le consegne su AssignmentSubmission. Un link sconosciuto o non più valido
(documento cancellato, rotta rimossa) mostra un avviso invece di lanciare un errore del router
non gestito. ATTENZIONE al merge di v2.59.0: l'upstream riscrive il ramo «batches» della sua
navigateToPage (sempre BatchDetail): conflitto sicuro, tenere la nostra funzione, che fa già
lo stesso."""

  [[entries.sites]]
  file = "frontend/src/components/Notifications/NotificationPanel.vue"
  anchor = "const notifyUnavailable = () =>"

  [[entries.sites]]
  file = "frontend/src/components/Notifications/NotificationPanel.vue"
  anchor = "segments[2] === 'learn' && segments[3]"
  symbol = "navigateToPage"

  [[entries.sites]]
  file = "frontend/src/components/Notifications/NotificationPanel.vue"
  anchor = "target = { name: 'QuizPage', params: { quizID: segments[1] } }"
  symbol = "navigateToPage"

  [[entries.sites]]
  file = "frontend/src/components/Notifications/NotificationPanel.vue"
  anchor = "router.push(target).catch(() => notifyUnavailable())"
  symbol = "navigateToPage"

# --- Modale evento di valutazione (Modals/Event.vue) ------------------------

[[entries]]
id = "event-modal-status-placeholder"
title = "Segnaposto tradotto sullo stato della valutazione"
layer = "spa-graft"
confidence = "low"
intent = """
La select Stato della valutazione nel modale dell'evento ha un segnaposto «Select option»
passato per __(). Introdotto in un commit generico di traduzioni (e577585ca): motivazione
dedotta, mostrare un invito in italiano quando lo stato non è ancora scelto invece di una
select vuota o di un testo inglese. ATTENZIONE al merge di v2.59.0: l'upstream passa il Dialog
a bare con slot #default e cambia la classe del titolo, fuori da questa riga."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/Event.vue"
  anchor = ":placeholder=\"__('Select option')\""

[[entries]]
id = "event-modal-certificate-chrome-pdf"
title = "Certificato dal modale evento scaricato con il generatore PDF Chrome"
layer = "spa-graft"
confidence = "high"
intent = """
download_pdf usa wkhtmltopdf se non riceve pdf_generator, ignorando sia il campo del Print
Format sia l'impostazione globale di stampa. Il modello di certificato custom usa
transform:rotate, che solo il generatore Chrome rende: con il predefinito il PDF esce vuoto
(memoria certificate_print_format_setup). Ogni URL di download del certificato nella SPA
aggiunge quindi &pdf_generator=chrome; la stessa regola vive anche in CertificationLinks.vue,
CourseCardOverlay.vue, CourseCertification.vue e ProfileCertificates.vue (commit 28976b969)."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/Event.vue"
  anchor = "&pdf_generator=chrome"
  symbol = "openCertificate"

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 prima del merge di v2.59.0 — impostazioni, profilo e ruoli
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 — gruppo "settings-roles" (Impostazioni, profilo, ruoli,
# barra laterale, permessi lezioni) prima del merge di v2.59.0
# ---------------------------------------------------------------------------

# ── Settings.vue ───────────────────────────────────────────────────────────

[[entries]]
id = "settings-tabs-stable-keys"
title = "Le schede delle impostazioni si identificano con una chiave stabile"
layer = "spa-graft"
confidence = "high"
intent = """
Tutte le etichette del dialogo Impostazioni (gruppi, schede, sezioni, campi, descrizioni) sono
tradotte con __() nel punto in cui vengono definite, per avere l'interfaccia in italiano. Una
etichetta tradotta non può più identificare la scheda: per questo ogni gruppo e ogni scheda ha
una `key` inglese e stabile, e su quella si basano la scheda attiva, la chiave del pannello, i
link diretti (openSettings('Members'), openSettings('General')…) e la scelta delle prop da
passare ai componenti (Members, Transactions, AI, TrueSkills API). Il titolo del gruppo si
stampa senza __() perché è già tradotto. Attenzione: 'Course Progress' e le due schede del
gruppo Email (Accounts, Templates) non hanno ancora una `key` (arrivate dall'upstream dopo il
nostro innesto). ATTENZIONE al merge di v2.59.0: l'upstream riscrive tutto il template con
SettingsDialog/SettingsNavItem, fa diventare activeTab una stringa uguale a item.label e usa
item.label in panelProps (['Users', 'Transactions']); rinomina inoltre il gruppo 'Users' in
'User Management' e la scheda 'Members' in 'Users'. Nella nuova struttura bisogna usare
item.key come :value e come chiave, e tenere i link diretti allineati alle chiavi."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = ':activeTab="activeTab?.key"'

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = ".find((item) => item.key === settingsStore.activeTab)"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "<span>{{ tab.label }}</span>"

[[entries]]
id = "settings-admin-only-tabs"
title = "Schede di sistema delle impostazioni solo per gli amministratori"
layer = "spa-graft"
confidence = "high"
intent = """
Le schede che cambiano il comportamento dell'intera piattaforma (Generale, Badge, Registrazione,
SEO) sono riservate agli amministratori (System Manager o utente Administrator). Il dialogo
Impostazioni è aperto anche a Moderator e quindi a Gestore (che ha sempre anche Moderator):
senza questa regola un gestore potrebbe cambiare configurazioni di sistema come l'accesso ospite,
la registrazione o i metadati SEO. Le stesse funzioni isAdministrator e canManageOsIntegrations
vengono passate a buildOslmsSettingsTabs per le schede os_lms. ATTENZIONE al merge di v2.59.0:
l'upstream cambia le icone delle stesse schede ('lucide-wrench'…) e riscrive il template; le
`condition: isAdministrator` sulle quattro schede vanno conservate."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "const ADMIN_ONLY_ROLES = ['System Manager', 'Administrator']"
  symbol = "isAdministrator"

  [entries.visibility]
  subject = "Schede Generale, Badge, Registrazione e SEO del dialogo Impostazioni"
  allow = ["System Manager", "Administrator"]
  deny = ["Moderator", "Gestore", "Docente", "Course Creator"]

[[entries]]
id = "settings-payment-group-removed"
title = "Gruppo Pagamenti tolto dalle impostazioni"
layer = "spa-graft"
confidence = "high"
intent = """
Il gruppo upstream "Payment" (Configurazione valuta/gateway, Gateway, Transazioni, Coupon) è
stato tolto volutamente dal dialogo Impostazioni (commit 39907513c "Hide payment section in
settings", confermato nel merge di v2.58.0: "il nostro Settings.vue non ha di proposito il
gruppo Payments"). Ogni merge che tocca quel blocco tende a reintrodurlo; va riscartato insieme
agli import di PaymentGateways, Coupons e Transactions. ATTENZIONE al merge di v2.59.0: l'upstream
modifica le icone di tutte e quattro le voci del gruppo, quindi il blocco comparirà nel conflitto
e non va ripreso."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = '// OSLMS-CUSTOM: upstream "Payment" group (Configuration, Gateways, Transactions, Coupons) intentionally removed from the settings dialog'

[[entries]]
id = "settings-google-calendar-tab"
title = "Scheda Google Calendar nelle impostazioni e calendario legato agli account Zoom"
layer = "spa-graft"
confidence = "high"
intent = """
Nel gruppo Conferenze c'è una scheda Google Calendar per gestire i calendari Google usati per
gli inviti delle classi live e delle valutazioni; è visibile solo a System Manager e Gestore
(uno dei pochi controlli che nomina esplicitamente il ruolo Gestore). Ogni account Zoom può
essere legato a uno di questi calendari (campo google_calendar su LMS Zoom Settings), così gli
inviti delle classi live partono dal calendario giusto; per questo la lista degli account Zoom
carica anche quel campo. ATTENZIONE al merge di v2.59.0: l'upstream riscrive ZoomSettings.vue
(ListView → List/ListRows) e l'elenco `fields` della risorsa va ricontrollato."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "const GOOGLE_CALENDAR_ROLES = ['System Manager', 'Gestore']"
  symbol = "canManageGoogleCalendars"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "template: markRaw(GoogleCalendarSettings),"

  [[entries.sites]]
  file = "frontend/src/components/Settings/ZoomSettings.vue"
  anchor = "'google_calendar',"
  symbol = "zoomAccounts"

  [entries.visibility]
  subject = "Scheda Google Calendar del dialogo Impostazioni"
  allow = ["System Manager", "Gestore"]
  deny = ["Moderator senza Gestore", "Docente", "Course Creator"]

[[entries]]
id = "settings-oslms-tabs-injection"
title = "Schede os_lms iniettate nel dialogo Impostazioni"
layer = "spa-graft"
confidence = "high"
intent = """
Le schede del modulo os_lms (TrueSkills API, Configurazioni con Corsi e Classi live, AI, Prompt
AI) non sono scritte in Settings.vue ma costruite da buildOslmsSettingsTabs in
@/oslms/utils/settings e aggiunte in coda con uno spread, per tenere il file vicino
all'upstream. Per farle funzionare Settings.vue deve: filtrare anche i gruppi interi con la loro
`condition` e togliere i gruppi rimasti vuoti (un gestore senza System Manager non deve vedere
un gruppo AI vuoto); passare `sections` ai componenti AI e TrueSkills API; ricevere
canManageOsIntegrations (amministratori più Gestore) per TrueSkills API e Prompt AI. Il ramo
'Branding' nello stesso controllo è un residuo upstream (BrandSettings non dichiara `sections`).
ATTENZIONE al merge di v2.59.0: con il nuovo template a pannelli le prop passano da
panelProps(item); lì vanno riportati sia `sections` per AI/TrueSkills API sia il filtro dei
gruppi."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "...buildOslmsSettingsTabs({ isAdministrator, canManageOsIntegrations }),"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "const OS_INTEGRATION_ROLES = ['Gestore']"
  symbol = "canManageOsIntegrations"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = ".filter((tab) => !tab.condition || tab.condition())"
  symbol = "tabs"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "activeTab.key == 'AI' ||"

  [entries.visibility]
  subject = "Schede os_lms TrueSkills API e Prompt AI (le schede Configurazioni e AI solo amministratori)"
  allow = ["System Manager", "Administrator", "Gestore"]
  deny = ["Moderator senza Gestore", "Docente", "Course Creator"]

[[entries]]
id = "settings-sidebar-extra-toggles"
title = "Interruttori aggiuntivi per le voci della barra laterale"
layer = "spa-graft"
confidence = "high"
intent = """
La scheda Barra laterale delle impostazioni ha cinque interruttori in più rispetto all'upstream
(Home, Programmi, Cerca, Quiz, Compiti), salvati in campi personalizzati di LMS Settings
definiti nelle fixture os_lms, così l'amministratore può nascondere anche queste voci (commit
c634f2bd1 e a4e416fe2). La barra laterale li applica con il meccanismo upstream
(updateSidebarLinksVisibility confronta il nome del campo con l'etichetta della voce); per la
voce Programmi i link vengono ricostruiti subito dopo il caricamento dei programmi, altrimenti la
voce non compare al primo caricamento."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'home',"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'programs',"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'search',"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'quizzes',"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'assignments',"

  [[entries.sites]]
  file = "frontend/src/components/Sidebar/AppSidebar.vue"
  anchor = "await programs.reload()\n\t\tupdateSidebarLinks()"

[[entries]]
id = "settings-welcome-tab"
title = "Scheda Benvenuto: notifica e video al primo accesso dello studente"
layer = "spa-graft"
confidence = "high"
intent = """
La scheda Benvenuto (gruppo Personalizza) configura la notifica di benvenuto e il video di
benvenuto mostrati allo studente al primo accesso; i due si attivano in modo indipendente
(commit 88a13f16b). Il video accetta sia un link esterno (YouTube, Vimeo) sia un file caricato
nello stesso campo, con il tipo di campo VideoSourceInput (commit 1ac7315b9). I campi sono
personalizzati su LMS Settings (fixture os_lms). ATTENZIONE: SettingFields.vue importa ancora
VideoSourceInput e FilePicker, ma il ramo `v-else-if="field.type == 'VideoSourceInput'"` del
template non c'è più (perso in un merge precedente): oggi il campo del video cade nel
FormControl generico. Da ripristinare, non da cancellare."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'welcome_notification_enabled',"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "name: 'welcome_video_file',"

  [[entries.sites]]
  file = "frontend/src/components/Settings/SettingFields.vue"
  anchor = "import VideoSourceInput from '@/oslms/components/Form/VideoSourceInput.vue'"

[[entries]]
id = "settings-dialog-style"
title = "Ritocchi di stile del dialogo Impostazioni"
layer = "spa-graft"
confidence = "high"
intent = """
Ritocchi estetici del dialogo Impostazioni: il Dialog usa ancora l'API v-model + options
(introdotta dopo un crash seguito a un merge, commit 265842cde), il pannello del contenuto ha
padding e scorrimento propri, il contenitore ha id "settings-modal". Nessuna regola funzionale:
al merge di v2.59.0 l'upstream sostituisce tutto con SettingsDialog, e questi ritocchi vanno
rivalutati sulla nuova struttura, non riapplicati alla cieca."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Settings.vue"
  anchor = "<Dialog v-model=\"show\" :options=\"{ size: '5xl' }\">"

# ── SettingFields.vue ──────────────────────────────────────────────────────

[[entries]]
id = "settings-fields-translations"
title = "Ritocchi di traduzione nei campi delle impostazioni"
layer = "spa-graft"
confidence = "high"
intent = """
Testi dei campi delle impostazioni tradotti con __(): pulsante di caricamento immagine e relativa
percentuale, descrizione del campo HTML di registrazione, segnaposto delle select. Solo
traduzione, nessuna regola funzionale."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/SettingFields.vue"
  anchor = "__('Upload an image')"

# ── Members.vue ────────────────────────────────────────────────────────────

[[entries]]
id = "settings-members-row-opens-edit"
title = "Clic sulla riga di un membro apre la modifica"
layer = "spa-graft"
confidence = "high"
intent = """
Nella scheda Membri il clic su una riga apre la finestra di modifica del membro (nome, ruoli,
compreso Valutatore), non solo la voce "Edit member" del menu; così il gestore modifica un
utente direttamente dall'elenco (commit 33b333d9f). In origine apriva EditMemberModal (con il
codice fiscale); oggi openEditMember apre NewMemberModal in modalità modifica, e le variabili
showEditMember, selectedMember, onNavigateProfile e onMemberUpdated sono residui non più usati.
ATTENZIONE al merge di v2.59.0: l'upstream riscrive l'elenco con List/ListRow, con
ListRow @click = openProfile e la cella azioni @click.stop; bisogna decidere quale clic apre
cosa sulla nuova struttura."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Members.vue"
  anchor = '@click="openEditMember(member)"'

[[entries]]
id = "settings-members-valutatore-badge"
title = "Badge del ruolo Valutatore nell'elenco membri"
layer = "spa-graft"
confidence = "high"
intent = """
L'elenco dei membri mostra un badge anche per il ruolo personalizzato Valutatore (valutatore di
una singola classe), che l'override get_members ora restituisce (commit 1af13d630); un ruolo
sconosciuto mostra il proprio nome invece di un badge vuoto, e le etichette dei ruoli sono
tradotte. ATTENZIONE al merge di v2.59.0: l'upstream riscrive il markup del badge
(`<span class="text-sm leading-5">{{ getRole(role) }}</span>`): va rimesso __()."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Members.vue"
  anchor = "Valutatore: 'Valutatore',"
  symbol = "getRole"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Members.vue"
  anchor = "return map[role] || role"
  symbol = "getRole"

  [[entries.sites]]
  file = "frontend/src/components/Settings/Members.vue"
  anchor = "{{ __(getRole(role)) }}"

# ── File con soli ritocchi ─────────────────────────────────────────────────

[[entries]]
id = "settings-email-template-list-translation"
title = "Titolo tradotto nell'elenco dei modelli email"
layer = "spa-graft"
confidence = "high"
intent = """
Il titolo della pagina Modelli email passa da __() per comparire in italiano. Solo traduzione.
ATTENZIONE al merge di v2.59.0: l'upstream riscrive il corpo del file (ricerca, List/ListRows)
ma non la riga del titolo; controllare che __(label) resti."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/EmailTemplate/EmailTemplateList.vue"
  anchor = ':title="__(label)"'

[[entries]]
id = "settings-transaction-details-cosmetic"
title = "Ritocchi nel dettaglio transazione"
layer = "spa-graft"
confidence = "high"
intent = """
Segnaposto tradotto nella select "Payment For Document Type" e una virgola finale di
formattazione. C'è anche un import di Switch (BooleanSwitch) non usato, rimasto da una
correzione di percorso. Il gruppo Pagamenti è comunque nascosto (settings-payment-group-removed).
Nessuna regola funzionale: al merge si può prendere la versione upstream e rimettere il solo
segnaposto."""

  [[entries.sites]]
  file = "frontend/src/components/Settings/Transactions/TransactionDetails.vue"
  anchor = ":placeholder=\"__('Select option')\""

[[entries]]
id = "settings-email-template-modal-formatting"
title = "Solo formattazione nella finestra dei modelli email"
layer = "spa-graft"
confidence = "high"
intent = """
Il file differisce dall'upstream solo per virgole finali aggiunte da Prettier; nessun
comportamento cambiato, per questo non porta marcatori. Al merge (v2.59.0 passa a
RichTextEditor e toglie :rows) prendere la versione upstream senza esitazioni."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/EmailTemplateModal.vue"
  anchor = "cleanError(err.messages[0]) || __('Error creating email template'),"

# ── Profilo ────────────────────────────────────────────────────────────────

[[entries]]
id = "profile-roles-custom-role-toggles"
title = "Interruttori dei ruoli Valutatore, Gestore e Docente nel profilo"
layer = "spa-graft"
confidence = "high"
intent = """
La scheda Ruoli del profilo (visibile ai moderatori) permette di assegnare anche i tre ruoli
personalizzati: Valutatore (valutatore per classe; l'interruttore assegna il ruolo globale,
che il sync della classe non revoca più), "Manager" = Gestore (il pacchetto completo di gestione)
e "Teacher" = Docente (istruttore globale su tutti i corsi e le classi). ROLE_NAME_MAP traduce la
chiave dell'interruttore nel nome reale del ruolo; gli override os_lms di get_roles e save_role
leggono e scrivono questi ruoli."""

  [[entries.sites]]
  file = "frontend/src/pages/ProfileRoles.vue"
  anchor = "@update:modelValue=\"saveRole('valutatore')\""

  [[entries.sites]]
  file = "frontend/src/pages/ProfileRoles.vue"
  anchor = "@update:modelValue=\"saveRole('manager')\""

  [[entries.sites]]
  file = "frontend/src/pages/ProfileRoles.vue"
  anchor = "@update:modelValue=\"saveRole('instructor')\""

  [[entries.sites]]
  file = "frontend/src/pages/ProfileRoles.vue"
  anchor = "manager: 'Gestore',"
  symbol = "ROLE_NAME_MAP"

  [entries.visibility]
  subject = "Interruttori Valutatore, Manager (Gestore) e Teacher (Docente) nella scheda Ruoli del profilo"
  allow = ["Moderator"]
  deny = ["LMS Student", "Docente", "Course Creator", "Valutatore"]

[[entries]]
id = "profile-tab-bar-hidden-for-students"
title = "Barra delle schede del profilo nascosta agli studenti"
layer = "spa-graft"
confidence = "high"
intent = """
Uno studente nel profilo ha solo la scheda Certificati, quindi la barra delle schede sarebbe
solo rumore: si mostra solo ai non studenti (istruttori, moderatori, valutatori), che possono
avere altre schede come Ruoli (commit 3f63fec00). ATTENZIONE al merge di v2.59.0: l'upstream
cambia lo stesso elemento TabButtons (`:buttons` diventa `:options`); il v-if va conservato."""

  [[entries.sites]]
  file = "frontend/src/pages/Profile.vue"
  anchor = 'v-if="!$user.data?.is_student"'

  [entries.visibility]
  subject = "Barra delle schede nella pagina profilo"
  allow = ["Course Creator", "Moderator", "Batch Evaluator", "Docente", "Valutatore"]
  deny = ["LMS Student"]

[[entries]]
id = "profile-tabs-certificates-only"
title = "Profilo senza schede Informazioni, Disponibilità e Calendario"
layer = "spa-graft"
confidence = "low"
intent = """
Il profilo ha tolto la scheda Informazioni (About) e parte dalla scheda Certificati (commit
3f63fec00); le schede Disponibilità (Slots) e Calendario valutazioni (Schedule) per valutatori
e moderatori sono commentate (commit 498ec89d4 "Hide Schedule and Calendar tabs in profile").
Il motivo non è scritto nei commit: probabilmente il flusso upstream di prenotazione delle
valutazioni con disponibilità del valutatore non è usato dal cliente, e le informazioni
personali del profilo non servono. Le route ProfileAbout, ProfileEvaluator e
ProfileEvaluationSchedule esistono ancora in router.js (e la route base redirige ancora a
ProfileAbout)."""

  [[entries.sites]]
  file = "frontend/src/pages/Profile.vue"
  anchor = "let buttons = [{ label: __('Certificates'), value: 'Certificates' }]"
  symbol = "getTabButtons"

  [[entries.sites]]
  file = "frontend/src/pages/Profile.vue"
  anchor = "if (!activeTab.value) activeTab.value = 'Certificates'"
  symbol = "setActiveTab"

  [[entries.sites]]
  file = "frontend/src/pages/Profile.vue"
  anchor = "// if (currentUserHasHigherAccess() && isEvaluatorOrModerator()) {"
  symbol = "getTabButtons"

[[entries]]
id = "profile-cover-image-removed"
title = "Profilo senza immagine di copertina"
layer = "spa-graft"
confidence = "low"
intent = """
Nella pagina profilo l'immagine di copertina e il pulsante per cambiarla sono stati tolti,
resta solo lo spazio vuoto sopra l'avatar; l'intestazione ha uno sfondo grigio chiaro (commit
20bee4c5f "fix profile styke"). Il motivo non è scritto: probabilmente una scelta grafica del
cliente. Il componente EditCoverImage e la risorsa coverImage sono ancora importati ma non più
usati nel template."""

  [[entries.sites]]
  file = "frontend/src/pages/Profile.vue"
  anchor = '<div class="group relative h-[130px] w-full"></div>'

[[entries]]
id = "profile-load-error-no-permission"
title = "Profilo non caricato: messaggio di permesso invece della pagina vuota"
layer = "spa-graft"
confidence = "high"
intent = """
Se la risorsa del profilo fallisce (profilo di altri senza ruolo LMS, username inesistente) la
pagina mostra NoPermission invece di restare bianca (commit 97b62f6f9). È la metà frontend della
voce profile-own-without-lms-role (lato api.py): le due vanno riapplicate insieme."""

  [[entries.sites]]
  file = "frontend/src/pages/Profile.vue"
  anchor = '<NoPermission v-else-if="profile.error" />'

[[entries]]
id = "profile-evaluator-availability"
title = "Disponibilità del valutatore senza Google Calendar configurato"
layer = "spa-graft"
confidence = "high"
intent = """
La pagina delle disponibilità del valutatore legge i dati da os_lms.os_lms.api.get_evaluator_details
invece che dall'endpoint upstream: la versione os_lms prova a creare il Google Calendar del
valutatore ma ignora l'errore quando le API Google non sono configurate, così la pagina si apre
lo stesso (commit 30838fc12 "Fix Slots profile"). Se l'utente aggiunge una fascia senza giorno o
orari riceve un avviso invece di un nulla silenzioso; i segnaposto delle select sono tradotti.
La scheda Slots è oggi nascosta nel profilo (profile-tabs-certificates-only), ma la route esiste."""

  [[entries.sites]]
  file = "frontend/src/pages/ProfileEvaluator.vue"
  anchor = "url: 'os_lms.os_lms.api.get_evaluator_details',"
  symbol = "evaluator"

  [[entries.sites]]
  file = "frontend/src/pages/ProfileEvaluator.vue"
  anchor = "toast.warning(__('Please fill in all fields: day, start time and end time'))"
  symbol = "add"

[[entries]]
id = "profile-evaluation-schedule-heading"
title = "Titolo e stato vuoto nel calendario delle valutazioni prenotate"
layer = "spa-graft"
confidence = "high"
intent = """
La scheda Calendario valutazioni del profilo ha un titolo ("Valutazioni prenotate") e un testo
quando non c'è nulla in calendario, per far capire a cosa serve la scheda (commit 1815bdc42).
La scheda è oggi nascosta nel profilo (profile-tabs-certificates-only), ma la route esiste."""

  [[entries.sites]]
  file = "frontend/src/pages/ProfileEvaluationSchedule.vue"
  anchor = "{{ __('Booked Evaluations') }}"

  [[entries.sites]]
  file = "frontend/src/pages/ProfileEvaluationSchedule.vue"
  anchor = "{{ __('No evaluations scheduled yet') }}"

[[entries]]
id = "profile-edit-codice-fiscale"
title = "Codice fiscale nella modifica del profilo"
layer = "spa-graft"
confidence = "high"
intent = """
Lo studente inserisce il proprio codice fiscale dalla modifica del profilo: è il campo
personalizzato codice_fiscale di User, che l'integrazione TrueSkills invia come fiscalId per
emettere l'open badge del certificato. Si valida il formato (16 caratteri, schema italiano) e si
salva maiuscolo; vuoto è ammesso. È un dato personale e non passa da get_profile_details: viene
letto a parte con frappe.client.get_value sul proprio record e confrontato a parte per lo stato
"modificato". ATTENZIONE al merge di v2.59.0: l'upstream cambia lo slot dell'intestazione
(#body-header → #title) e la colonna della bio (TextEditor → RichTextEditor)."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditProfile.vue"
  anchor = 'v-model="profile.codice_fiscale"'

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditProfile.vue"
  anchor = "const CODICE_FISCALE_RE = "
  symbol = "saveProfile"

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditProfile.vue"
  anchor = "fieldname: 'codice_fiscale',"

[[entries]]
id = "profile-edit-image-optional"
title = "Immagine del profilo facoltativa"
layer = "spa-graft"
confidence = "high"
intent = """
Il profilo si salva anche senza immagine: l'upstream la rendeva obbligatoria, e uno studente che
voleva solo correggere nome o codice fiscale era bloccato (commit 02839f37a). La modifica
consiste nell'aver tolto `:required="true"` dall'Uploader e la voce 'Profile Image' dalla
validazione dei campi obbligatori; l'ancora è il commento che lo segnala."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditProfile.vue"
  anchor = "<!-- OSLMS-CUSTOM: profile image is optional -->"

[[entries]]
id = "profile-edit-reduced-fields"
title = "Modifica profilo ridotta a una colonna"
layer = "spa-graft"
confidence = "low"
intent = """
La finestra di modifica del profilo è a una sola colonna: sono commentati titolo (headline),
LinkedIn, Twitter e l'intera colonna destra (disponibile per, lingua, bio) (commit 29f976c3b
"fix: style"). Il motivo non è scritto: probabilmente il cliente vuole un profilo essenziale
(nome, cognome, immagine, codice fiscale). Nota: con la colonna destra commentata non si può più
cambiare la lingua dal profilo. ATTENZIONE al merge di v2.59.0: l'upstream modifica righe
dentro i blocchi commentati (RichTextEditor per la bio), il che darà conflitti in zone
commentate."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditProfile.vue"
  anchor = '<div class="grid grid-cols-1 gap-10">'

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditProfile.vue"
  anchor = "<!-- <FormControl v-model=\"profile.headline\" :label=\"__('Headline')\" />"

# ── permissions.py ─────────────────────────────────────────────────────────

[[entries]]
id = "permissions-valutatore-lesson-access"
title = "Il Valutatore legge le lezioni dei corsi delle sue classi"
layer = "spa-graft"
confidence = "high"
intent = """
Un Valutatore rivede i corsi delle classi che valuta: può leggere ogni lezione e i suoi
materiali per studenti come uno studente iscritto, senza iscrizione (quindi senza tracciamento
dei progressi) (commit b6b075222). I materiali riservati agli istruttori restano esclusi: il
ramo sta di proposito dopo il controllo instructor_only. ATTENZIONE al merge di v2.59.0:
l'upstream riscrive can_access_lesson in resolve_lesson_access, che restituisce la coppia
(is_instructor, can_access); il ramo del valutatore va rimesso lì come `return False, True`
dopo il controllo can_modify_course, così instructor_only continua a escluderlo. L'upstream
aggiunge anche can_access_quiz: valutare se il valutatore debba leggere anche i quiz."""

  [[entries.sites]]
  file = "lms/lms/permissions.py"
  anchor = "if is_course_valutatore(lesson_row.course, user):"
  symbol = "can_access_lesson"

  [entries.visibility]
  subject = "Risorse per studenti delle lezioni dei corsi delle classi valutate"
  allow = ["Valutatore della classe che contiene il corso"]
  deny = ["Valutatore su materiali riservati agli istruttori"]

# ── AppSidebar.vue ─────────────────────────────────────────────────────────

[[entries]]
id = "sidebar-more-group-removed"
title = "Gruppo \"Altro\" (pagine web) tolto dalla barra laterale"
layer = "spa-graft"
confidence = "low"
intent = """
Il gruppo upstream "More" della barra laterale (pagine web personalizzate, con il pulsante + per
aggiungerle riservato ai moderatori) è stato tolto (commit 032752e69 "Hide More group section in
sidebar"). Il motivo non è scritto: probabilmente la funzione pagine web non è usata. Nello
script restano toggleWebPages, openPageModal e PageModal."""

  [[entries.sites]]
  file = "frontend/src/components/Sidebar/AppSidebar.vue"
  anchor = '<!-- OSLMS-CUSTOM: upstream "More" group (custom web pages + add-page button) removed from the sidebar -->'

[[entries]]
id = "sidebar-frappe-onboarding-removed"
title = "Onboarding, aiuto e riferimenti a Frappe tolti dalla barra laterale"
layer = "spa-graft"
confidence = "low"
intent = """
Dalla barra laterale sono stati tolti il riquadro "Completa il tuo profilo" per gli studenti
(commit 5b09a2c99), il banner Getting Started con HelpModal e IntermediateStepModal (commit
f3be3c1bc), e le icone di aiuto, di prenotazione della sessione di onboarding con il team Frappe
e "Powered by Frappe Learning" (commit 1d3fa64ea). Deduzione: la piattaforma è presentata con il
marchio del cliente e non deve rimandare a Frappe né al suo percorso di onboarding. Nello script
restano import e variabili dell'onboarding (setUpOnboarding, showOnboarding, profileIsComplete)."""

  [[entries.sites]]
  file = "frontend/src/components/Sidebar/AppSidebar.vue"
  anchor = '<!-- OSLMS-CUSTOM: upstream "Complete your profile" prompt and Frappe GettingStartedBanner removed -->'

  [[entries.sites]]
  file = "frontend/src/components/Sidebar/AppSidebar.vue"
  anchor = '<!-- OSLMS-CUSTOM: upstream help, onboarding-call and "Powered by Frappe Learning" icons removed -->'

[[entries]]
id = "sidebar-app-version-label"
title = "Versione dell'app nel piede della barra laterale"
layer = "spa-graft"
confidence = "high"
intent = """
Il piede della barra laterale mostra il numero di versione dell'app, scritto a mano e aggiornato
a ogni rilascio (commit 10b5ac7eb, fd1856c7b, cbdc9582b), per sapere quale versione è installata
su un sito quando si segnala un problema. Il numero si aggiorna, l'elemento deve restare."""

  [[entries.sites]]
  file = "frontend/src/components/Sidebar/AppSidebar.vue"
  anchor = '<span id="versionApp" class="text-xs text-ink-gray-5">'

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 prima del merge di v2.59.0 — lezioni, editor e media
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 — gruppo "lessons-media" (pagina lezione, editor lezione,
# upload e media, utils/index.js, index.css) prima del merge di v2.59.0
# ---------------------------------------------------------------------------

[[entries]]
id = "lesson-sequential-access-lock"
title = "Blocco sequenziale di lezioni e quiz nella pagina lezione"
layer = "spa-graft"
confidence = "high"
intent = """
Quando il corso ha attivi enforce_lesson_order o enforce_quiz_on_completion (impostazioni
os_lms del corso, OsCourseSettings), lo studente non può aprire una lezione finché la
precedente non è completata, né il quiz finché la lezione non è completata. I flag
lesson_access e quiz_access li calcola l'override os_lms di get_lesson; la pagina mostra la
schermata «Lezione bloccata» al posto del corpo, le due schermate «Quiz bloccato» (quiz
dentro il contenuto EditorJS e quiz legacy body/quiz_id), disabilita i pulsanti Next e
nasconde il pannello Note. markProgress non segna mai completa una lezione bloccata:
altrimenti il timer di permanenza, che gira dietro la schermata di blocco, sbloccherebbe
da solo la lezione successiva e il blocco diventerebbe decorativo. Già perso una volta
con il merge upstream 1f5978236 (04/06/2026) e ripristinato in d88fc97f4: senza questo
innesto i due interruttori restano configurabili ma non hanno alcun effetto. Le condizioni
sono innestate sulle catene v-if esistenti (non un <template> che avvolge il corpo) per
tenere piccola la superficie di conflitto. ATTENZIONE: v2.63.0 riscrive profondamente
Lesson.vue; ricollocare schermate, guardie e pulsanti disabilitati nella nuova struttura."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "v-if=\"lessonBlocked\""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "v-if=\"quizBlocked && contentHasQuiz\""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "v-if=\"quizBlocked && lesson.data?.quiz_id\""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "if (lessonBlocked.value) return"
  symbol = "markProgress"

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "const applyAccessFromLesson = (data) => {"
  symbol = "applyAccessFromLesson"

[[entries]]
id = "lesson-instructors-hidden-from-students"
title = "Istruttori nascosti agli studenti nell'intestazione della lezione"
layer = "spa-graft"
confidence = "high"
intent = """
Stessa regola di course-instructors-hidden-from-students (pagina corso), applicata
all'intestazione della lezione: avatar e nomi degli istruttori li vedono solo moderatori e
istruttori della lezione (isAdmin), non gli studenti. Introdotta con lo stesso commit
1cf84e356; se l'upstream riscrive l'intestazione, rimettere il gate isAdmin sulla riga
degli istruttori."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "v-if=\"!zenModeEnabled && isAdmin\""

  [entries.visibility]
  subject = "istruttori nell'intestazione della lezione"
  allow = ["Moderator", "course-instructor"]
  deny = ["Student", "Guest"]

[[entries]]
id = "lesson-edit-button-editor-hash"
title = "Il pulsante Modifica della lezione apre la scheda Editor"
layer = "spa-graft"
confidence = "high"
intent = """
Gemella di course-outline-editor-hash: il pulsante Modifica nella pagina lezione porta al
corso con hash '#editor', che è l'id della scheda Editor; l'upstream usava '#course editor',
che non corrisponde a nessuna scheda, e l'amministratore finiva sull'anteprima del corso
invece che nell'editor della lezione. Se l'upstream corregge da sé l'hash in modo
equivalente, accettare la sua versione e marcare la voce come accepted-drift."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "hash: '#editor',"

[[entries]]
id = "lesson-discussions-removed"
title = "Discussioni/Community rimosse dalla lezione; restano solo le Note dello studente"
layer = "spa-graft"
confidence = "high"
intent = """
Il cliente non usa la community: la scheda Community (Discussions) e il pulsante delle
domande in modalità Zen sono rimossi per tutti i ruoli. Sotto la lezione resta solo il
pannello Note, visibile allo studente iscritto; moderatori e istruttori non vedono nessuna
scheda (currentTab resta null). Rimossi anche TabButtons e l'import di Discussions.
ATTENZIONE al merge di v2.59.0: l'upstream cambia :buttons in :options proprio sui
TabButtons che abbiamo tolto; mantenere la rimozione, non reintrodurre il componente."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "currentTab === 'Notes'"

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "// Community/discussions are hidden for all users and roles; only the"

  [entries.visibility]
  subject = "pannello Note sotto la lezione"
  allow = ["Student"]
  deny = ["Moderator", "course-instructor"]

[[entries]]
id = "lesson-ai-tutor-context"
title = "Il tutor AI conosce la lezione aperta o in modifica"
layer = "spa-graft"
confidence = "high"
intent = """
Il tutor AI os_lms (pulsanti flottanti AiFixedButtons) risponde nel contesto della lezione
corrente: la pagina lezione e l'editor della lezione pubblicano il nome della lezione nello
store aiContext appena caricata. Senza, il tutor riceve solo il corso e perde il contesto
della lezione, sia per lo studente sia per l'autore."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "if (data.name) aiContext.setLesson(data.name)"
  symbol = "setupLesson"

  [[entries.sites]]
  file = "frontend/src/pages/LessonForm.vue"
  anchor = "if (data.lesson.name) aiContext.setLesson(data.lesson.name)"

[[entries]]
id = "lesson-zen-mode-fullscreen-only"
title = "Modalità Zen nascosta dove manca la Fullscreen API (iPhone)"
layer = "spa-graft"
confidence = "high"
intent = """
La modalità Zen usa la Fullscreen API, che su iPhone (tutti i browser iOS sono WebKit) non
esiste per elementi non video: il pulsante non faceva nulla. canGoZen() rileva il supporto
e nasconde il comando dove non può funzionare (intestazione, controlli in lezione,
anteprima del CourseEditor); resta su desktop, Android e iPad."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "if (!fullscreenSupported) return false"
  symbol = "canGoZen"

[[entries]]
id = "lesson-video-error-keeps-enforcement"
title = "Un errore non fatale del player non disattiva l'obbligo di vedere il video"
layer = "spa-graft"
confidence = "high"
intent = """
Con enforce_video_completion attivo, un errore del player fa ripiegare il completamento
sul timer di permanenza. Plyr inoltra però anche errori non legati alla riproduzione: Vimeo
risponde con PrivacyError alla chiamata di metadati getVideoUrl() sui video non in elenco,
mentre il video funziona. Il ripiego scatta solo se il player non è mai diventato pronto e
l'errore non nomina il metodo SDK da cui arriva (shouldEngageFallbackOnPlayerError in
utils/lessonProgress.ts); il timeout di 15 s senza ready resta invariato. Senza, un video
Vimeo non in elenco disattiva silenziosamente l'obbligo di completamento."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "!shouldEngageFallbackOnPlayerError({"
  symbol = "getPlyrSource"

  [entries.checks]
  unit = "frontend/src/tests/lessonProgress.test.ts"

[[entries]]
id = "lesson-highlight-offset"
title = "Evidenziazioni dello studente sull'occorrenza selezionata"
layer = "spa-graft"
confidence = "high"
intent = """
Un'evidenziazione salvata solo come testo si ripristinava sempre sulla prima occorrenza
(evidenziando il terzo «con» si illuminava il primo). Ora la nota salva anche text_offset
(campo custom os_lms su LMS Lesson Note, creato in apps/os_lms/os_lms/setup.py) e il
ripristino usa l'offset, con la ricerca testuale come ripiego per le note vecchie che
sceglie l'occorrenza più vicina all'offset. Nello stesso percorso: uno span per nodo di
testo (surroundContents falliva sulle selezioni che attraversano elementi), niente span
annidati quando la lista note si ricarica, il menu abbina la nota per testo+offset (prima
cancellava la nota sbagliata) e la rimozione scarta gli span invece di azzerare lo sfondo.
ATTENZIONE al merge di v2.59.0: l'upstream elimina frontend/src/utils/frappe-ui-colors.json,
l'import colorsJSON e getColor da utils/index.js e passa createHighlightSpan e il menu a
var(--<colore>-200/400); utils/index.js importa ancora quel JSON, quindi accettare la
rimozione upstream e togliere getColor dal nostro import multiriga in InlineLessonMenu,
tenendo getRangeOffset e removeHighlight."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "'text_offset', 'note'"

  [[entries.sites]]
  file = "frontend/src/components/Notes/InlineLessonMenu.vue"
  anchor = "selectionOffset.value = getRangeOffset(range)"
  symbol = "updateMenuPosition"

  [[entries.sites]]
  file = "frontend/src/components/Notes/InlineLessonMenu.vue"
  anchor = "const noteForSelection = () => {"

  [[entries.sites]]
  file = "frontend/src/components/Notes/InlineLessonMenu.vue"
  anchor = "removeHighlight(notesToDelete.name)"
  symbol = "deleteHighlight"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "export const getRangeOffset = (range, selector = '#editor') => {"
  symbol = "getRangeOffset"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "findSegmentsAtOffset(root, note.text_offset, phrase) ||"
  symbol = "highlightText"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "export const removeHighlight = (name) => {"
  symbol = "removeHighlight"

  [entries.checks]
  unit = "frontend/src/tests/lessonHighlight.test.ts"

[[entries]]
id = "lesson-highlight-menu-ink"
title = "Ritocco di stile: etichette del menu evidenziazione leggibili nel tema"
layer = "spa-graft"
confidence = "high"
intent = """
Il menu che compare selezionando testo nella lezione non aveva un colore di inchiostro:
nomi dei colori e azioni restavano neri e ignoravano il tema (illeggibili in scuro).
text-ink-gray-9 sul contenitore li fa ereditare; l'intestazione «Highlight» mantiene il
suo text-ink-gray-5."""

  [[entries.sites]]
  file = "frontend/src/components/Notes/InlineLessonMenu.vue"
  anchor = "class=\"text-sm text-ink-gray-9 absolute bg-surface-elevation-2 border rounded-md z-10 w-44\""

[[entries]]
id = "lesson-long-strings-wrap"
title = "Stringhe lunghe nella lezione vanno a capo invece di spostare la pagina"
layer = "spa-graft"
confidence = "high"
intent = """
Una lezione con un token lungo senza spazi (per esempio un URL Cloudflare/Bunny incollato
come testo) sforava la colonna e spingeva tutta la pagina a destra. overflow-wrap sul
contenuto (.ProseMirror e .ce-paragraph, per battere i default di EditorJS), min-w-0 sulle
colonne della griglia 70/30 e sul contenitore del titolo, break-words sul titolo della
lezione e sul titolo del corso nella barra laterale. ATTENZIONE al merge di v2.59.0:
l'upstream cambia la stessa riga del titolo in Lesson.vue (text-5xl-semibold ->
text-4xl-semibold) e del titolo corso in StudentLessonSidebar (text-xl-semibold ->
text-lg-semibold): prendere la classe upstream e riaggiungere break-words."""

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "<div class=\"text-5xl-semibold text-ink-gray-9 break-words\">"

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "overflow-wrap: break-word;"

  [[entries.sites]]
  file = "frontend/src/components/StudentLessonSidebar.vue"
  anchor = "text-ink-gray-9 leading-snug break-words"

[[entries]]
id = "lesson-sidebar-open-active-chapter"
title = "La barra laterale apre il capitolo della lezione attiva"
layer = "spa-graft"
confidence = "high"
intent = """
Il Disclosure di headlessui legge defaultOpen solo al mount e la barra laterale non si
rimonta tra una lezione e l'altra: passando con «Next» a una lezione del capitolo
successivo, quel capitolo restava chiuso. La chiave del Disclosure include se il capitolo
contiene la lezione attiva, così il capitolo che diventa attivo si rimonta e si apre,
mentre gli altri conservano lo stato scelto a mano. ATTENZIONE: v2.63.0 riscrive
StudentLessonSidebar; verificare che la nuova versione apra da sé il capitolo attivo,
altrimenti reinnestare la chiave."""

  [[entries.sites]]
  file = "frontend/src/components/StudentLessonSidebar.vue"
  anchor = ':key="`${chapter.name}:${chapterHasActiveLesson(chapter)}`"'

  [[entries.sites]]
  file = "frontend/src/components/StudentLessonSidebar.vue"
  anchor = "function chapterHasActiveLesson(chapter) {"
  symbol = "chapterHasActiveLesson"

[[entries]]
id = "editor-i18n-labels"
title = "Etichette dell'editor EditorJS tradotte"
layer = "spa-graft"
confidence = "high"
intent = """
EditorJS mostra menu dei blocchi, barra inline e block tune in inglese se non riceve un
dizionario i18n. getEditorI18n() passa ogni etichetta per __() così l'editor segue la
lingua dell'utente (traduzioni in lms/translations/it.csv); le chiavi devono restare le
stringhe inglesi esatte che EditorJS e i suoi tool cercano. La usano la pagina lezione
(renderer in sola lettura) e BlockEditor.vue (editor dell'autore, file fuori da questo
gruppo)."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "export function getEditorI18n() {"
  symbol = "getEditorI18n"

  [[entries.sites]]
  file = "frontend/src/pages/Lesson.vue"
  anchor = "i18n: getEditorI18n(),"
  symbol = "renderEditor"

[[entries]]
id = "lesson-form-os-settings-panel"
title = "Pannello impostazioni os_lms nell'editor della lezione"
layer = "spa-graft"
confidence = "high"
intent = """
Al posto dell'interruttore upstream «Include in preview» l'editor della lezione monta
OsLessonForm, che raccoglie le impostazioni volute dal cliente: include_in_preview, durata
(usata per la durata del corso), tag della lezione e controlli dell'indicizzazione per il
tutor AI. Se un merge riscrive LessonForm e lo perde, i campi restano nel database ma
nessuno può più modificarli. ATTENZIONE: v2.63.0 riscrive LessonForm; rimontare il
componente nella nuova struttura."""

  [[entries.sites]]
  file = "frontend/src/pages/LessonForm.vue"
  anchor = "<OsLessonForm :lesson=\"lesson\" @dirty=\"markDirty\" />"

  [[entries.sites]]
  file = "frontend/src/pages/LessonForm.vue"
  anchor = "import OsLessonForm from '@/oslms/pages/OsLessonForm.vue'"

[[entries]]
id = "lesson-form-editable-fields"
title = "Il salvataggio della lezione scrive solo i campi modificabili"
layer = "spa-graft"
confidence = "high"
intent = """
L'upstream salvava con set_value l'intero oggetto lesson caricato all'apertura; così ogni
salvataggio o autosave riscriveva anche i campi gestiti dal server, in particolare
index_status e indexed_at dell'indicizzazione AI (RAG os_lms), riportandoli ai valori
vecchi. Ora si inviano solo i campi dell'elenco EDITABLE_LESSON_FIELDS (che include i
campi custom duration e tags). Un nuovo campo modificabile dall'editor va aggiunto
all'elenco, altrimenti non si salva. ATTENZIONE: v2.63.0 dichiara «fix(lesson)!: stop
lesson content being destroyed on save» e riscrive il percorso di salvataggio: verificare
che la nuova versione non reinvii i campi del server e reinnestare il filtro se serve."""

  [[entries.sites]]
  file = "frontend/src/pages/LessonForm.vue"
  anchor = "for (const key of EDITABLE_LESSON_FIELDS) {"

[[entries]]
id = "lesson-form-style-tweaks"
title = "Ritocchi di stile nell'editor della lezione"
layer = "spa-graft"
confidence = "low"
intent = """
Su telefono l'editor perde il margine orizzontale e il padding di 20 unità (resta px-3),
così non è schiacciato in una colonna stretta (commit a1437f5fa). Il titolo ha
!bg-transparent (probabilmente per non ereditare lo sfondo dei campi dal tema Elite) e
le note dell'istruttore un mb-2. ATTENZIONE al merge di v2.59.0: l'upstream cambia la
stessa riga di classi del titolo (text-3xl -> text-2xl): prendere la sua e riaggiungere
!bg-transparent."""

  [[entries.sites]]
  file = "frontend/src/pages/LessonForm.vue"
  anchor = "class=\"space-y-6 px-3 sm:mx-10 sm:px-20\""

  [[entries.sites]]
  file = "frontend/src/pages/LessonForm.vue"
  anchor = "!bg-transparent"

[[entries]]
id = "upload-documents-and-archives"
title = "Documenti e archivi caricabili nelle lezioni"
layer = "spa-graft"
confidence = "high"
intent = """
Gli autori allegano alle lezioni anche documenti e archivi (doc/xls/ppt/odt, csv, txt,
zip, rar, 7z…), non solo immagini, video, audio e PDF; prima tutto il resto veniva
rifiutato o mostrato come immagine rotta. I file non multimediali diventano una scheda di
download (FileBlock). Quando Frappe non restituisce file_type (alcuni archivi) il tipo si
ricava dall'estensione, sia all'upload sia al caricamento, e il blocco resta valido
invece di sparire al salvataggio. Gli errori di upload (per esempio file oltre
max_file_size) compaiono in un toast invece di lasciare il blocco vuoto."""

  [[entries.sites]]
  file = "frontend/src/components/UploadPlugin.vue"
  anchor = ":fileTypes=\"acceptedFileTypes\""

  [[entries.sites]]
  file = "frontend/src/components/UploadPlugin.vue"
  anchor = "const documentExtensions = ["

  [[entries.sites]]
  file = "frontend/src/components/UploadPlugin.vue"
  anchor = "file_type: file.file_type || extension,"
  symbol = "addFile"

  [[entries.sites]]
  file = "frontend/src/components/UploadPlugin.vue"
  anchor = "const onFailure = (error) => {"
  symbol = "onFailure"

  [[entries.sites]]
  file = "frontend/src/utils/upload.js"
  anchor = "const app = createApp(FileBlock, {"
  symbol = "Upload.renderFile"

  [[entries.sites]]
  file = "frontend/src/utils/upload.js"
  anchor = "if (!savedData || !savedData.file_url) {"
  symbol = "Upload.validate"

  [[entries.sites]]
  file = "frontend/src/utils/upload.js"
  anchor = "getFileType(file) {"
  symbol = "Upload.getFileType"

  [[entries.sites]]
  file = "frontend/src/utils/upload.js"
  anchor = "isImage(type) {"
  symbol = "Upload.isImage"

[[entries]]
id = "upload-unattached-new-lesson"
title = "Upload senza allegato finché la lezione non è salvata"
layer = "spa-graft"
confidence = "high"
intent = """
Una lezione nuova non ha ancora un docname; inviare doctype senza un docname valido fa
rifiutare l'upload da Frappe recente (417 «Attached To Name must be a string or an
integer»). Finché il docname manca il file si carica senza allegarlo. ATTENZIONE al merge
di v2.59.0: l'upstream risolve lo stesso problema nello stesso computed (nuova prop
uploadContext, contesti reattivi in LessonForm e upload.js che passa uploadContext);
accettare la versione upstream, tenere intorno le nostre aggiunte di
upload-documents-and-archives e marcare questa voce come accepted-drift."""

  [[entries.sites]]
  file = "frontend/src/components/UploadPlugin.vue"
  anchor = "if (props.docname) {"
  symbol = "uploadArgs"

[[entries]]
id = "upload-pdf-block-mobile"
title = "PDF delle lezioni leggibili su mobile"
layer = "spa-graft"
confidence = "high"
intent = """
I browser mobili non mostrano un PDF in un iframe (Chrome Android mostra un segnaposto di
download, Safari iOS una prima pagina statica). I blocchi PDF caricati nella lezione
passano quindi per PdfBlock, che sotto i 640px mostra una scheda che apre il file in una
nuova scheda e su desktop tiene il frame incorporato. Parte della stessa correzione di
lesson-pdf-inline-filename (nome del file nel Content-Disposition); PdfBlock si usa anche
in LessonContent.vue per le lezioni markdown."""

  [[entries.sites]]
  file = "frontend/src/utils/upload.js"
  anchor = "const app = createApp(PdfBlock, {"
  symbol = "Upload.renderFile"

  [entries.checks]
  unit = "frontend/src/tests/pdfBlock.test.ts"

[[entries]]
id = "upload-toolbox-icon-theme"
title = "Ritocco di stile: icona Upload dell'editor segue il tema"
layer = "spa-graft"
confidence = "low"
intent = """
L'icona del tool Upload nel menu dei blocchi era nera fissa, invisibile nel tema scuro;
con currentColor segue il colore del testo del menu. Commit «fix color» (75ded9ac6), il
motivo è dedotto dal codice."""

  [[entries.sites]]
  file = "frontend/src/utils/upload.js"
  anchor = "color: 'currentColor',"
  symbol = "Upload.toolbox"

[[entries]]
id = "video-block-dead-timeupdate"
title = "Rimosso il binding @timeupdate orfano dal VideoBlock"
layer = "spa-graft"
confidence = "high"
intent = """
Il template legava @timeupdate="updateTime" a una funzione che non esiste più (il player
traccia il tempo con videoRef.ontimeupdate): il gestore era un no-op che generava un
avviso di Vue. La personalizzazione consiste nella rimozione della riga, quindi l'ancora è
il commento. Se l'upstream toglie o ridefinisce da sé il binding, marcare la voce come
accepted-drift."""

  [[entries.sites]]
  file = "frontend/src/components/VideoBlock.vue"
  anchor = "OSLMS-CUSTOM: dead @timeupdate binding removed"

[[entries]]
id = "editor-color-picker-tool"
title = "Tool colore editorjs-color-picker nell'editor della lezione"
layer = "spa-graft"
confidence = "low"
intent = """
Aggiunto il 20/05/2026 (6804b7d1d) per colorare il testo delle lezioni con la dipendenza
editorjs-color-picker, adattata a tool inline da ColorPickerInline. Da allora l'upstream ha
introdotto il proprio tool colore (utils/inline/Color.ts, <lms-inline-color>) e il nostro
«ColorPicker» è ancora registrato ma non compare in INLINE_TOOLBAR_ORDER: non è chiaro se
serva ancora (per esempio per rendere correttamente contenuti colorati salvati con il
vecchio tool) o sia codice morto. ATTENZIONE: v2.63.0 rimuove il tool colore inline
upstream; decidere con il committente prima di quel merge se questo tool deve tornare lo
strumento colore delle lezioni o essere eliminato. Resta anche la dipendenza in
package.json (memoria frappeui_1_0_beta_merge_2026_06)."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "class ColorPickerInline extends ColorPicker {"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "class: ColorPickerInline,"
  symbol = "getEditorTools"

[[entries]]
id = "editor-inline-color-persist"
title = "Il colore del testo delle lezioni sopravvive al caricamento"
layer = "spa-graft"
confidence = "high"
intent = """
Il tool colore inline avvolge la selezione in <lms-inline-color style=…>; DOMPurify con la
configurazione di default scarta i tag sconosciuti, quindi a ogni caricamento o
visualizzazione il colore spariva. sanitizeJSON ammette quel tag. È la metà client della
correzione: la metà server è l'allowlist EDITORJS_CUSTOM_TAGS in lms/lms/utils.py (memoria
lesson_editorjs_custom_tags_server_strip). Lacuna nota: <lms-align> (allineamento) non è
nell'ADD_TAGS client e si perde al caricamento. ATTENZIONE: v2.63.0 rimuove il tool colore
inline (utils/inline/Color.ts) e riscrive il salvataggio del contenuto; i contenuti già
salvati con <lms-inline-color> vanno comunque mostrati, decidere con il committente come."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "ADD_TAGS: ['lms-inline-color']"
  symbol = "sanitizeJSON"

[[entries]]
id = "editor-header-inline-toolbar"
title = "Barra inline anche sui titoli delle lezioni"
layer = "spa-graft"
confidence = "low"
intent = """
I blocchi titolo dell'editor hanno la stessa barra inline dei paragrafi (colore,
allineamento, grassetto…): senza la chiave inlineToolbar EditorJS non apre la barra sui
titoli. Aggiunta nel merge-fix af167dc5a senza motivazione esplicita; il motivo è dedotto.
v2.63.0 aggiunge da sé inlineToolbar: INLINE_TOOLBAR_ORDER sul nuovo tool Heading: in quel
merge accettare la versione upstream e marcare la voce come accepted-drift."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "class: Header,\n\t\t\tinlineToolbar: INLINE_TOOLBAR_ORDER,"
  symbol = "getEditorTools"

[[entries]]
id = "embed-vimeo-share-links"
title = "Link di condivisione Vimeo incollati nelle lezioni"
layer = "spa-graft"
confidence = "high"
intent = """
Il pulsante «Copia link» di Vimeo dà vimeo.com/share/<uuid>, che non contiene l'id del
video, non reindirizza e che l'oEmbed di Vimeo rifiuta: gli autori ottenevano testo invece
del video. Il servizio vimeoShare intercetta solo l'incolla; VideoEmbed (sottoclasse di
Embed registrata come embed.class) chiede a os_lms resolve_vimeo_share la forma canonica e
riscrive il blocco come un normale blocco vimeo; se la risoluzione fallisce lascia l'URL
come testo con un toast. Invariante: vimeoShare non deve mai essere salvato (il backend
legge source/embed dei blocchi vimeo per l'audio HLS e il trascrittore AI). Memoria
vimeo_share_link_server_resolution; l'altra metà è VideoPreviewField.vue."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "class VideoEmbed extends Embed {"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "class: VideoEmbed,"
  symbol = "getEditorTools"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "regex: VIMEO_SHARE_RE,"
  symbol = "getEditorTools"

  [entries.checks]
  unit = "frontend/src/tests/vimeoShareEmbed.test.ts"

[[entries]]
id = "video-embed-url-reexport"
title = "getVideoEmbedURL riesportata da utils per l'hero del corso"
layer = "spa-graft"
confidence = "high"
intent = """
getVideoEmbedURL (YouTube e Vimeo, incluse le forme con hash) vive in utils/video.ts ed è
riesportata da utils/index.js per i chiamanti esistenti, in particolare l'hero os_lms del
corso (oslms/components/CourseHero.vue importa da '@/utils/'). Se il merge toglie la
riesportazione, il video dell'hero smette di compilare/funzionare."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "export { getVideoEmbedURL } from './video'"

[[entries]]
id = "sidebar-export-statistics"
title = "Voce «Export Statistics» nella barra laterale"
layer = "spa-graft"
confidence = "high"
intent = """
Voce di menu verso la pagina os_lms di esportazione delle statistiche studenti
(StudentStatsExport, CSV/XLSX). Compare solo se get_user_info restituisce can_export_stats,
calcolato da can_export_student_stats() in os_lms (System Manager e Gestore), e mai nel
menu mobile."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "return !forMobile && userResource?.data?.can_export_stats"
  symbol = "getSidebarItems"

  [entries.visibility]
  subject = "voce Export Statistics nella barra laterale"
  allow = ["System Manager", "Gestore"]
  deny = ["Moderator", "Docente", "Student", "Guest"]

[[entries]]
id = "search-like-filter-words"
title = "Ricerca nelle liste per parole indipendenti"
layer = "spa-graft"
confidence = "high"
intent = """
La casella di ricerca delle liste costruiva un unico LIKE contiguo ("%Corso p%"): aggiungere
una seconda parola azzerava i risultati. searchLikeFilter() unisce le parole con %
("%Corso%p%"), così ogni parola aggiunta restringe invece di svuotare. La usano le liste di
corsi, batch, programmi, quiz, assegnazioni ed esercizi di programmazione (file fuori da
questo gruppo)."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "export function searchLikeFilter(text) {"
  symbol = "searchLikeFilter"

[[entries]]
id = "sanitize-html-keep-iframes"
title = "sanitizeHTML conserva i video YouTube incorporati"
layer = "spa-graft"
confidence = "high"
intent = """
Nata per non perdere i video YouTube incorporati nella descrizione del corso (commit
514f89cf0): sanitizeHTML estrae gli iframe prima di DOMPurify e li reinserisce dopo, e
ammette tag video/source/div con i relativi attributi. La usano i moduli che ripuliscono i
testi (nuovo corso, profilo, assegnazioni, quiz, lavori, programmi, esercizi) e il pannello
notifiche. Nota di sicurezza da valutare col committente: gli iframe estratti tornano nel
testo senza passare da DOMPurify, con tutti i loro attributi; restano anche due
console.log di debug."""

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = '/<iframe[\s\S]*?<\/iframe>/gi,'
  symbol = "sanitizeHTML"

  [[entries.sites]]
  file = "frontend/src/utils/index.js"
  anchor = "ADD_TAGS: ['iframe'],"
  symbol = "sanitizeHTML"

[[entries]]
id = "theme-elite-import"
title = "Tema Elite del cliente caricato sopra frappe-ui"
layer = "spa-graft"
confidence = "high"
intent = """
Il foglio di stile globale importa il tema Elite del cliente (styles/theme/elite/main.css,
nostro), che ridefinisce colori e componenti per l'identità visiva della piattaforma. Senza
questo import l'intera SPA torna allo stile upstream. ATTENZIONE al merge di v2.59.0:
l'upstream toglie la riga adiacente @import './styles/codemirror.css'; accettare la
rimozione e tenere l'import del tema."""

  [[entries.sites]]
  file = "frontend/src/index.css"
  anchor = "@import './styles/theme/elite/main.css';"

[[entries]]
id = "popover-above-floating-buttons"
title = "Menu e popover sopra i pulsanti flottanti del tutor AI"
layer = "spa-graft"
confidence = "high"
intent = """
I wrapper popper di Radix Vue (Dropdown, Select, Tooltip, Popover di frappe-ui) sono
teletrasportati nel body senza z-index; gli elementi fissi locali, come i pulsanti
flottanti del tutor AI e le modali a z-50, li coprivano. z-index 200 li porta sopra.
Introdotto insieme al pulsante fisso del tutor AI (e1aa5e126)."""

  [[entries.sites]]
  file = "frontend/src/index.css"
  anchor = "[data-radix-popper-content-wrapper] {"

[[entries]]
id = "prose-hr-table-colors"
title = "Ritocchi di stile del testo ricco: separatori e bordi delle tabelle"
layer = "spa-graft"
confidence = "low"
intent = """
Nei testi ricchi (descrizioni) il separatore <hr> prende il colore del testo circostante
invece del grigio di prose, così resta visibile dove il testo è bianco (per esempio su un
banner scuro); i bordi delle tabelle ProseMirror usano --ink-gray-9 per essere visibili.
Commit «fix: style» e «fix color»: dove si vedeva il problema è dedotto dal commento."""

  [[entries.sites]]
  file = "frontend/src/index.css"
  anchor = ".ProseMirror hr {"

  [[entries.sites]]
  file = "frontend/src/index.css"
  anchor = ".ProseMirror .tableWrapper table td,"

[[entries]]
id = "quiz-radio-classic"
title = "Radio button classici nei quiz"
layer = "spa-graft"
confidence = "high"
intent = """
@tailwindcss/forms ridisegna i radio selezionati come cerchio pieno colorato con puntino
bianco; il cliente vuole l'aspetto classico (cerchio bianco con puntino nero) per le
opzioni dei quiz e per ogni radio nativo, e nessun anello di focus. Le regole del plugin
usano :where() (specificità 0), quindi bastano selettori semplici, con le varianti
:hover/:focus perché il plugin riafferma lo sfondo in quegli stati. Memoria
frappeui_tokens_raw_rgb_channels: accent-color è inerte con appearance:none."""

  [[entries.sites]]
  file = "frontend/src/index.css"
  anchor = "input[type='radio']:checked:hover,"

  [[entries.sites]]
  file = "frontend/src/index.css"
  anchor = "input[type='radio']:focus {"

[[entries]]
id = "image-upload-translations"
title = "Ritocchi di traduzione nei selettori di immagine"
layer = "spa-graft"
confidence = "high"
intent = """
Nei selettori di immagine di copertina (EditCoverImage) e Unsplash il segnaposto della
ricerca e l'etichetta «Upload Image» passano per __(), così compaiono in italiano. In
UnsplashImageBrowser si importa anche TextInput, usato nel template ma mai importato (il
campo di ricerca non veniva risolto). v2.59.0 cambia l'API del Popover in cima agli stessi
file, fuori dalle nostre righe."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/EditCoverImage.vue"
  anchor = ":placeholder=\"__('search by keyword')\""

  [[entries.sites]]
  file = "frontend/src/components/UnsplashImageBrowser.vue"
  anchor = ":placeholder=\"__('search by keyword')\""

  [[entries.sites]]
  file = "frontend/src/components/UnsplashImageBrowser.vue"
  anchor = "import { Popover, FileUploader, Button, TextInput } from 'frappe-ui'"

[[entries]]
id = "video-statistics-tab-label"
title = "Ritocco di traduzione: etichetta delle schede nelle statistiche video"
layer = "spa-graft"
confidence = "high"
intent = """
L'etichetta delle schede «Video N» usava __() su un template literal, che non è
traducibile (la chiave cambia a ogni indice); ora è __('Video {0}').format(n)."""

  [[entries.sites]]
  file = "frontend/src/components/Modals/VideoStatistics.vue"
  anchor = "label: __('Video {0}').format(index + 1),"

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 prima del merge di v2.59.0 — corsi, programmi e home
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Censimento 2026-09-23 — gruppo "courses-programs-home" (bozza prima del merge di v2.59.0)
# Card corso, elenco corsi, dashboard corso, modulo corso, home, programmi, offerte di lavoro
# ---------------------------------------------------------------------------

# --- Card del corso (CourseCard.vue) ---------------------------------------

[[entries]]
id = "courses-card-duration"
title = "Durata del corso sulla card al posto degli iscritti"
layer = "spa-graft"
confidence = "high"
intent = """
La card del corso mostra la durata complessiva (somma dei minuti delle lezioni, campo os_lms
total_minutes calcolato dall'override get_courses) invece del numero di studenti iscritti:
per il cliente la durata è l'informazione utile per scegliere un corso, il numero di iscritti
no. È già stata persa una volta in un merge (commit ff9a87957): il blocco era rimasto con la
nostra condizione e il tooltip "Durata" ma mostrava di nuovo icona e numero degli iscritti.
Dopo ogni merge controllare che dentro il blocco ci siano ancora l'icona orologio e
formattedDuration."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCard.vue"
  anchor = "<div v-if=\"formattedDuration\">"

  [[entries.sites]]
  file = "frontend/src/components/CourseCard.vue"
  anchor = "const formattedDuration = computed(() => {"
  symbol = "formattedDuration"

[[entries]]
id = "courses-card-certification-badge"
title = "Indicatore \"certificazione disponibile\" sulla card del corso"
layer = "spa-graft"
confidence = "high"
intent = """
Al posto della valutazione media (le recensioni sono disattivate, vedi course-reviews-removed)
la card indica se il corso rilascia un certificato, anche quando il certificato è solo
TrueSkills (trueskills_certificate_enabled, esposto dall'override os_lms di get_courses):
senza, i corsi che rilasciano solo il badge TrueSkills non mostravano l'icona. ATTENZIONE,
deriva già presente: dentro il blocco oggi c'è di nuovo il contenuto upstream della
valutazione (stella + formatRating(course.rating)) invece dell'icona Award del commit
ae30b25cb, cioè la stessa deriva da merge già corretta per la durata; va chiesto al
committente se ripristinare l'icona."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCard.vue"
  anchor = "course.trueskills_certificate_enabled"

[[entries]]
id = "courses-card-tag-colors"
title = "Tag colorati (LMS OS Tag) sulla card del corso"
layer = "spa-graft"
confidence = "high"
intent = """
I tag del corso si mostrano sulla card come badge colorati, con i colori configurati nel
doctype os_lms LMS OS Tag. L'elenco corsi carica una volta la mappa tag→colore e la fornisce
con provide('tagColorMap'); CourseTagBadges la legge con inject (la pagina corso fa lo
stesso, vedi course-overview-tag-colors). Senza il provide i badge non hanno colore.
ATTENZIONE al merge di v2.59.0: l'upstream elimina l'import di
@/utils/frappe-ui-colors.json (file cancellato) nella riga subito sotto il nostro import di
CourseTagBadges e riscrive gradientColor con var(--colore-400): prendere la loro versione di
gradientColor e tenere i nostri import."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCard.vue"
  anchor = "<CourseTagBadges v-if=\"course.tags\" :tags=\"course.tags\" class=\"my-1\" />"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "doctype: 'LMS OS Tag',"
  symbol = "tagResource"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "provide('tagColorMap', tagColorMap)"

[[entries]]
id = "courses-card-footer-removed"
title = "Piè di card del corso rimosso (istruttori, prezzo, certificazione)"
layer = "spa-graft"
confidence = "low"
intent = """
Dalla card del corso è stata tolta la fascia inferiore dell'upstream: avatar e nomi degli
istruttori, prezzo del corso a pagamento e icona "Get Certified". Tolta nel commit 84bde1890
(che introduce la durata) senza motivazione scritta. Presumibilmente coerente con la scelta
di non mostrare gli istruttori agli studenti (course-instructors-hidden-from-students) e con
l'indicatore di certificazione spostato in alto; il prezzo sparisce anche per i corsi a
pagamento. Il blocco non è commentato ma cancellato, per questo l'ancora è sul marcatore."""

  [[entries.sites]]
  file = "frontend/src/components/CourseCard.vue"
  anchor = 'OSLMS-CUSTOM: upstream card footer (instructor avatars, price, "Get Certified" icon) removed'

# --- Recensioni, outline, certificati ----------------------------------------

[[entries]]
id = "course-reviews-legacy-block-residue"
title = "Residuo di merge in CourseReviews (stelle e testo duplicati)"
layer = "spa-graft"
confidence = "low"
intent = """
Non è una personalizzazione da conservare: è un residuo di un vecchio merge. Accanto al blocco
upstream riprogettato (stelle + testo con "vedi altro") è rimasta la vecchia versione di
stelle e testo della recensione, che quindi comparirebbero due volte; di nostro ci sono solo i
token colore del tema (fill-ink-amber-2 / fill-ink-gray-3, commit bf14f80d1). Oggi è
invisibile: CourseReviews non viene reso nella pagina corso (course-reviews-removed). Al
merge di v2.59.0 (che tocca le classi di testo delle righe vicine) prendere la versione
upstream intera ed eliminare il blocco residuo, poi togliere questa voce; da confermare col
committente."""

  [[entries.sites]]
  file = "frontend/src/components/CourseReviews.vue"
  anchor = "? 'fill-ink-amber-2'"

[[entries]]
id = "create-outline-button-import"
title = "Import esplicito di Button in CreateOutline"
layer = "spa-graft"
confidence = "high"
intent = """
Import esplicito di Button da frappe-ui aggiunto durante la correzione di un merge (commit
027c0c56f) perché il componente non risolveva più Button da solo. Il componente non è usato
da nessuna pagina: se l'upstream lo cancella o aggiunge lo stesso import, accettare la sua
versione."""

  [[entries.sites]]
  file = "frontend/src/components/CreateOutline.vue"
  anchor = "import { Button } from 'frappe-ui'"

[[entries]]
id = "course-certificate-pdf-chrome"
title = "Certificato del corso scaricato con il generatore PDF Chrome"
layer = "spa-graft"
confidence = "high"
intent = """
download_pdf usa wkhtmltopdf se non riceve pdf_generator, ignorando sia il Print Format sia
le Print Settings; il modello di certificato del cliente usa transform:rotate, che solo il
generatore Chrome rende, e con wkhtmltopdf il PDF usciva bianco (memoria
certificate-print-format-setup). Ogni link di download del certificato nella SPA aggiunge
&pdf_generator=chrome; lo stesso innesto vive anche in components/Modals/Event.vue e in
oslms/composables/useCertificateViewer.js."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseCertification.vue"
  anchor = "&pdf_generator=chrome"
  symbol = "openCertificate"

# --- Dashboard del corso (CourseDashboard.vue) --------------------------------

[[entries]]
id = "course-dashboard-rating-card-hidden"
title = "Riquadro \"Valutazione media\" nascosto nella dashboard del corso"
layer = "spa-graft"
confidence = "high"
intent = """
Le recensioni dei corsi sono disattivate (course-reviews-removed, confermato dal committente
il 2026-09-23), quindi la valutazione media sarebbe sempre 0: il riquadro è commentato e la
riga dei numeri passa da 4 a 3 colonne. Sono rimasti Iscritti, Completamento medio e Lezioni.
Il blocco commentato è la modifica, per questo l'ancora è sul commento."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "<!-- <NumberChartGraph"

[[entries]]
id = "course-dashboard-sortable-students"
title = "Colonne ordinabili nell'elenco studenti della dashboard del corso"
layer = "spa-graft"
confidence = "high"
intent = """
Chi gestisce il corso può ordinare gli studenti per nome, avanzamento e data di iscrizione
cliccando le intestazioni (freccia di direzione). L'ordinamento è lato server (orderBy della
list resource) così resta corretto anche sulle pagine caricate con "Carica altro"; il
default esplicito 'creation desc' coincide con lo stato iniziale di sortColumn/sortOrder.
Stessa funzione nella dashboard del batch (AdminBatchDashboard)."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "@click=\"toggleSort(item.key)\""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "const toggleSort = (key: string) => {"
  symbol = "toggleSort"

[[entries]]
id = "course-dashboard-progress-labels-it"
title = "Legenda della distribuzione dei progressi in italiano"
layer = "spa-graft"
confidence = "high"
intent = """
Il backend restituisce le fasce di avanzamento in inglese ("Just Started (0-30%)"…) e non
passano da __(): la risposta viene rietichettata in italiano, conservando il suffisso
"(intervallo%)" letto dal tooltip. Siccome le etichette non iniziano più con Just/In/Adv, il
colore della legenda si sceglie per posizione della riga e non per nome. ATTENZIONE al merge
di v2.59.0: l'upstream riscrive proprio quel backgroundColor (row.name.startsWith →
var(--colore-400)), elimina theme, progressColors basato sul json e l'import di
@/utils/frappe-ui-colors.json (file cancellato): tenere la sua sintassi var(--…-400) ma
scegliere il colore con ['red', 'amber', 'blue', 'green'][idx]."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "'Just Started (0-30%)': 'Appena iniziato (0-30%)',"
  symbol = "chartDetails"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "['red', 'amber', 'blue', 'green'][idx]"

[[entries]]
id = "course-dashboard-mobile-layout"
title = "Dashboard del corso impilata in una colonna su mobile"
layer = "spa-graft"
confidence = "high"
intent = """
Su telefono le griglie a più colonne della dashboard rendevano il contenuto più largo dello
schermo e l'intera pagina scorreva di lato. I numeri vanno in una colonna sotto sm, elenco
studenti e grafici laterali in una colonna sotto lg (con min-w-0 perché il contenuto non
allarghi la griglia), legenda e torta impilate sotto sm, l'elenco studenti scorre in
orizzontale dentro il suo riquadro."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "grid grid-cols-1 lg:grid-cols-[2fr_1fr] gap-5 items-start"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = "grid grid-cols-1 sm:grid-cols-[2fr_1fr] gap-4 sm:gap-0"

[[entries]]
id = "course-dashboard-search-by-name"
title = "Ritocchi di stile/traduzioni nella dashboard del corso"
layer = "spa-graft"
confidence = "high"
intent = """
Ritocchi senza regola di business: il campo di ricerca studenti è compatto (classe small-form
del tema elite) ed etichettato "Cerca per nome", perché cerca solo sul nome; i riquadri hanno
la classe card del tema elite; l'intestazione dell'elenco usa un bordo inferiore invece dello
sfondo grigio. Da riapplicare solo se l'upstream riscrive la pagina."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseDashboard.vue"
  anchor = ":placeholder=\"__('Search by name')\""

[[entries]]
id = "courses-programs-theme-card-hooks"
title = "Classe card del tema elite su riquadri e card di corsi e programmi"
layer = "spa-graft"
confidence = "high"
intent = """
La classe card è l'aggancio dello stile delle card del tema elite
(frontend/src/styles/theme/elite/main.css): aggiunta ai riquadri numerici delle dashboard
(NumberChartGraph, usato da dashboard corso, dashboard batch e statistiche video) e alle card
dei programmi, per l'amministratore e per lo studente. Ripristinata dopo un reset delle classi
Tailwind (commit 7574da03e). Senza la classe le card perdono sfondo e ombra del tema.
ATTENZIONE al merge di v2.59.0: l'upstream cambia le classi del titolo nella riga subito
sotto le nostre (text-xl-semibold → text-lg-semibold): tenerle entrambe."""

  [[entries.sites]]
  file = "frontend/src/components/NumberChartGraph.vue"
  anchor = "<div class=\"border rounded-lg p-3 space-y-2 card\">"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/Programs.vue"
  anchor = "class=\"border rounded-md p-3 hover:border-outline-gray-3 cursor-pointer space-y-2 card\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/StudentPrograms.vue"
  anchor = "class=\"border rounded-md p-3 hover:border-outline-gray-3 cursor-pointer card\""

# --- Pagina e modulo del corso -----------------------------------------------

[[entries]]
id = "course-overview-original-shadowed"
title = "CourseOverview originale oscurato dall'override"
layer = "spa-graft"
confidence = "high"
intent = """
Codice morto a runtime: il plugin osOverrideTheme serve al suo posto
frontend/src/overrides/pages/Courses/CourseOverview.vue, dove vivono tutte le
personalizzazioni della pagina corso (course-hero-banner, course-mobile-cta-bar,
course-reviews-removed…). Rispetto a v2.58.1 il file differisce solo per le virgole finali
di Prettier. La voce esiste per ricordarlo: al merge prendere sempre la versione upstream di
questo file (tenendo la riga del marcatore) e riportare nell'override le modifiche upstream
utili. ATTENZIONE al merge di v2.59.0: l'upstream toglie la cache della risorsa outline e la
carica con un watch sul nome del corso (fetch immediato), oltre a ridurre le classi dei
titoli: valutare se portare il watch nell'override. L'ancora è sul marcatore perché la voce
documenta l'oscuramento, non una regola."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseOverview.vue"
  anchor = "OSLMS-CUSTOM: dead code at runtime"

[[entries]]
id = "course-form-overview-theme-hooks"
title = "Agganci del tema elite nella sezione panoramica del modulo corso"
layer = "spa-graft"
confidence = "high"
intent = """
Nella sezione "Panoramica" delle impostazioni del corso, il contenitore dell'editor della
descrizione porta la classe ProseMirror-wrapper (più un anello di focus) e il campo "Corsi
correlati" la classe os-multilink: sono gli agganci con cui il tema elite
(styles/theme/elite/main.css) dà a editor e campo lo sfondo surface-gray-2 degli altri input.
ATTENZIONE al merge di v2.59.0: l'upstream sostituisce il TextEditor di frappe-ui con
RichTextEditor proprio dentro il nostro contenitore: tenere la classe sul contenitore e
verificare che il selettore .ProseMirror-wrapper div div:nth-child(1) colpisca ancora
l'area di scrittura."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseOverviewSection.vue"
  anchor = "class=\"ProseMirror-wrapper rounded-t-lg"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseOverviewSection.vue"
  anchor = "class=\"os-multilink\""

[[entries]]
id = "course-form-mobile-layout"
title = "Creazione e copertina del corso utilizzabili su mobile"
layer = "spa-graft"
confidence = "high"
intent = """
Su telefono il campo copertina del corso (anteprima + pulsanti) e i campi del dialogo "Nuovo
corso" stavano su due colonne e uscivano dallo schermo: sotto sm anteprima e controlli si
impilano, l'anteprima occupa la larghezza disponibile e i pulsanti vanno a capo; il dialogo
usa una colonna."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/CourseThumbnailField.vue"
  anchor = "flex flex-col sm:flex-row items-start gap-3 sm:gap-5"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/NewCourseModal.vue"
  anchor = "grid grid-cols-1 sm:grid-cols-2 gap-5 border-b pb-5 mb-5"

[[entries]]
id = "course-new-modal-title-translated"
title = "Ritocchi di traduzione nel dialogo Nuovo corso"
layer = "spa-graft"
confidence = "high"
intent = """
Il titolo del dialogo "New Course" era una stringa fissa in inglese: passa da __() per
comparire in italiano. Nessuna regola di business."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/NewCourseModal.vue"
  anchor = ":title=\"__('New Course')\""

# --- Elenco corsi (Courses.vue) ------------------------------------------------

[[entries]]
id = "courses-list-students-enrolled-only"
title = "Lo studente vede nell'elenco corsi solo i corsi a cui è iscritto"
layer = "spa-graft"
confidence = "high"
intent = """
Per lo studente l'elenco corsi ha solo la scheda "Iscritto": le schede pubbliche "Pubblicati"
e "In arrivo" sono nascoste, il selettore delle schede sparisce quando resta una sola scheda,
lo studente atterra sempre su "Iscritto" (anche con una scheda salvata in precedenza) e un
?tab= non consentito nell'URL viene ignorato. Effetto collaterale voluto dal commit 5e3e6529d:
è stato tolto il ramo upstream che dava la scheda "Iscritto" a ogni utente non
amministratore, quindi un utente che non è né studente né Moderator/istruttore/valutatore
di certificati vede solo "Pubblicati" e "In arrivo". ATTENZIONE al merge di v2.59.0:
l'upstream rinomina :buttons in :options proprio sul TabButtons che noi abbiamo spezzato su
più righe con il v-if: tenere il v-if e usare :options."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "v-if=\"courseTabs.length > 1\""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "return [{ label: __('Enrolled'), value: 'enrolled' }]"
  symbol = "courseTabs"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "if (tab && courseTabs.value.some((t) => t.value === tab)) {"
  symbol = "setFiltersFromQuery"

  [entries.visibility]
  subject = "schede Pubblicati e In arrivo dell'elenco corsi"
  allow = ["Moderator", "Docente", "course-instructor", "Batch Evaluator", "Valutatore"]
  deny = ["Student"]

[[entries]]
id = "courses-list-persist-tab"
title = "L'elenco corsi ricorda la scheda selezionata"
layer = "spa-graft"
confidence = "high"
intent = """
La scheda scelta nell'elenco corsi resta selezionata quando si esce e si torna (localStorage
'lms_courses_tab'); se la scheda salvata non è più disponibile per il ruolo dell'utente si
torna alla prima disponibile. Stessa funzione nell'elenco batch."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "useLocalStorage('lms_courses_tab', 'live')"

[[entries]]
id = "courses-list-category-per-tab"
title = "Filtro categoria dell'elenco corsi limitato alla scheda attiva"
layer = "spa-graft"
confidence = "high"
intent = """
Il menu delle categorie elencava solo le categorie dei corsi pubblicati, quindi risultava
vuoto sulle schede con bozze o iscrizioni (Creati, Non pubblicati, Iscritto). Ora le
categorie si ricaricano per la scheda attiva con gli stessi filtri di get_courses (il backend
get_course_categories li accetta; l'override os_lms applica lo scoping del valutatore), il
menu si nasconde se la scheda non ha categorie e una categoria scelta su un'altra scheda
viene azzerata al cambio scheda. Si usa una risorsa semplice perché createListResource
lasciava data vuoto con opzioni { label, value }."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = ":options=\"categoryOptions\""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "const reloadCategories = () => {"
  symbol = "reloadCategories"

[[entries]]
id = "courses-programs-list-search"
title = "Ricerca per parole indipendenti negli elenchi di corsi e programmi"
layer = "spa-graft"
confidence = "high"
intent = """
La casella di ricerca costruiva un unico LIKE sull'intera frase ("%Corso p%"), quindi
aggiungere una seconda parola azzerava i risultati: searchLikeFilter divide la ricerca in
parole e le cerca in ordine con spazi liberi ("%Corso%p%"). Inoltre il ricaricamento parte
da update:modelValue e non dall'evento input del DOM, che arrivava prima dell'aggiornamento
del v-model: la ricerca era sempre in ritardo di un carattere e svuotare la casella non
toglieva il filtro. Stessa correzione negli elenchi di batch, quiz, compiti ed esercizi di
programmazione (fuori da questo gruppo)."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "const titleFilter = searchLikeFilter(title.value)"
  symbol = "updateTitleFilter"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "class=\"w-full lg:w-40\"\n\t\t\t\t\t@update:modelValue=\"updateCourses()\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/Programs.vue"
  anchor = "const titleFilter = searchLikeFilter(title.value)"
  symbol = "updateTitleFilter"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/Programs.vue"
  anchor = "@update:modelValue=\"updatePrograms()\""

[[entries]]
id = "courses-list-certification-first-click"
title = "Il filtro Certificazione dell'elenco corsi si applica al primo clic"
layer = "spa-graft"
confidence = "high"
intent = """
La casella "Certificazione" usava @change, che scatta prima che il Checkbox di frappe-ui
aggiorni il modello: il filtro si applicava con un clic di ritardo. Con update:modelValue
legge il valore aggiornato. Il filtro di certificazione include anche i corsi con solo
certificato TrueSkills (update_course_filters lato server). L'ancora è su due righe perché
l'evento da solo compare tre volte nel file."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = ":label=\"__('Certification')\"\n\t\t\t\t\t\t@update:modelValue=\"updateCourses()\""

[[entries]]
id = "courses-list-gestore-no-import"
title = "Import dei corsi da file nascosto al Gestore"
layer = "spa-graft"
confidence = "high"
intent = """
Il Gestore può creare i corsi solo a mano: dal menu "Crea" spariscono "Importa tramite Data
Import" e "Importa tramite ZIP". Il System Manager li conserva anche se ha il ruolo Gestore,
perché l'Administrator ha implicitamente ogni ruolo e altrimenti perderebbe l'import. La
regola controlla il nome del ruolo in roles (non esiste un flag is_manager). ATTENZIONE al
merge di v2.59.0: l'upstream rinomina le icone del menu ('book-open' → 'lucide-book-open',
'upload' → 'lucide-upload', 'folder-plus' → 'lucide-folder-plus') dentro l'array che noi
abbiamo spezzato in menu + push: riapplicare i nuovi nomi nella nostra struttura."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "const canImportCourse = computed(() => {"
  symbol = "canImportCourse"

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "if (!canImportCourse.value) return menu"
  symbol = "courseMenu"

  [entries.visibility]
  subject = "import dei corsi da Data Import e da ZIP"
  allow = ["Moderator", "Course Creator", "System Manager"]
  deny = ["Gestore"]

[[entries]]
id = "courses-list-upcoming-students-unpublished"
title = "Scheda In arrivo senza filtro pubblicati per lo studente"
layer = "spa-graft"
confidence = "low"
intent = """
Aggiunta con il commit 7c44a7dc8 ("accesso ai corsi tramite i batch"): sulla scheda "In
arrivo" lo studente non riceveva il filtro published=1, presumibilmente per vedere i corsi
non pubblicati dei suoi batch. Da courses-list-students-enrolled-only (2026-07-20) lo
studente non vede più la scheda "In arrivo", quindi il ramo oggi non si raggiunge.
Chiedere al committente se toglierlo o conservarlo."""

  [[entries.sites]]
  file = "frontend/src/pages/Courses/Courses.vue"
  anchor = "currentTab.value != 'upcoming'"
  symbol = "updateStudentFilter"

# --- Home ----------------------------------------------------------------------

[[entries]]
id = "home-valutatore-admin-home"
title = "Il Valutatore riceve la home in stile amministratore"
layer = "spa-graft"
confidence = "high"
intent = """
Il Valutatore non è uno studente: la home dello studente lanciava widget per cui non ha
permessi (valutazioni in arrivo, conteggio delle richieste di certificato) e produceva 403.
Home.vue lo manda sulla home in stile amministratore (useAdminHome = isAdmin || is_valutatore),
senza serie giornaliera né conteggio valutazioni, passandogli le proprie lezioni dal vivo
(get_my_live_classes, che include i batch che valuta) invece di quelle amministrative.
AdminHome gli nasconde l'invito "Crea corso" e aggiunge la sezione "Batch che valuti"
(os_lms.os_lms.api.get_evaluation_batches) perché la pagina non resti vuota. Memoria
valutatore-role-scoped-per-batch."""

  [[entries.sites]]
  file = "frontend/src/pages/Home/Home.vue"
  anchor = "const useAdminHome = computed(() => isAdmin.value || isValutatore.value)"
  symbol = "useAdminHome"

  [[entries.sites]]
  file = "frontend/src/pages/Home/Home.vue"
  anchor = ":liveClasses=\"isAdmin ? adminLiveClasses : myLiveClasses\""

  [[entries.sites]]
  file = "frontend/src/pages/Home/AdminHome.vue"
  anchor = "url: 'os_lms.os_lms.api.get_evaluation_batches',"
  symbol = "evaluationBatches"

  [[entries.sites]]
  file = "frontend/src/pages/Home/AdminHome.vue"
  anchor = "!user.data?.is_valutatore"

  [entries.visibility]
  subject = "home in stile amministratore"
  allow = ["Moderator", "Docente", "course-instructor", "Batch Evaluator", "Valutatore"]
  deny = ["Student"]

[[entries]]
id = "home-admin-live-class-card"
title = "Lezioni dal vivo della home amministratore con la card condivisa"
layer = "spa-graft"
confidence = "high"
intent = """
Le lezioni dal vivo della home amministratore usano LiveClassCard, la card unica condivisa
con la home dello studente e la scheda del batch: finestra di accesso di 15 minuti, controllo
che l'ospite abbia avviato la lezione, pulsante "Avvia" per chi conduce, ingresso da
osservatore per il Valutatore anche se la lezione non risulta avviata, link di ingresso
interno protetto (join_live_class) invece dell'URL Zoom/Meet. Memoria
live-class-internal-gated-join. ATTENZIONE al merge di v2.59.0: l'upstream cambia le classi
del titolo dentro la card inline che noi abbiamo sostituito: tenere LiveClassCard."""

  [[entries.sites]]
  file = "frontend/src/pages/Home/AdminHome.vue"
  anchor = ":observer=\"Boolean(cls.is_valutatore)\""

  [[entries.sites]]
  file = "frontend/src/pages/Home/AdminHome.vue"
  anchor = "import LiveClassCard from '@/components/LiveClassCard.vue'"

[[entries]]
id = "home-welcome-video-hero"
title = "Video di benvenuto in cima alla home"
layer = "spa-graft"
confidence = "high"
intent = """
La home mostra in cima il video di benvenuto configurato dal cliente
(oslms/components/Home/WelcomeVideoHero.vue: video o iframe, si riduce a fine riproduzione).
Il componente decide da sé se mostrarsi; nella pagina upstream va solo montato."""

  [[entries.sites]]
  file = "frontend/src/pages/Home/Home.vue"
  anchor = "<WelcomeVideoHero />"

[[entries]]
id = "home-live-classes-toggle"
title = "Nessuna richiesta delle lezioni dal vivo quando sono disattivate"
layer = "spa-graft"
confidence = "high"
intent = """
Le lezioni dal vivo si possono disattivare per tutto il sito con l'impostazione
enable_live_classes (gruppo Configurazioni delle impostazioni os_lms): la home non chiede più
né le lezioni dello studente né quelle amministrative. Stessa regola nella scheda del batch
(BatchDetail.vue)."""

  [[entries.sites]]
  file = "frontend/src/pages/Home/Home.vue"
  anchor = "settingsStore.settings.data?.enable_live_classes !== 0"
  symbol = "liveClassesEnabled"

  [[entries.sites]]
  file = "frontend/src/pages/Home/Home.vue"
  anchor = "auto: liveClassesEnabled.value && isAdmin.value ? true : false,"
  symbol = "adminLiveClasses"

# --- Programmi (ProgramForm.vue, Programs.vue) ---------------------------------

[[entries]]
id = "program-form-title-rename"
title = "Il titolo del programma si modifica rinominando il documento"
layer = "spa-graft"
confidence = "high"
intent = """
LMS Program ha come nome il titolo (autoname field:title, allow_rename). Il campo Titolo
dell'upstream era legato a program.name, quindi su un programma esistente modificarlo non
cambiava nulla di persistente. Ora il campo è legato a program.title e, se al salvataggio il
titolo è diverso dal nome, il documento viene prima rinominato con frappe.client.rename_doc e
poi salvato col nuovo nome. Nota: validateTitle (upstream) pulisce ancora program.name e non
program.title."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "v-model=\"program.title\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "'/api/method/frappe.client.rename_doc',"
  symbol = "updateProgram"

[[entries]]
id = "program-form-description-editor"
title = "Descrizione del programma modificabile con formattazione"
layer = "spa-graft"
confidence = "high"
intent = """
Il modulo del programma ha un editor ricco per la descrizione (campo custom os_lms di LMS
Program), mostrata poi allo studente (program-description). Al salvataggio passa da
sanitizeRichHTML e non dal sanitizer a lista chiusa, che toglieva span/mark/s e gli attributi
style usati per colore, evidenziazione e barrato. L'elenco dei programmi carica il campo
description perché il modulo copia i dati del programma da quell'elenco. Il TextEditor è
quello di frappe-ui: in v2.59.0 l'upstream sostituisce TextEditor con RichTextEditor in altri
moduli, valutare se allinearsi."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = ":content=\"program.description\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "program.value.description = sanitizeRichHTML(program.value.description)"
  symbol = "saveProgram"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/Programs.vue"
  anchor = "'description',"
  symbol = "programs"

  [entries.checks]
  unit = "frontend/src/tests/sanitizeRichHTML.test.ts"

[[entries]]
id = "program-form-add-courses-multi"
title = "Più corsi aggiunti insieme a un programma"
layer = "spa-graft"
confidence = "high"
intent = """
Il dialogo "Aggiungi corso al programma" usa un MultiSelect: si scelgono più corsi in una
volta, quelli già presenti nel programma sono esclusi dalla lista e le nuove righe prendono il
titolo del corso dalle opzioni in cache. È stato rotto una volta da un refactor (commit
3255d0a07: dialogo intitolato "Iscrivi membro", campo Link a un ref inesistente, aggiunta
sempre rifiutata): il dialogo serve solo i corsi, i membri hanno i loro due dialoghi.
ATTENZIONE al merge di v2.59.0: l'upstream riformatta proprio l'onClick con currentForm che
noi abbiamo tolto: tenere la nostra versione."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "v-model=\"selectedCourses\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "const addCourses = (close: () => void) => {"
  symbol = "addCourses"

[[entries]]
id = "program-form-add-members-bulk"
title = "Membri del programma aggiunti in blocco o da un batch"
layer = "spa-graft"
confidence = "high"
intent = """
Al posto del singolo campo "Membro del programma" ci sono due azioni: "Aggiungi membro" (più
utenti insieme, esclusi quelli già membri) e "Aggiungi gruppo", che elenca gli iscritti di un
batch scelto (LMS Batch Enrollment, esclusi i già membri) e permette di selezionarli uno a
uno o tutti. Serve a iscrivere un'intera classe a un programma senza inserire gli studenti a
mano. Sparisce il collegamento upstream "crea nuovo utente" (onCreate → openSettings)."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "@click=\"openMemberForm('batch')\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "v-model=\"selectedMembers\""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "const addMembersFromBatch = (close: () => void) => {"
  symbol = "addMembersFromBatch"

[[entries]]
id = "program-form-full-document"
title = "Il modulo del programma lavora sul documento completo"
layer = "spa-graft"
confidence = "low"
intent = """
Invece delle due list resource upstream sui doctype figli (LMS Program Course, LMS Program
Member) il modulo carica l'intero LMS Program con frappe.client.get (righe ordinate per idx),
lo ricarica all'apertura del dialogo e dopo l'aggiornamento dei conteggi, modifica corsi e
membri in locale (rimozione per chiave di riga, riordino) e salva tutto il documento con un
PUT su /api/resource. Il riordino di un programma già salvato scrive l'idx di ogni riga con
frappe.client.set_value. Dai commit risulta che le list resource lasciavano gli elenchi vuoti
(commit 02e82b3bc "fix load program details", 3255d0a07); il motivo esatto della scelta del
PUT al posto di setValue non è scritto."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "url: 'frappe.client.get',"
  symbol = "programDoc"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "/api/resource/LMS%20Program/"
  symbol = "updateProgram"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "selectionsSet.has(item[rowKey])"
  symbol = "remove"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "fieldname: { idx: course.idx },"
  symbol = "updateOrder"

[[entries]]
id = "program-form-translated-selection"
title = "Ritocchi di traduzione nel modulo del programma"
layer = "spa-graft"
confidence = "high"
intent = """
Testi del modulo del programma resi traducibili: il banner di selezione delle righe usa
l'override os_lms di ListSelectBanner (importato direttamente dal percorso overrides) con il
testo "N righe selezionate" passato da selectionText, e le etichette delle colonne (Titolo,
Membro, Nome completo) passano da __(). Nessuna regola di business."""

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "const selectionText = (count: number) =>"
  symbol = "selectionText"

  [[entries.sites]]
  file = "frontend/src/pages/Programs/ProgramForm.vue"
  anchor = "import ListSelectBanner from '@/overrides/frappe-ui/src/components/ListView/ListSelectBanner.vue'"

# --- Offerte di lavoro -----------------------------------------------------------

[[entries]]
id = "job-form-select-placeholder"
title = "Ritocchi di traduzione nel modulo offerta di lavoro"
layer = "spa-graft"
confidence = "high"
intent = """
Le tendine Tipo e Modalità di lavoro mostrano il segnaposto tradotto "Seleziona opzione"
invece di un valore vuoto. Nessuna regola di business. L'ancora è su due righe perché il
segnaposto compare due volte nel file."""

  [[entries.sites]]
  file = "frontend/src/pages/JobForm.vue"
  anchor = ":placeholder=\"__('Select option')\"\n\t\t\t\t\t\t\t:options=\"jobTypes\""
